# Pluiz v2 — 작업 체크리스트

> 📍 [문서 허브](README.md) · **왜**는 [ROADMAP.md](ROADMAP.md) · **상세**는 [BACKLOG.md](BACKLOG.md) · **기록**은 [DEVLOG.md](DEVLOG.md)

**이 문서 하나만 보면 "지금 뭘 하는 중이고 다음이 뭔지" 알 수 있다.**
작업이 끝나면 여기 체크박스를 채우고 DEVLOG에 기록한다. 둘이 한 세트다.

> **다른 문서와 겹치지 않게 하는 규칙**
> 여기엔 **할 일과 순서만** 적는다. 배경·근거·해결방법은 적지 않고 **링크**한다.
> (같은 내용이 두 곳에 있으면 한쪽만 갱신돼 어긋난다 — 이 프로젝트가 이미 겪은 사고다.)

| 표시 | 뜻 |
|---|---|
| `[x]` | 완료 (DEVLOG에 기록됨) |
| `[ ]` | 남음 |
| 🔵 | **지금 하는 중** |
| ⏸️ | 사용자 결정 대기 — 내가 못 정한다 |
| ⚠️ | 위험하거나 먼저 확인이 필요 |

---

## 🔵 지금 하는 중

> # ✅ 2026-09-16 — **API 지출 한도를 풀었다** (사용자)
>
> `429 RESOURCE_EXHAUSTED`로 **모든 모델 호출이 죽어 있었다.** 1-4를 재려다 발견했고,
> 사용자가 https://ai.studio/spend 에서 한도를 올려 **복구됐다**(15/15 실측으로 확인).
>
> 🔑 **D-1 점검(9/21)의 «모델을 한 번 불러 본다»가 이걸 잡는 항목이다** — BL-57 때 넣어
> 뒀는데 **오늘 그 한 줄의 값이 증명됐다.** 시연 당일 아침에도 반드시 돈다.
>
> ⚠️ **다시 나면 시연이 이렇게 보인다** — ① 명령마다 **~40초** 멈춤(SDK 재시도, 실측)
> ② **영어 오류 원문 낭독**([G-14](research/2026-09_안전에러_전수감사.md) — 429는
> `_is_network_error`에 안 걸려 `_dead_end("error")`로 간다) ③ 오프라인 안내도 안 나옴.
> 🔵 캐시가 잡는 22.4%만 살아남는다.

> ## 🚨 2026-09-15 밤 — **모델 별칭이 굴러가 모든 명령이 죽었다가 복구됐다** (BL-57)
>
> `gemini-2.5-flash`는 **버전이 자동으로 굴러가는 별칭**이다. 코드는 그대로인데
> 같은 날 12:23엔 되고 18:27엔 **전부 빈 응답**(`out=0`)이 됐다.
> `thinking_budget=0`으로 막았다 → [BACKLOG BL-57](BACKLOG.md) · mock **48파일 1714/1714**.
>
> ⚠️ **D-1 점검에 «모델을 한 번 불러 본다»가 추가됐다.** 별칭은 또 굴러갈 수 있다.
> ✅ **«Gemini 2.0 Flash» 표기 — 2026-09-16에 닫았다.** 살아 있는 문서
> (`teamwork/01_프로젝트_소개.md` — 10/28 소개글 재료)는 **2.5로 고쳤고**,
> 6월 스냅샷 넷(슬라이드·STUDY_GUIDE·Q&A·발전사)은 **규칙대로 안 고치고**
> 머리에 🚨 **«그대로 말하면 틀린다» 표**를 붙였다 — 모델 · **도구 27→41개**(실측) ·
> `create_react_agent`→`StateGraph`. 전면 갱신은 예정대로 **C1·C5(11월)** 이다.
>
> ### ✅ 같은 날 BL-53 실기도 통과했다 (승인 경로)
>
> 못 알아들은 답이 **재질문**으로 처리되고 승인 대기가 살아남아 「그래」에 **실제로 삭제**됐다.
> ⚠️ **1층(반추 폐기)·2층(어절 상한)은 여전히 미검증** — 아래 「3차에서 볼 것」 참조.
>
> ## 🧭 **2026-09-16 — 시연 이후 개발 방향이 확정됐다**
>
> 사용자 노트를 정리해 **[`docs/planning/개발_계획.md`](planning/개발_계획.md)** 로 확정했다
> (개인 폴더라 저장소에는 없다. 서사는 [DEVLOG](DEVLOG.md) 2026-09-16).
>
> | | |
> |---|---|
> | **~9/19** | ✅ **녹음 수집 도구 완성 · 배포됨** → https://pluiz-collect.vercel.app<br>✅ **안전·에러 전수 목록화**(2026-09-16 · [빈 곳 17개](research/2026-09_안전에러_전수감사.md))<br>✅ **캐시 절감액**(2026-09-16 · [히트율 22.4% · 18배 · $0.13](research/2026-09_캐시_절감액.md))<br>✅ **Vision 기준선**(2026-09-16 · [15/15 · 환각률 0%](research/2026-09_Vision_기준선.md))<br>✅ **오프라인 커버리지**(2026-09-16 · [50.0% · 로컬만 76.9%](research/2026-09_오프라인_커버리지.md))<br>🎉 **§3 ① 다섯 개 전부 닫혔다** |
> | **9/20~9/22** | **시연 — 이 일정은 그대로다**(아래 12일 일정) |
> | **9/23~10/31** | M7 → 응답 품질 → 멀티 API 표 → 오프라인 보강 → 오프라인 TTS → 진행 표시 → BL-55·56 → 속도 측정 |
> | **11월** | 지표 6종 · 실사용 한 달(10/31 시작) · 발표·전시 |
> | 🚫 **범위 밖** | **배포(Installer)** · Vision 자율 인식 · 오프라인 LLM → [ROADMAP § 안 하기](ROADMAP.md) |
>
> 🔴 **임계 경로는 «녹음 20~30명»이고 10/06까지다.** 코드가 아니라 사람이라 당길 수 없다.
>
> - [x] ✅ **도구 완성 · 실기 확인** (2026-09-16) — 링크 → 녹음 → 보내기 → `pull` → `ingest` 가 **끝까지 돌았다**
> - [x] ✅ **뿌렸다** — 2026-09-16 한 번 더 뿌렸다(사용자). 현황 4명. **계속 뿌린다**
> - [ ] 🔴 **본인 것 제대로 한 번 더** — 지금 것은 건너뛴 게 많은 동작 확인용이다(양성 5/20)
> - [ ] 🔁 **`npm run pull` 을 주기적으로** — 알림이 없어서 직접 봐야 한다. 주 2회 권장
>
> **현황: 4명 / 20~30명** (2026-09-16 · 박성규·황순현·김소영·변소윤)
>
> 📌 수집 중 나온 질문의 답 → [`design/M7-1_녹음수집.md`](design/M7-1_녹음수집.md)
> (컴퓨터로만? 아니다 · 소리가 작다? 임계가 틀렸었다 · 앞으로의 절차)
> 🔊 들어보기 → `python scripts/inspect_wakeword.py --play <이름>`

> ### 🔵 남은 실기는 **2건이다** — 2026-09-16에 둘이 사람 없이 닫혔다
>
> - [x] **D-01a** — ✅ **2026-09-16.** 돌려 보니 **갈래에 따라 갈렸다** —
>       `name='에이'`·`extension='티엑스티'` 로 **나눠 오면 못 찾았다**(문턱이 세는 단위가
>       «파일명 전체»가 아니라 «인자 하나»가 돼 있었다). `_match_in`에서만 고쳤고
>       테스트 4건을 박았다 → [DEVLOG 2026-09-16](DEVLOG.md)
>       ⚠️ **«코드가 푼다»까지다.** LLM이 실제로 어떻게 나누는지는 3차 로그의
>       `find_file` 인자를 봐야 안다 — 다만 **어느 쪽으로 나눠도 이제는 찾는다**
> - [ ] **9장면 👁 배지** — 화면을 눈으로 적는다
> - [ ] **BL-51** — 응답 **본문**을 읽는다(로그엔 글자 수만 남는다)
> - [x] ❓ **오프라인 오탐 확인** — ✅ **오탐이 아니다.** 로그에서 오프라인 분기를 탄 **7회가
>       전부** `'오늘 저녁 뭐 먹지'` 이고, 그건 [LAUNCH_CHECKLIST **E-03**](testing/LAUNCH_CHECKLIST.md)
>       (*"Wi-Fi 끈 채"*)이다. 탐침도 멀쩡했다(`8.8.8.8:53` 10/10 · 중앙 38ms, 예산 800ms).
>       **사람이 Wi-Fi를 끄고 돌린 것**이다
> - [x] ✅ **팀원 학번이 public 저장소에 올라가 있던 것** — **2026-09-16 A안으로 닫았다.**
>       실제 범위는 **타인 한 명분**이었다(박세민은 애초에 없었다). 작업본 D01·D02·D08 +
>       DEVLOG에서 **전부 «제출 시 채움»으로 바꿨다** — 작업본은 원래 제출 직전 hwp로
>       옮기는 물건이라 **잃는 게 없다.**
>       🚫 **B안(히스토리 재작성+force push)은 안 한다** — ① force 뒤에도 SHA로 남는다
>       ② **10/20 병합(19커밋 분기)과 부딪힌다** ③ 협업자 clone이 깨진다.
>       ⚠️ **히스토리에는 그대로 있다.** 근거·재검토 조건 → [teamwork/학번_공개노출_대응.md](teamwork/학번_공개노출_대응.md)
> - [x] ✅ **AI Studio 결제 상태 확인** — **유료 Tier 1 · 총 ₩3,340 · 절감 ₩0.00**(2026-09-16).
>       🔑 «절감 0»이라 **컨텍스트 캐싱이 비어 있는 기회**로 확정됐다
>       → [캐시 절감액 §7](research/2026-09_캐시_절감액.md)
> - [ ] 🎤 **Windows 음성 액세스 대조표 20문장** — Wi-Fi 끄고 음성 액세스로 같은 20문장.
>       Pluiz 쪽은 다 쟀다. **차별점 주장에 필요한 건 대조군뿐이다**
>       → [오프라인 커버리지 §6](research/2026-09_오프라인_커버리지.md)
> - [ ] 📣 🔴 **오다영에게 알린다** — *"파일에선 지웠는데 히스토리엔 남아 있어. 원하면 지울게."*
>       **내가 대신 정할 수 없다**(남의 개인정보). 🗓️ **10/20 병합 전에** 답을 받아 두는 게 싸다
> - [ ] ⚠️ **D-1 점검에 «망이 TCP 53을 막는지»를 넣었다** — 아래 참조.
>       탐침이 TCP 53을 쓴다. **전시장 망이 막으면 인터넷이 되는데도 «오프라인»이 된다**


> ## 🧭 축이 둘이다 — **개발(9/22)** 과 **문서·발표·서류(10/28~11/24)**
>
> **2026-09-15에 학과 산출물 8종(D01~D08)이 드러났다.** 어느 문서에도 없던 축이다.
> 전체 지도는 **A 개발 · B 저장소 · C 발표 · D 제출물 · E 미팅 · F 산출물** 6축이고,
> 사람별 분담과 과제 지시서는 [`docs/teamwork/`](teamwork/)에 있다.
>
> | | 어디 |
> |---|---|
> | **산출물 D01~D08** (md로 쓰고 제출 직전 hwp로) | [`documentations/작업본/`](documentations/작업본/README.md) |
> | **팀원에게 넘기는 것** (오다영: 포스터·소개글·시장조사·발표) | [`teamwork/README.md`](teamwork/README.md) |
> | ⏸️ 박세민 과제 A·B·C | 준비만 됨 — **배정 보류** |
>
> ### 🔴 지금 걸려 있는 행동 항목 (개발 아님)
>
> - [x] ✅ **오다영 GitHub 협업자 초대** — 2026-09-16 완료
> - [ ] 🗓️ **`feature/byeonsoyun` → `develop` → `main` 병합** — **10/20까지.**
>       ⚠️ fast-forward가 아니다(19커밋 분기). **별도 세션으로 결과를 눈으로 본다.**
>       🚨 소개글이 **공개 Github URL**을 요구하는데 지금 `main`은 **2026-09-01**이다
> - [ ] ❓ **D01~D08 제출 기한 확인** — 10월 중순~말 추정.
>       기한이 10월 중순이면 **D07(최종 구현 후 문서)이 기능 동결보다 먼저 온다**
> - [ ] ❓ 팀 번호 · 프로젝트 유형 · 박세민 학번 · 개발 시작일 · 미팅 요일
>
> 📌 **세션 서사는 [DEVLOG](DEVLOG.md) 최상단**에 있다. 여기엔 할 일만 적는다.


> ## 🚩 새 세션이면 이 한 줄만 보면 된다
>
> # 🔴 2026-09-22 시연이 기한이다 (교수님 미팅, 2026-09-10)
>
> 요구는 **«배포»가 아니라 «전체 기능이 제대로 실행되는 모습»** 이다.
> 그래서 11월 항목(패키징·자동 업데이트·평가지표 집계·발표자료 전면 갱신)은
> **기한 밖**으로 내보냈고, **«사람 앞에서 깨지는 것»** 기준으로 다시 줄 세웠다.
> **시연 이후에도 개발은 계속되므로 «완벽»이 아니라 «끊기지 않는 것»이 목표다.**
> 일정·대본·재판정 근거는 아래 **「📅 9/22 시연 — 12일 일정」** 절에 있다.
>
> ## ✅ **BL-53을 닫았다** (2026-09-15) — 🔵 **다음은 9/20 3차 리허설이다**
>
> **2차 리허설이 잡은 🔴 하나가 닫혔다. [즉시/위험] 미해결이 0건이 됐다.**
> mock **47파일 1708/1708**(실패 0 · 1672 → **+36**). 같은 날 ⏸️ **결정 2건**도 닫았다 — **M-01 `open_app` 확대** · **재료비 버림.**
> **⏸️ 가 셋에서 하나(D-01b)로 줄었다.**
>
> ### 🔑 원인이 «STT 환각»이 아니라 **«우리 프롬프트의 반추»** 였다
>
> 승인 대기 중 올라온 49자는 지어낸 말이 아니라 `services/stt.py`의
> `_WHISPER_PROMPT`(108자)를 Whisper가 **그대로 되뱉은 연속 조각**이었다 —
> 15번째 글자부터, 한 글자도 다르지 않다.
> 🚨 **그래서 «STT 쪽이라 여기서 못 끝낸다»는 판단이 틀렸다.** 우리가 만든 입력이라
> 우리가 막을 수 있었다 → [BACKLOG BL-53](BACKLOG.md)
>
> | 층 | 무엇 | 어디 |
> |---|---|---|
> | **1** | 반추면 **버린다**(→ *"인식하지 못했습니다"*, 승인 대기는 살아 있다) | `services/stt.py` |
> | **2** | 승인 대기 중 명령 인정에 **어절 상한 8** | `core/graph.py` |
> | **3** | `no_speech_prob`·`avg_logprob` **계측만**(임계는 3차 로그로) | `services/stt.py` |
>
> ⚠️ **`other_command` 분기는 그대로다**([절대 규칙 11](../CLAUDE.md)). 상한을 넘으면
> **재질문**이고, 두 번 다 못 알아들으면 *"취소했습니다"* 다 — 조용히 새지 않는다.
>
> ### 👉 그래서 다음은 **9/20 3차 리허설**이다 — 9/17·9/18이 비어 있다
>
> **코드로 할 수 있는 건 또 없어졌다.** 남은 것은 전부 **사람이 마이크 앞에 앉아야**
> 갈린다. 3차에서 **반드시 같이 볼 것**:
>
> | 볼 것 | 어떻게 판정하나 |
> |---|---|
> | ✅ **BL-53** | **2026-09-15 18:47 승인 경로 통과** — 못 알아들은 답(`'플루이즈 빠이'`)이 **재질문**으로 처리되고 승인 대기가 살아남아 「그래」에 **실제로 삭제**됐다. ⚠️ **1층(반추 폐기)·2층(어절 상한)은 여전히 미검증** — 반추가 안 났고 8어절 넘는 입력도 안 왔다 |
> | 🆕 **3층 계측** | `[STT] whisper 신뢰도 \| no_speech=… \| avg_logprob=…` 를 **모은다.** 🎁 **첫 실측값이 나왔다**(`no_speech=0.006 \| avg_logprob=-0.712`, 정상 인식 케이스). 오인식·반추 케이스의 값을 더 모아야 임계가 정해진다 |
> | ✅ **D-01a** | **2026-09-16에 코드로 닫았다** — 나눠 온 갈래가 비어 있었고 고쳤다(테스트 4건). 3차에서 볼 것은 **LLM이 인자를 어떻게 나누는가**(로그의 `find_file` 인자)뿐이다 |
> | ⬜ **9장면(👁 배지)** | 로그로는 `사유=detected` 로 돌았는데 **화면을 아무도 안 적었다** |
> | ⚠️ **BL-51** | 응답이 `34자`였다는 것만 로그에 남는다. **말이 세 번 바뀌었는지는 본문을 봐야 한다** |
> | 🆕 **G-07** | 메모장에 글자를 쓰고 **저장하지 않은 채** *"메모장 꺼줘"* → 정말 꺼지는가 · 내용은 어떻게 되는가. `close_app`이 종료를 **확인하지 않는다** |
> | 🆕 **G-08** | 설정을 한 번 열었다 닫은 뒤 *"설정 최대화해줘"* → **말만 하는가**(유령 창) |
> | 🆕 **G-02** | 리허설 뒤 `pluiz.log`에서 `[캐시 실행]` 줄 수와 «턴 완료» 수가 맞는가. **캐시 도구 실패는 로그에 안 남는다** |
>
> 🆕 셋은 **2026-09-16 전수 감사**가 찾은 것이다 → [`research/2026-09_안전에러_전수감사.md`](research/2026-09_안전에러_전수감사.md).
> **코드를 안 고치고** 리허설 한 판으로 갈리는 것만 골라 올렸다.
>
> ### ⏸️ 남은 결정 — **하나뿐이다** (셋 중 둘이 2026-09-15에 닫혔다)
>
> - ~~**M-01을 `open_app`까지 넓힐까**~~ → ✅ **2026-09-15 넣었다.**
>   근거는 «무해해서»가 아니라 **«멱등해서»**(이미 켜져 있으면 포커스만 한다).
>   🚨 허용목록을 **합치지 않고** 칸을 나눴다 → [M3-2 §6-1](design/M3-2_미시도_단계_재시도.md)
> - 🔵 **D-01b** — 경로를 모르는 파일 탐색. **ADR 필요.**
>   ⚠️ **9/19 «새 기능 시작 금지»에 걸린다** — 시연 뒤로 미루는 쪽을 권한다
> - ~~🗓️ **캡스톤 재료비 지원 — 신청 마감 9/30**~~ → ❌ **2026-09-15 «버린다»로 결정.**
>   불가 품목을 보면 살 것이 없다. **⏸️ 가 하나 줄었다** → [ROADMAP](ROADMAP.md)
>
> ### 🟡 2차 리허설이 곁다리로 잡은 것 둘 — **2026-09-15에 번호를 붙였다**
>
> - **[BL-55](BACKLOG.md)** — `get_running_apps`가 **프로세스를 센다**(`tools/system.py:528`).
>   창이 없는 백그라운드까지 «켜져 있는 앱»이라 **사용자가 보는 기준과 다르다**
> - **[BL-56](BACKLOG.md)** — *"지금까지 한 거 뭐 했는지 정리해 줄래"* → **`사유=못함`**.
>   🔑 **재료는 이미 다 있다**(히스토리·턴 로그) — «그걸 보는 자리»가 없을 뿐이다
>
> ### ⏸️ BL-53이 남긴 것 — **시연 뒤에 한다**
>
> - **프롬프트를 문장 꼴로** 바꾸면 반추 확률 자체가 내려간다. 다만 **모든 음성 명령의
>   인식이 같이 움직여** 시연 6일 전 전역 변경은 아래 「일정이 깨지는 경로 ③」 그 자리다
> - **3층 임계값** — 실측 없이 정하면 추측이다(BL-23의 교훈). **3차 로그를 보고 정한다**
>
> 📌 **10/28 마감이 둘 있다**(작품 소개글 hwp · 포스터 PPT A2). 서류라 여기 안 올린다 —
> 전부 [ROADMAP § 전시회 제출물](ROADMAP.md)에 있다.
>
> 📋 **2차 리허설에서 사용자가 채운 진행표** → [testing/REHEARSAL_2.md](testing/REHEARSAL_2.md)

<details><summary>▶ 2026-09-14 밤 — 2차 리허설 결과 (BL-53 ⏸️ 결정 대기였던 때)</summary>

> ## 🔵 **다음 세션은 [BL-53](BACKLOG.md)부터** — ⏸️ **고칠지는 사용자가 정한다**
>
> **2026-09-14 밤에 2차 리허설을 돌았다(9/17에서 사흘 앞당겼다).**
> **대본 9장면이 7분 24초에 안 끊기고 끝났다.** mock **46파일 1672/1672**.
> 고친 셋 중 둘이 사람 앞에서 사라진 것을 확인했고, **새 결함 둘**이 나왔다.
>
> | | 무엇 | 판정 |
> |---|---|---|
> | 🔴 **[BL-53](BACKLOG.md)** | **승인이 STT 환각 하나에 깨졌다.** *"그래"* 가 `whisper 폴백`에서 **49자**로 지어내져 `other_command`가 됐다 | ⏸️ **고칠지 결정 대기** |
> | 🟢 **[BL-54](BACKLOG.md)** | 호출어를 바꿔도 **모델은 계속 «플루이즈»를 듣는다**(불일치 검사가 시작할 때만 돈다) | ⏸️ **10월 [M7](design/M7_웨이크워드_재구축.md)로 미뤘다** |
>
> ### 👉 그래서 ⏸️ **결정이 하나다 — BL-53을 9/22 전에 고칠까**
>
> | | 시연에서 터질 확률 | 고치는 비용 |
> |---|---|---|
> | **BL-53** | 🔴 **높다.** 대본에 삭제 장면이 **둘**이고, 터지면 «정직하게 말한다»가 아니라 **«승인»이라는 간판**이 깨진다 | 작을 수 있다 — 승인 대기 중 답이 **«예/아니오 계열인가»** 를 먼저 보면 49자짜리는 애초에 안 들어온다 |
>
> ⚠️ **`other_command` 분기를 없애는 방향은 안 된다** — [절대 규칙 11](../CLAUDE.md)이 막는 자리다.
>
> ### ✅ 2차 리허설이 닫은 것
>
> | | 근거 |
> |---|---|
> | **[BL-50](BACKLOG.md)** | 캐시에 **새 항목 0개.** `'그거 꺼 줘'` 가 실행되고도 안 박혔다 — 게이트가 먹었다 |
> | **M-08**(M-01 누수) | `[M-01]` 재시도가 **한 줄도 안 찍혔다.** 삭제는 안 묶였고 허용목록이 안 샜다 |
> | **M-07** | 계획 2/2 둘 다 실행 · `[M-01]` 줄 없음 = **«해당 없음»**(문서가 예고한 정상 경우) |
> | **8장면** | 가져오기가 백업을 떴다(`command_cache.backup-20260914-214122.json`) |
>
> ### ⚠️ 실기 미검증으로 **남은** 것
>
> - **D-01a(`_romanize_ko`)가 한 번도 안 돌았다.** STT가 «에이점 티엑스티»를 **`a.txt`로 먼저 바꿔서**
>   코드에 넘겼다. **결과는 통과지만 코드가 푼 게 아니다** — 3차에서 STT가 다르게 받으면 그때 갈린다
> - **9장면(👁 배지)** — 로그로는 `감시 시작 → 종료 | 사유=detected` 로 돌았는데 **화면은 아무도 안 적었다**
> - **[BL-51](BACKLOG.md)** — 응답이 `34자`였다는 것만 로그에 남는다. **말이 세 번 바뀌었는지는 본문을 봐야 한다**
>
> ### 🆕 리허설이 곁다리로 잡은 것 둘 (아직 BL 번호 없음)
>
> - `get_running_apps`가 **프로세스를 센다**(`tools/system.py:528`) — 창이 없는 백그라운드까지
>   «켜져 있는 앱»이다. 사용자가 보는 기준과 다르다
> - *"지금까지 한 거 뭐 했는지 정리해 줄래"* → **`사유=못함`**. 시연에서 나올 법한 질문이다
>
> ### ⏸️ 그대로 남아 있는 결정 (2026-09-14 낮에 생겼다)
>
> - **M-01을 `open_app`까지 넓힐까** → [M3-2 §6-1](design/M3-2_미시도_단계_재시도.md)
> - **D-01b** — 경로를 모르는 파일 탐색. **ADR 필요**
> - 🗓️ **캡스톤 재료비 지원 — 신청 마감 9/30.** 불가 품목을 보면 **신청할 게 없을 수 있다.**
>   버릴 거면 지금 버려야 잊지 않는다 → [ROADMAP § 전시회 제출물](ROADMAP.md)
>
> 📌 **10/28 마감이 둘 있다**(작품 소개글 hwp · 포스터 PPT A2). 서류라 여기 안 올린다 —
> 전부 [ROADMAP § 전시회 제출물](ROADMAP.md)에 있다.
>
> 📋 **2차 리허설에서 사용자가 채운 진행표** → [testing/REHEARSAL_2.md](testing/REHEARSAL_2.md)

</details>

<details><summary>▶ 2026-09-14 낮 — ⏸️ 3건 결정 + M-01·D-01a 구현</summary>

> ## 🔵 **다음 세션은 2차 리허설이다** — 이제 **내가 할 수 있는 건 없다**
>
> **2026-09-14에 ⏸️ 3건이 전부 결정됐고, 고치기로 한 둘은 같은 날 닫았다.**
> mock **46파일 1672/1672**. 남은 것은 **사람이 마이크 앞에 앉는 것**뿐이다.
>
> | ⏸️ 였던 것 | 결정 | 결과 |
> |---|---|---|
> | **M-01** 안 해 본 단계를 실행해 달라 | **읽기전용만 재시도** | ✅ [M3-2 ADR](design/M3-2_미시도_단계_재시도.md) · `test_plan_node` 61 → **77** |
> | **D-01** 파일명 발음 표기 | **(a) 발음만 먼저** | ✅ `_romanize_ko` · `test_find_file` 42 → **57**. ⏸️ (b) 경로 탐색은 남았다 |
> | **대본 1** 메모장 위치 | **안 고친다** | Windows 동작이고 사용자도 *"맞긴 한데"* 라고 썼다 |
>
> 🚨 **둘 다 «고치다 새 거짓말을 만들 뻔한» 자리였다** — 자세한 건 [DEVLOG](DEVLOG.md) 최상단.
>

> **1차 리허설이 잡은 셋을 같은 날 전부 닫았다.** 셋 다 «사람 앞에서 보이는» 것이었다.
> mock **46파일 1641/1641**.
>
> | | 무엇이었나 | 어떻게 닫았나 |
> |---|---|---|
> | ✅ **[BL-51](BACKLOG.md)** | 한 응답에서 **했다 → 못 했다 → 또 했다** (대본 7장면) | 원인이 **둘**. 접미를 **최종 본문**으로 판정 + `prune_done_steps` |
> | ✅ **[BL-50](BACKLOG.md)** | `'그거 꺼줘'` 가 `close_app(메모장)` 으로 학습 (대본 2장면) | **L4 지시대명사 게이트** — `learn`·`find`·`suggest` **세 입구 전부** |
> | ✅ **[BL-52](BACKLOG.md)** | 로그 `도구=` 가 «실행»이 아니라 «요청» | `요청=` · `실행=` 으로 **칸을 나눔** (소비자도 같이) |
>
> ### 👉 그래서 다음은 **2차 리허설**이다 — 고친 것이 사람 앞에서 정말 사라졌는지
>
> 진행표는 그대로다 → **[testing/LAUNCH_CHECKLIST.md](testing/LAUNCH_CHECKLIST.md)**.
> `[ ]` 로 남은 것이 다음 판이고, **이번엔 아래 둘을 반드시 같이 본다**:
>
> | 볼 것 | 왜 |
> |---|---|
> | **대본 7장면**(*"메모장 열고 b.txt도 지워줘"* → 승인) | BL-51이 정말 한 문장으로 끝나는가 |
> | **대본 2장면**(*"그거 꺼줘"*) 뒤 **캐시를 확인** | BL-50 — 이제 안 박혀야 한다 |
> | ⚠️ **`WAKE_WORD_ENABLED=false` 로 §1~§7** | 1차 때 켠 채로 돌려 **W-04 판정이 흐려졌다** |
>
> ### 🆕 2차 리허설에서 **새로 볼 것 둘** (2026-09-14에 들어갔다 · 실기 미검증)
>
> | 볼 것 | 어떻게 판정하나 |
> |---|---|
> | **M-01** — 계획이 말로만 때운 단계 | 로그에 **`[Plan] [M-01] 단계 미시도 → 1회 재시도`** 가 찍히는지. 찍히고 **«못 했어요»가 안 붙으면** 먹은 것이다 |
> | **D-01a** — 발음 표기 | *"에이점 티엑스티 찾아줘"* → `a.txt` 가 나와야 한다. ⚠️ **삭제도 같이 본다**(*"에이점 티엑스티 지워줘"*) — 삭제 경로는 코드가 아니라 **모델이 푼다** |
>
> ### ⏸️ 아직 사용자 결정이 남은 것 (2026-09-14에 새로 생겼다)
>
> - **M-01을 `open_app`까지 넓힐까** — 사용자가 든 예(*"메모장 열고 계산기도"*)가
>   지금 안 걸린다. `open_app`은 읽기가 아니라 상태를 바꾼다 → [M3-2 §6-1](design/M3-2_미시도_단계_재시도.md)
> - **D-01b** — 경로를 모르는 파일 탐색. 넓히면 속도·비밀파일 노출면이 같이 커진다 → **ADR 필요**
> - 🗓️ **캡스톤 재료비 지원 — 신청 마감 9/30** (2026-09-14 공지). 최대 30만원인데
>   **소프트웨어·앱·USB·주변기기가 전부 불가 품목**이라 이 프로젝트는 **신청할 게 없을 수 있다.**
>   버릴 거면 지금 버려야 잊지 않는다 → [ROADMAP § 전시회 제출물](ROADMAP.md)
>
> 📌 **10/28 마감이 둘 새로 생겼다**(작품 소개글 hwp · 포스터 PPT A2). 기능이 아니라
> 서류라 여기 체크리스트에 안 올린다 — 전부 [ROADMAP § 전시회 제출물](ROADMAP.md)에 있다.


</details>

<details><summary>▶ 2026-09-12 밤 — 1차 리허설 결과 (셋을 잡았고 같은 날 닫았다)</summary>

> ## 🔵 **다음 세션은 BL-51 → BL-50** (2026-09-12 1차 리허설에서 나왔다)
>
> **1차 리허설을 돌았다. 대본 9장면이 끝까지 갔다** — «끊기지 않는다»는 확인됐다.
> 그런데 **대본 자체가 결함 둘을 만든다.** 둘 다 사람 앞에서 보인다:
>
> | 순서 | 무엇 | 왜 이 순서인가 |
> |---|---|---|
> | **①** | 🚨 **[BL-51](BACKLOG.md)** — 한 응답에서 **했다 → 못 했다 → 또 했다** | **대본 7장면**이 이 경우다. «정직하게 말한다»가 이 프로젝트의 간판인데 **거기서 깨진다** |
> | **②** | 🚨 **[BL-50](BACKLOG.md)** — `'그거 꺼줘'` 가 `close_app(메모장)` 으로 학습된다 | **대본 2장면**이 이걸 만든다. 🚨 **리허설을 돌 때마다 재오염**된다 — 지우는 걸론 안 되고 게이트가 필요하다 |
> | ③ | 🟡 **[BL-52](BACKLOG.md)** — 로그 `도구=` 가 «실행»이 아니라 «요청» | 기능은 멀쩡하고 **판단을 오염시킨다.** 실제로 오판을 한 번 만들었다 |
>
> ✅ **닫힌 것**: [BL-40](BACKLOG.md) 실기 판정(`위험 2개` 한 번 — **A안이 먹었다**) ·
> [BL-46](BACKLOG.md) 라이브 확인(**0.04~0.07초**).
>
> ⚠️ **§1~§7을 웨이크워드 켠 채로 돌렸다**(`→ WAKE` 26회). 체크리스트 §8이 경고한 그대로라
> **W-04(답변 중 음성이 끊긴다)가 그 부작용일 수 있다** — `WAKE_WORD_ENABLED=false`로
> 한 번만 다시 보면 갈린다.
>
> 📋 **채워진 체크리스트가 그대로 남아 있다** → [testing/LAUNCH_CHECKLIST.md](testing/LAUNCH_CHECKLIST.md).
> `[ ]` 로 남은 것이 다음 판이다.
>
> ### ⏸️ 사용자 결정이 필요한 기능 요청 3건 (리허설에서 나왔다)
>
> - **M-01** — 복합 명령의 실패한 단계를 **그냥 실행**해 달라.
>   ⚠️ M3-1이 «실행을 강제하지 않는다»고 **일부러 정한 것**과 부딪힌다 → **ADR 필요**
> - **D-01** — 파일명 한/영·발음 표기(«에이점 티엑스티») + **경로를 모르는 파일 탐색**
> - **대본 1** — 메모장이 마지막 위치에 열린다(«중앙이 아니다»)


</details>

<details><summary>▶ 2026-09-12 낮 — 리허설 전에 닫은 것들</summary>

> ## 🔵 **리허설은 미뤘다 — 코드로 확인 가능한 것부터 계속 간다** (2026-09-12 사용자 결정)
>
> *"리허설은 나중에 할게. 기능적으로 확인할 부분들만 따로 테스트 하고 계속 개발 진행"*
>
> ✅ **BL-46도 닫았다 — 오프라인 실패 턴이 8.01초 → 0.17초다.**
> 백로그의 해법(«오프라인이면 LLM을 아예 안 부른다»)을 그대로 했다. `fast_path`보다
> **뒤**에서 단락해 오프라인 캐시 히트는 그대로 돈다(0.14초).
> → [design/BL-46_오프라인_LLM_생략.md](design/BL-46_오프라인_LLM_생략.md)
>
> 그리고 **§7의 «알려진 결함 2개»를 사람 없이 닫았다** —
> ✅ **결함 A(데이터 손실 위험)는 재현되지 않는다**(BL-37이 이미 막고 있었다) ·
> ✅ **결함 B는 재현됐고 같은 날 고쳤다**(🆕 [BL-48](BACKLOG.md)).
> 🔑 **«실기가 필요하다»고 적힌 것 중 상당수가 코드로 끝난다** — 셋 다 서버 안에서
> 끝나는 판정이었다. mock **46파일 1601/1601**.
>
> ## 👉 **1차 리허설 진행표는 준비돼 있다** (⏸️ 사용자가 원할 때)
>
> 진행표: **[testing/LAUNCH_CHECKLIST.md](testing/LAUNCH_CHECKLIST.md)**.
> **§0.5에 대본 9장면이 리허설의 뼈대로 들어갔고**, 부품 점검(§1~§8)은 그 뒤다 —
> 부품을 먼저 다 보면 사람이 지쳐 **대본을 못 돈다.**
>
> 📉 **미검증이 39 → 33건으로 줄었다.** 3차 실기 로그가 아직 남아 있어
> **다시 돌리지 않고 오프라인 5건(E-01·04·07·08·10)을 닫았다.**
>
> ✅ **그러다 문서에 없던 결함이 나왔고 같은 날 닫았다 → [BL-47](BACKLOG.md).**
> **밝기 명령만 캐시 히트인데 5~6초**였다(다른 캐시 히트는 0.02~1.08초).
> 원인은 BL-46의 LLM 상한이 **아니라** `wmi` 미설치로 인한 **PowerShell 폴백 4.5초**(실측).
> 사용자 결정(*"지금 하자"*)으로 `pip install wmi` — **4.5초 → 0.04초**,
> 40% 점프 → **10%**, 응답에 퍼센트가 돌아왔다(`✓ 밝기: 40% → 50%`).
> 같은 뿌리가 **BL-45의 진짜 원인**이기도 했다 — 이 PC는 밝기를 **읽을 수 있는 PC였다.**
> mock **45파일 1578/1578**(`test_dependencies` 34 → 35).
>
> ## ✅ 2026-09-11 밤 — 실기 3회로 결함 **10개**를 찾아 **8개를 닫았다**
>
> 사용자가 `launch.bat`으로 직접 돌렸고 **로그와 대화 전문**이 결함을 줄줄이 잡았다.
> mock은 **전부 초록인 채**였다. 절차는 [testing/LAUNCH_CHECKLIST.md](testing/LAUNCH_CHECKLIST.md)
>
> | | |
> |---|---|
> | 🔴 **BL-38** | 오프라인 턴 하나가 **세션을 영구 고장**시켰다(입력 8개 침묵) — 해결 |
> | 🔴 **BL-39** | 실패한 턴이 **로그에 한 줄도 없었다**(ERROR 0건인데 6턴 사라짐) — 해결 |
> | 🔴 **BL-41** | 읽기가 막은 복합 명령을 **쓰기가 통과**시켜 캐시가 오염됐다 — 해결 |
> | 🔴 **BL-42** | 오프라인이 **«차별점»이 아니라 «약점»**이었다(45초 · 「네」 미실행 · 얇은 사전) — 해결 |
> | 🔴 **BL-37** | 제안이 *"그림 반 열어줘"* 에 **«크롬»을 제안**했다 — 해결 |
> | 🟡 **BL-44** | *"밝기 줄여줘"* 가 **볼륨 트리거에 빼앗겨** miss였다 — 해결 |
> | 🟡 **BL-45** | 밝기가 **한 번에 40% 점프**했다 — 해결 |
> | 🟡 **BL-40** | 파일 N개 = **승인 N번.** A안(프롬프트)+계측 적용 — ⏸️ **실기 판정 대기** |
> | ⏸️ **BL-46** | 오프라인 실패 응답이 **여전히 8초.** 해법은 «LLM을 아예 안 부르기» |
> | ⏸️ **BL-43** | S1이 **군말에 약하다**(`'볼륨 좀 올려줘'`) — 퍼지가 받아 주고 있다 |
>
> **오프라인은 3차 실기에서 전부 통과했다** — 그림판·볼륨·밝기·«메모장 만들어줘» 차단까지.
> 🚨 **남은 체감 문제는 속도 하나(BL-46)** 이고, 시연 대본에는 오프라인 장면이 없다.
>
> 🔵 **그래서 다음 할 일은 기능이 아니라 리허설이다.**
> ⏸️ 일정표는 1차를 9/13에 두지만 **9/12로 앞당기는 쪽을 권한다**(날이 비어 있다).

</details>

> ## ~~👉 다음 세션은 1차 리허설 준비부터 (⏸️ BL-35 판단도 같이)~~
>
> ✅ **BL-33 — 해결하고 같은 날 실기까지 통과했다.** 고정 8초 타이머를 걷어내고
> **발화 종료 감지**를 넣었다(무음 1.2초 · 상한 30초 · 말이 없으면 STT를 안 부른다 ·
> TTS 되먹임 차단). 로그에 **`[STT] 234자`** — 8초로는 나올 수 없는 길이다.
>
> ✅ **BL-34 — 실기가 바로 다음 것을 잡았다.** *"날씨 알려줘"* 에 **링크로 답하고 있었다.**
> `fetch_web_info`를 직접 돌려 보니 **숫자가 한 개도 없었다** — 검색 스니펫에 실시간
> 기온은 **구조적으로 없다.** `get_weather` 신설(도구 **41개**) → [BACKLOG BL-34](BACKLOG.md)
> > 🚨 **«도구를 더 만들지 않는다»의 전제가 깨졌다.** 그 문장의 근거는 *"«도구가 없어서
> > 실패»한 사례가 하나도 없다"* 였는데, **오늘 처음 확인됐다**(아래 ② 「도구 사용 실측」).
>
> ✅ **BL-35 — 사용자 결정으로 같은 날 고쳤다.** *"찾아볼게요"* 라고 답하고 **도구를
> 하나도 안 부르던 것**. BL-19과 **같은 기계**로 한 번만 재시도한다 —
> 재시도도 실패하면 **응답을 고쳐 쓰지 않는다**(그물의 최악은 «LLM 1회 낭비»여야 한다).
> 🚨 **계획 실행 중에는 안 건다** → [BACKLOG BL-35](BACKLOG.md)
>
> ## 🎙️ 웨이크워드 — **목표가 «시연 넘기기»에서 «제대로 만들기»로 올라갔다** (2026-09-11)
>
> 사용자 결정: *"구글 어시스턴트나 시리처럼 완성도 높게. 학교 서버 빌려서 학습할 수도 있다."*
> **진짜 기한은 9/22가 아니라 전시회 11/24**다. 계획을 ADR로 분리했다
> → **[design/M7_웨이크워드_재구축.md](design/M7_웨이크워드_재구축.md)**
>
> 🔑 **진단: 아키텍처는 이미 맞다.** 실측해 보니 이미 openWakeWord 임베딩 백본 위에
> 작은 헤드를 얹은 **표준 구성**이다(1536 = 96차원 × 16프레임). 고칠 것은 하나다 —
> **학습 분포에 실제 마이크 오디오가 한 조각도 없다.** 양성도 음성도 전부 TTS다.
> 그래서 **놓침 80%와 오탐 50%가 같은 뿌리의 두 얼굴**이다.
> 🚨 음성(negative)이 **TTS 40문장**뿐인데, FA를 결정하는 건 그쪽이다.
>
> **⚠️ 9/22 전에는 모델을 건드리지 않는다.** 시연은 완화책으로 넘기고
> **재구축은 9/23~10월 말**이다(기능 동결 10월 말).
>
> 🆕 **BL-23에 반대 방향이 붙었다** — *"부스럭거리기만 해도 탐지된다"*(사용자, 9/11).
> 30초에 헛깨어남 **6번**. 🚨 **임계값으로는 못 고친다** — 오탐이 `0.990`~`1.000`인데
> 놓침은 `0.5`~`0.6`이다. 「오탐 임계 0.8 → 0.95」 항목은 **이 데이터로 답이 났다(막힌 길).**
> 완화책 셋을 싼 순서로 적어 뒀다 → [BACKLOG BL-23](BACKLOG.md).
>
> ✅ **2026-09-11 결정(사용자) — 웨이크워드는 «시연 후»다.**
> *"시연 전까지 기획한 기능들 전체 개발 이후에 웨이크워드를 다룬다."*
> **9/15·9/16이 기능으로 재배정**됐고(원래 웨이크워드 ①②였다), 재구축은
> [M7](design/M7_웨이크워드_재구축.md)의 9/23~10월 말 일정을 따른다.
> 시연에는 **켜 두고**, 완화책은 ✅ **«헛깨어남을 조용히 닫는다»로 정해 같은 날 넣었다** —
> 웨이크워드가 자동으로 연 녹음이 실패하면 오류를 안 띄우고 오버레이까지 닫는다.
> ⚠️ 사람이 직접 누른 녹음은 **오류를 그대로 보여준다**(삼켜도 되는 건 «아무도
> 시키지 않은 턴»뿐이다). `test_voice_vad` 62 → **78**
>
> 🗓️ **9/12·9/13이 남는다** — BL-31·32·33이 전부 앞당겨져 닫혔다.
> **1차 리허설을 9/12로 앞당기는 쪽을 권한다** — 실기에서 뭐가 나오든 고칠 날이 더 생긴다.
>
> ⚠️ **BL-31·32·33은 mock만 통과했다. 실기 미검증이다** — «코드가 들어갔다»와 «동작한다»는
> 다르다. 확인은 리허설에서 대본과 같이 한다:
> *"개발착수서 찾아줘"* → *"그거 열어줘"*(BL-31) · *"a.txt는 지우고 b.txt 열어줘"* →
> 승인 → **지웠다는 말이 나오는지**(BL-32) · **긴 문장을 끝까지 말해 보기**(BL-33).
>
> **⚠️ 사용자가 눈으로 볼 것 (15분)**: `launch.bat` 띄우고 —
> ① 몇 마디 → 로그에 **`사유=`** 태그(BL-28) ② *"a.txt랑 b.txt 지워줘"*(BL-24)
> ③ 이름을 **틀리게** 대고 파일 찾기(BL-07) ④ ⚙️ 설정 → **내보내기/가져오기**(M6).
> 자세한 절차는 ①에 있다. **D-9 리허설에서 대본과 같이 본다.**
>
> **⏸️ → 되살림**: **웨이크워드(BL-23)** 를 시연에 넣기로 했다(사용자 결정) — 2일 배정.
> **임베딩 캐시 B(제안)** 는 **D-4 조건부**다(위가 깨끗할 때만).
>
> **미룸 유지**: 포인팅 좌표 정밀도 · BL-18(**시연이 단일 화면이라 리스크 0**) ·
> 병렬 실행 · Playwright 조작. **리허설에 흡수**: BL-19 · UI 채팅창 전환 버그.
>
> ## 🔬 2026-09-10 저녁 — **실기가 mock이 못 보던 둘을 잡았다**
>
> 사용자가 라이브로 돌려 로그를 줬고, 거기서 **BL-17·BL-30**이 나왔다.
> 둘 다 **mock은 전부 초록인 채로** 살아 있던 결함이다.
> 같은 로그에서 **BL-29·BL-28·BL-27은 라이브 통과**가 확인됐다.
> 남은 신규 둘(BL-31·BL-32)은 **재현까지 끝나 있고 고치지 않았다.**

> ## 📅 2026-09-10에 닫힌 것
>
> **오전·오후 두 세션이다.** 아래 위쪽 다섯이 오전, 아래쪽 넷이 오후.
>
> | | |
> |---|---|
> | 🔴 **BL-27** | 승인 응답 「그래」가 `close_app`으로 학습돼 있었다. **실기 통과** |
> | 🔴 **BL-28** | 도구를 안 부른 턴의 «사유»가 로그에 없었다 → `사유=` 태그 |
> | 🔴 **BL-24** | 위험 호출이 둘이면 **묻지 않은 삭제가 실행**됐다 |
> | 📊 **도구 사용 실측** | 3주차 항목. 등록 40개 중 **13개** 사용 · 캐시 **0.09s vs LLM 9.25s** · **로그의 2/3가 테스트였다** |
> | ⏸️ **임베딩 캐시** | ADR·구현·실측 완료. **B냐 C냐만 남았다** → 오후에 «보류»로 결정 |
> | 🔴 **BL-29** | 「에러 핸들링 표준화」가 **스타일이 아니라 결함**이었다 — `✗`(52곳)를 T04가 못 봐 **거짓 성공이 사용자에게 나갔다** |
> | ✨ **BL-07** | 파일 찾기. **막고 있던 건 근사 매칭이 아니라 띄어쓰기였다**(3/8 → 7/8) |
> | ✨ **M6** | 내보내기/가져오기. **가져오기는 학습의 다른 입구다** — BL-27 필터를 우회하지 못하게 막았다 |
> | 🚨 **BL-11 재발** | **테스트가 사용자의 실제 캐시를 고치고 있었다.** M6를 만들다 드러났다 |
>
> 세션 서사는 [DEVLOG](DEVLOG.md) 최상단에 있다. 여기엔 쓰지 않는다.
>
> ⏸️ **오디오(웨이크워드·TTS)는 2026-09-09에 «미룸»으로 정했다** — 사용자 결정.
> 항목은 ① 아래 「⏸️ 오디오」 절에 모아 뒀고, **기능을 막지 않는다**(마이크 버튼으로
> 음성을 쓴다). 재개하면 **볼륨 끄고 30초 확인**부터다.
>
> **Phase 2(화면 이해)는 2026-09-07에 종료됐다** — 다섯 기능 전부 실기 확인.
> 계획 수립(10월 항목)도 라이브를 통과해 **기본 켜짐**이 됐다.
> 웨이크워드는 2026-09-09에 **⏸️ 미룸**으로 정해져 ①에서 내려갔다.
> **①에 새로 볼 것은 2026-09-10에 들어간 셋**이다(위 상자).

> **직전 세션에 무슨 일이 있었는지는 [DEVLOG.md](DEVLOG.md) 최상단 한 항목만 읽으면 된다.**
> 여기엔 세션 서사를 쓰지 않는다 — 두 곳에 있으면 한쪽만 갱신돼 어긋난다.
> (2026-09-05에 09-03·09-04·09-05 요약 세 개를 걷어냈다. 전부 DEVLOG에 있던 중복이다)

## 📅 9/22 시연 — 12일 일정 (2026-09-10 확정)

**리허설이 뼈대다.** 이 저장소의 역사가 «mock 1,262개가 전부 초록인데 실기가 둘을 잡는다»
(2026-09-10 BL-17·BL-30)이므로, 기능 추가가 아니라 **리허설 3회**를 축으로 놓고
나머지를 그 사이에 끼운다. **리허설 다음 날은 «거기서 나온 것을 고치는 날»로 비워 뒀다** —
비워 두지 않으면 반드시 밀린다.

| 날짜 | 할 일 |
|---|---|
| ~~9/11~~ | [x] ✅ **BL-33**(실기 통과) + [x] ✅ **BL-34 날씨**(실기 통과) + [x] ✅ **BL-35** — 하루 배정이 **셋을 닫았다.** 🆕 BL-23에 오탐 방향 기록 |
| ~~9/12~~ | [x] ✅ **BL-31** + [x] ✅ **BL-32** — **9/10 밤에 미리 닫혔다.** 이 날이 비었다 |
| ~~9/12~~ | [x] ✅ **1차 리허설 완료** — 대본 9장면이 끝까지 갔다. 결함 셋([BL-50·51·52](BACKLOG.md))을 잡았고 BL-40·46 판정이 닫혔다 |
| ~~9/12~~ | [x] ✅ **1차에서 나온 것 수정 — 셋 다 같은 날 닫았다**([BL-51](BACKLOG.md)·[BL-50](BACKLOG.md)·[BL-52](BACKLOG.md)). mock **1641/1641** |
| ~~9/13~~ | 비었다 — 작업 없음 |
| ~~9/14~~ | [x] ✅ **⏸️ 3건 결정 + 둘 구현**([M-01](BACKLOG.md)·[D-01a](BACKLOG.md)) · [BL-40](BACKLOG.md) 판정 기록 정정. mock **1672/1672** |
| ~~9/14 밤~~ | [x] ✅ 🎬 **2차 리허설 완료 — 9/17에서 사흘 앞당겼다.** 대본 9장면 **7분 24초**, 안 끊겼다. 결함 **2건**([BL-53](BACKLOG.md)·[BL-54](BACKLOG.md)) |
| ~~9/15~~ | [x] ✅ **BL-53 해결** — 원인이 «STT 환각»이 아니라 **«프롬프트 반추»**였다. 3층(폐기·상한·계측). `test_bl53_stt_echo` **31 신설** |
| ~~9/15~~ | [x] ✅ **임베딩 캐시 B — 배선·테스트·문서 완료**(2026-09-11에 앞당겨 끝냈다). `test_cache_suggest` **33건** |
| ~~9/16~~ | [x] ✅ 같이 닫혔다. ⚠️ **실기만 남았다** — Wi-Fi 끄고 확인([M5 §6-5](design/M5_임베딩_캐시.md)) |
| **9/17** | 비었다 — 2차를 9/14에 당겨 돌았고 BL-53도 9/15에 닫혔다 |
| ~~9/15~~ | [x] ✅ **M-01 `open_app` 확대**(⏸️ 결정) · ❌ **재료비 버림**(⏸️ 결정). `test_plan_node` 77 → **82** |
| **9/18** | [ ] **여유일**(2차 수정은 9/15에 끝났다) |
| **9/19** | [ ] 여유일 · **시연 대본 확정.** 이후 **새 기능 시작 금지**<br>🆕 **«앞으로의 계획» 한 장도 같이 확정한다** — 9/22 미팅이 *"완성 버전"* 만이 아니라 **«뭘 어떻게 더 업데이트 할 건가»** 를 같이 요구한다(2026-09-16 사용자 노트에서 드러났다). 초안은 `docs/planning/다루고자_하는_내용.md` §6 |
| **9/20** | [ ] 🎬 **3차 리허설** — 끊김 없이 **두 번** |
| **9/21** | [ ] 🔒 **코드 동결.** 환경만 점검(아래) |
| **9/22** | 🎤 **시연** |

### 🔒 D-1(9/21) 점검 목록 — 코드는 고치지 않는다

- [ ] 🚨 **`.env`의 `WAKE_WORD_ENABLED`를 `true`로 되돌린다** — 2026-09-11에 개발 편의로 껐다.
      **대본 1번 장면이 «플루이즈» 호출이다.** 안 되돌리면 거기서 시연이 죽는다
- [ ] 🚨 **`.env`의 `WAKE_WORDS`가 `플루이즈`인지 확인한다** — [BL-54](BACKLOG.md).
      호출어를 바꾼 채로 켜면 **전용 모델이 버려지고 Whisper 경로(감지율 천장 69%)로 떨어진다.**
      2026-09-14 리허설 뒤 실제로 `메타몽`인 채 남아 있었다
- [ ] 🚨 **모델을 한 번 불러 본다** — 아무 명령이나 하나. **응답이 비면 [BL-57](BACKLOG.md)이다.**
      `gemini-2.5-flash`는 **버전이 굴러가는 별칭**이라 2026-09-15에 실제로 터졌다
      (코드는 그대로인데 같은 날 12:23 정상 → 18:27 전부 빈 응답)
- [ ] 🚨 **시연장 망에서 «오프라인»으로 안 보이는지 확인한다** — 오프라인 탐침이
      `8.8.8.8:53`·`1.1.1.1:53` 에 **TCP**로 붙는다. 이 집에서는 38ms에 붙지만
      **학교·전시장 망이 53을 막으면 인터넷이 되는데도 «인터넷이 연결되지 않아서»** 가 나가고
      **LLM을 아예 안 부른다**([BL-46](BACKLOG.md) — 캐시에 없는 명령이 전부 죽는다).
      확인: 그 망에서 캐시에 없는 말 하나 → 오프라인 문구가 나오면 걸린 것이다.
      막혀 있으면 유선/핫스팟으로 바꾼다 (2026-09-16에 «오탐 아님»을 확인하며 드러난 경로)
- [ ] mock 스위트 실측 — `python scripts/run_mock_suite.py` (숫자를 기억으로 적지 않는다)
- [ ] 테스트가 더럽힌 캐시 되돌리기 — `git status`에 `cache/` 가 뜨면 되돌린다(BL-11 계열)
- [ ] **백업** — `services/wakeword_model.npz` · `cache/command_cache.json`
- [ ] Gemini API 키 잔량 · `logs/pluiz.log` 정리(측정 구간을 깨끗이)
- [ ] 시연에 쓸 파일 미리 만들기 — 바탕화면에 `a.txt` · `b.txt`
- [ ] ⚠️ **외부 모니터를 연결하지 않는다** — 연결하면 [BL-18](BACKLOG.md)이 되살아난다
- [ ] 시연 당일: 서버 먼저 띄우고 **한 번 말을 걸어 예열**한다(첫 LLM 턴이 가장 느리다)

### 🎬 시연 대본 — 9장면 약 7분

순서에 이유가 있다. **③이 ③의 자리에 있어야** «캐시»가 «같은 명령을 두 번째로 할 때»로 보인다.

| # | 말할 것 | 증명하는 것 |
|---|---|---|
| 1 | “플루이즈” → “메모장 켜줘” | 웨이크워드 · 음성 인식 · 앱 제어 |
| 2 | “그거 꺼줘” | **맥락 유지** — 지시대명사를 이전 턴에 묶는다 |
| 3 | “메모장 켜줘” (같은 말 한 번 더) | **캐시 즉시 실행 0.09s vs LLM 9.25s.** 오프라인 차별점 |
| 4 | “지금 화면에 뭐 있어?” | Vision — Phase 2 본체 |
| 5 | “블루투스 어디서 켜?” | 설정을 **열고** 돋보기·고리로 짚는다. **프로젝트의 얼굴** |
| 6 | “a.txt 지워줘” → “응” | **HITL 승인** — 되돌릴 수 없는 동작은 사람에게 묻는다 |
| 7 | “메모장 열고 b.txt도 지워줘” | 계획 2단계 + 승인. 못 한 단계는 **정직하게 말한다** |
| 8 | ⚙️ 내보내기 → 가져오기 | 이식 가능성. «클라우드를 안 한 것»의 대안(M6) |
| 9 | (여유) “오류 뜨면 알려줘” | 화면 감시 — **어떤 이유로 멈추든 알린다**. BL-19 측정도 여기서 |

**⚠️ 실패했을 때 할 말을 대본에 같이 적어 둔다:**

- **포인팅이 빗나가면** → «추정한 위치예요» 라벨을 가리키며 *"Vision의 추정 오차라
  고리로 범위를 보여 줍니다."* **숨기지 않는 것이 설계 의도다.**
- **웨이크워드가 안 깨면** → 한 번만 더 부르고 바로 마이크 버튼.
  *"감지율 개선이 남은 과제입니다."*

### ⚠️ 이 일정이 깨지는 경로 — 버리는 순서를 미리 정해 둔다

1. **웨이크워드 재학습 실패** → **D-6 저녁에 판정**하고 `git checkout services/wakeword_model.npz`.
   대본을 마이크 버튼 기준으로 바꾸면 **시연은 그대로 성립한다.** 일정을 더 쓰지 않는다
2. **1차 리허설에서 결함이 셋 이상** → D-8이 그 완충이고, 넘치면 **임베딩 캐시 B를 버린다**
   (D-4 분기에서 자동으로 그렇게 된다)
3. **D-1에 «하나만 더» 고치고 싶어진다** → 🚨 **가장 흔한 실패.** 이 저장소는 «고치다 옆을
   깼다»가 반복된다(BL-26 1차 수정이 같은 유령에 속아 통과했다). **발견한 것은 백로그에
   적고 시연 뒤에 고친다.**

---

### ① 실기 확인 — 사람이 눈으로 봐야 하는 것 (⚠️ 사용자)

> ## 📋 **확인 절차는 [testing/MANUAL_TESTS.md](testing/MANUAL_TESTS.md)로 옮겼다** (2026-09-10)
>
> **66건이 우선순위와 함께 한 문서에 있다.** «아직 아무도 안 본» 18건이 맨 앞에 있다:
> 🔴 오늘 들어간 것 6 → 🟡 승인·거부 5 → 🟡 웨이크워드 2 → 🟡 계획 수립 5.
> **거기서부터 보면 된다** — 여기엔 «무엇이 왜 미검증인가»만 남긴다.
>
> BL-10에 *"TASKS ①이 정리될 때 절차를 옮겨 오는 것이 나머지 절반"* 이라고
> 적어 둔 것을 이제 했다. [즉시/위험]이 0이라 «지금 하는 중»과 «상시 카탈로그»가
> 더는 섞이지 않는다.

**2026-09-07에 Phase 2 다섯 기능이 전부 통과했다.** 아래 체크된 항목들은
재확인이 필요할 때 쓰는 절차로 남겨 둔 것이다 — **다시 돌릴 필요 없다.**

> ## ⚠️ 2026-09-10에 들어간 것이 **전부 라이브 미검증**이다
>
> `launch.bat` 띄우고 **15분**이면 다 확인된다. 이상하면 **그 증상만** 알려주면 된다.
>
> | 볼 것 | 어떻게 |
> |---|---|
> | ✅ **BL-33** 음성 길이 | **2026-09-11 통과.** `[STT] 234자` 발화가 안 잘렸다. 아직 못 본 것: 말 없이 6초(`nospeech`) · 30초 상한 · 시끄러운 방 |
> | 🔴 🆕 **BL-34** 날씨 (2026-09-11) | *"오늘 날씨 어때"* → **숫자가 나와야** 한다(«확인해볼게요»·링크가 아니라). *"어제 날씨는?"* · *"부산 날씨"* 도. ⚠️ 실제 기온이 맞는지는 **창밖을 보면 된다** |
> | **BL-28** `사유=` 태그 | 아무 말이나 몇 마디 → `logs/pluiz.log`에 `\| 사유=잡담` 이 보이면 된다. 앱을 켜면 `[캐시 실행]` 줄도 보인다 |
> | **BL-24** 다중 삭제 | *"a.txt랑 b.txt 지워줘"* → 질문이 **둘 다 이름을 부르고 «2개»라고 말해야** 한다. 「네」 하면 둘 다, 「아니」 하면 둘 다 안 지워진다 |
> | **BL-27** 「그래」 | 승인 대기가 **아닐 때** 「그래」 → **아무 앱도 안 꺼져야** 한다 (2026-09-10에 한 번 통과했다) |
> | 🆕 **BL-29** 거짓 성공 | 없는 앱을 시킨다 — *"한글 열어줘"*(안 깔린 앱). **"열었어요"라고 하면 안 된다.** 못 열었다고 해야 정상 |
> | 🆕 **BL-07** 파일 찾기 | 이름을 **일부러 틀리게** 대 본다 — 띄어쓰기를 다르게(*"개발 착수서"*)·확장자를 모른 채. 못 찾으면 **"혹시 이건가요?"** 가 떠야 한다 |
> | 🆕 **M6** 내보내기 | ⚙️ 설정 맨 아래 → **내보내기** → zip이 받아진다 → 그걸 **가져오기**. *"이미 있는 건 그대로 뒀어요"* 가 나오면 정상 |
>
> ⚠️ **M6 가져오기는 상태를 바꾼다.** 자기가 방금 내보낸 파일로만 시험하세요.
> (서버가 자동 백업을 남기지만, 되돌릴 일을 안 만드는 게 낫다)

**그 밖에 남은 것:**

- [x] ✅ **「플루이즈」 감지율 측정 — 10번 중 2번** (2026-09-09). **분모가 생겼다.**
  사전 판정대로 **4번 이하 → 녹음이 정당화**됐고, 같은 날 녹음까지 마쳤다.
  - 놓친 창이 대부분 `0.000`이라 **임계 조정으로는 못 고친다**(확인됨).
    ⚠️ 다만 «전부 0.015 이하»는 **과일반화였다** — `0.512`가 나왔다 → [BL-23](BACKLOG.md)
  - «무음 스킵»은 57창 중 10회뿐이라 **에너지 관문도 범인이 아니다.**
    모델이 **듣고 나서** 0을 냈다 → 남는 설명은 **TTS ↔ 실제 마이크 간극**
- [x] ✅ **실제 녹음** (2026-09-09) — `python scripts/record_wakeword.py`.
  143초 · 발화 33번 · 판정 통과. **기존 문서 절차(`sd.rec(150초)` 한 줄)는 쓰지 않았다** —
  쓸 만한 녹음인지 **학습을 돌려 봐야** 알 수 있었기 때문이다(스크립트가 즉시 판정한다)
- [x] 🚨 **재학습 → 실패 → 되돌림** (2026-09-09). 재학습본이 **아무 말에나 깨어났다**
      (관문 통과 창의 **86.7%**를 「플루이즈」로 판정, 대부분 `prob=1.000`).
      `git checkout services/wakeword_model.npz` 로 **2026-09-08 학습본이 다시 설치돼 있다.**
      **어제보다 나빠진 것은 없다** → [BL-23](BACKLOG.md)
      > 원인이 좁혀졌다: 「TTS ↔ 실제 마이크 간극」이 아니라
      > **«음성 표본에 실제 마이크 녹음이 0개»** 였다. 그러면 모델이 단어 대신
      > **«합성음이냐 진짜 마이크냐»** 를 배운다. ⚠️ 검증 성적(93.6%)은 멀쩡했다 —
      > 검증셋 음성도 전부 TTS라 **같은 편향을 공유하는 눈**이었다
- [x] ✅ **포인팅 1차 실기 — 통과 3 / 결함 2** (2026-09-09). 고리·라벨이 뜨고,
      8초 자동 해제되고, **없는 것은 안 그린다**(인쇄 버튼). 클릭도 먹힌다(§4-1 확인).
      **드러난 결함 2개는 같은 날 고쳤다:**
      - 🚨 **좌표가 2배 어긋났다** — 파이썬은 **물리 픽셀**(3072×1920), Electron은
        **DIP**(1536×960)다. 이 PC 배율이 **200%**. → `screen.screenToDipPoint`
        ⚠️ **배율 100% PC에서는 이 버그가 안 보인다** → [ADR §5-A](design/M4_포인팅_확대.md)
      - 🐢 **캡처가 표시 8초를 통째로 기다렸다** — 한 턴에 포인팅+화면설명이 오면
        (계획 2단계) 턴이 **28초**가 됐다. 기다리지 않고 **치우는** 쪽으로 정정
        → [ADR §5 인용 상자](design/M4_포인팅_확대.md)
- [x] ✅ **포인팅 2차 실기 — 통과** (2026-09-09). DPI 변환이 먹혀 좌표가 제자리
      근처로 왔고, **고리 안쪽 클릭이 통과**한다(§4-1 실기 확인). 지연 **28s → 9.7s**.
      남은 좌표 오차는 **Vision의 추정 오차**라 고칠 수 있는 종류가 아니다 —
      고리와 «추정한 위치예요» 라벨이 그래서 있다
      > 「포인팅+화면설명 동시 안 됨」은 **포인팅 버그가 아니다** — 2단계에서 모델이
      > `describe_screen`을 안 불렀다(BL-19 계열). M3-1이 «못 했어요»로 보고한다
- [x] ✅ **음성 연속 인식 — 통과** (2026-09-09). 4번 연속 성공(56/42/115/75KB).
      실패하던 때는 전부 ~130KB 균일이었다 → [BL-25](BACKLOG.md)
- ⚙️ **웨이크워드 설정 화면**(BL-13, 2026-09-07 신설) — 눌러 본 적이 없다.
  ⚙️ → 호출어를 바꾸고 **저장** → 10초 뒤 그 말로 불러 보면 된다
  > ⚠️ 호출어를 «플루이즈»가 아닌 말로 바꾸면 **자동으로 Whisper 경로로 물러난다**
  > (전용 모델은 학습된 말만 안다). 로그에 *"호출어가 학습된 말과 다릅니다"* 가 찍히고,
  > 감지율이 69%로 떨어지는 게 **정상 동작**이다.
- [x] ✅ **BL-17 — 2026-09-10 실기 재현·해결.** 재질문 뒤 승인 대기를 놓치고 있었다

→ 숫자는 [README 상태표](README.md). `launch.bat` 띄우고 **이상하면 그 증상만** 알려주면 된다.

### ⏸️ 오디오 — 사용자 결정으로 **미룸** (2026-09-09)

**웨이크워드·TTS 관련은 전부 여기 모아 둔다.** 기능을 막지 않는다 — 마이크 버튼으로
음성을 쓰고, 방해되면 `.env`에 `WAKE_WORD_ENABLED=false` 한 줄이면 된다.

- [ ] ⏸️ **가설 확인 30초** ← 재개하면 **이것부터.** 볼륨을 끄거나 이어폰을 꽂고 몇 턴 →
      **턴 직후 WAKE가 사라지면 TTS 자기 목소리가 범인**이고, 그건 임계값과 무관하게
      고칠 수 있다(재생 중 웨이크워드 정지). ⚠️ TTS 없이 난 WAKE도 있어 원인이
      하나가 아닐 수 있다 → [BL-23](BACKLOG.md)
- [ ] ⏸️ **«플루이즈가 아닌 말» 녹음 2~3분** (⚠️ 사용자) — 재학습의 선행 조건.
      평소 말투로 아무 말이나. **«플루이즈»만 안 나오면 된다.**
      `record_wakeword.py`에 `--negative` 옵션이 필요하다(미구현)
      > 이걸 넣으면 지름길이 막힌다 — 실제 마이크 오디오가 양성·음성 **양쪽에** 있게 된다
- [ ] ⏸️ **여러 사람 녹음** — ADR §7의 «한 사람 과적합». `--append`로 더하면 된다

---


- [x] ✅ **좌표 클릭 — 2026-09-07 통과.** 승인 → `(779,275)` 클릭 2회 성공,
      없는 '인쇄 버튼'은 정직하게 거절. ⚠️ **최소화된 창에서는 안 된다**(캡처 불가) —
      활성화하면 된다. 아래는 재확인이 필요할 때의 절차다.
      ```
      메모장 열어줘
      메모장에서 파일 메뉴 눌러줘
      ```
      1. **승인 질문**이 떠야 한다 — *"'메모장' 창에서 '파일 메뉴'을(를) 찾아서
         클릭할까요? (클릭은 되돌릴 수 없어요)"*
      2. **"응"** → 파일 메뉴가 실제로 열려야 한다
      3. 없는 걸 시켜보기: *"메모장에서 인쇄 버튼 눌러줘"* →
         **아무 데도 안 눌리고** "찾지 못해 클릭하지 않았습니다"가 나와야 한다
      > 승인이 붙어 있어 사고 위험은 낮다. 확인할 건 **승인 흐름이 클릭에서도 도는지**와
      > **좌표가 진짜 눌리는지**다.

- [x] ✅ **화면 감시 — 알림 도달 2026-09-07 통과.** [BL-22](BACKLOG.md)를 고친 뒤
      **처음으로 알림이 실제로 도달했다.** `13:55:57 감시 시작` →
      `13:56:09 변화 감지 → 화면 확인 1/20회` → `13:56:13 종료 | 사유=detected | Vision 1회`.
      화면 1장만 나갔고 감지 후 자동 종료까지 설계대로 돌았다(고지·중단은 그 전에 확인됨).
      > 판독이 `안녕하세요`를 `안녕`으로 답했는데 **오독이 아니다** — 변화 감지 1초 뒤
      > 찍으므로 **타이핑 중이면 중간 상태**를 본다. 늦추려면 `_SETTLE_SECONDS`(1.0초).
      **3·4번도 2026-09-07에 통과했다** — 최소화하면 `capture_failed`로 멈추고 알리며
      (창이 깜빡이지 않는다), 상한에서는 `timeout` + *"못 봤어요"* 가 온다. **Vision 0회**라
      상한 경로에서 화면이 한 장도 나가지 않았다. 아래는 재확인용 절차다:
      > ⚠️ **2026-09-04에 BL-19이 나왔다** — 감시가 **시작조차 안 되고 있었다**(도구 미호출).
      > 고쳤지만 **항상 켜지지는 않았다**(모델 쪽 변동). **2026-09-07에 재시도를
      > `tool_choice` 강제로 바꿨다** — 여기서 볼 것이 하나 늘었다:
      > 첫 응답이 말뿐이었더라도 `logs/pluiz.log`에 `[BL-19] … 1회 재시도(강제=watch_screen)`
      > 가 찍히고 **그 다음엔 켜져야 한다.** 강제까지 하고도 안 켜지면 남는 건 모델 교체다.
      > 시작이 안 되면 *"시작하지 못했어요"* 라고 나오니, **그때는 한 번 더 말해 보면 된다.**
      > *"지켜볼게요"* 라고 해 놓고 안 켜지면 **그게 회귀다** → [BL-19](BACKLOG.md)
      > 아래는 **감시가 켜진 다음**에 볼 것들이다 — 자동 테스트가 못 보는 부분이다.
      ```
      메모장 지켜보다가 글자 생기면 알려줘
      ```
      1. **고지 응답**이 떠야 한다 — 무엇을·5초마다·10분/20회 상한·"그만 봐"
      2. 메모장에 아무 글자나 입력 → **5~15초 안에 오버레이가 앞으로 나오고
         소리로 알려야 한다**(채팅에도 👁 메시지)
      3. **최소화 함정** — 감시 중 그 창을 최소화 →
         **창이 깜빡이면 안 되고**, "창이 안 보여서 멈췄다"는 알림이 와야 한다
      4. **조용한 종료가 없는지** — 아무 일 없는 창을 감시 → 상한에서
         *"못 봤어요"* 알림이 와야 한다.
         빨리 보려면 `.env`에 `SCREEN_WATCH_MAX_MINUTES=1` · `SCREEN_WATCH_MAX_VISION_CALLS=3`
      5. **중단** — *"그만 봐"* → 즉시 멈춤 + 헤더의 👁 배지가 사라져야 한다
      > 로그는 `logs/pluiz.log`의 `[Monitor]` 줄. 시작·변화 감지·화면 확인 n회·종료 사유가
      > 전부 찍힌다. **이상하면 그 줄만 알려주면 된다.**

- [x] ⚠️ **계획 수립(M3) — 2026-09-07에 돌렸고 결함이 나왔다 → [BL-21](BACKLOG.md).**
      계획은 네 번 다 제대로 섰지만 **건너뛴 단계를 완료로 기록**했고, 그게
      **캐시에 학습됐다.** **같은 날 ①·② 다 고쳤다**(→ [M3-1 ADR](design/M3-1_단계완료판정.md)).
      아래를 다시 돌려 확인한 뒤 기본값을 정한다. **두 문장을 같이 봐야 한다** —
      *"메모장이랑 계산기 열어줘"* 는 문구가 **붙어야** 하고,
      *"메모장 열고 계산기도 열어줘"* 는 **안 붙어야** 한다(거짓 실패 확인).
      코드는 2026-09-05에 들어갔고 mock 28건은 통과했다. **기본이 꺼짐**이라
      확인하려면 `.env`에 `PLAN_ENABLED=true` 한 줄을 넣고 서버를 다시 띄워야 한다.
      ```
      메모장 열고 계산기도 열어줘
      ```
      1. **둘 다 실제로 떠 있어야 한다.** 응답 문구로 판정하지 말 것 —
         이어서 *"지금 켜져 있는 앱 알려줘"* 로 **상태**를 본다(V-05를 그렇게 만든 이유)
      2. 로그 `logs/pluiz.log`의 `[Plan]` 줄 — `계획 2단계` · `1/2 단계 완료` · `2/2 …`
      3. 한 단계만 됐으면 응답 끝에 **"다만 이건 못 했어요: …"** 가 붙어야 한다
      4. 승인이 끼는 경우: *"메모장 열고 test.txt 지워줘"* → 승인 질문 → **"응"** →
         **삭제까지 완주**해야 한다(계획이 승인 중단을 살아남는지)
      > 끝나면 `.env`의 `PLAN_ENABLED` 줄을 지운다 — **기본은 꺼짐이다.**
      > 캐시가 더러워졌으면 `git checkout cache/command_cache.json` (BL-11)

- [ ] **UI 기능 왕복** — 채팅 · 🎙️음성 · 즐겨찾기 · 히스토리 · ⚙️설정을 **마우스로** 클릭.
      인증(BL-14)이 붙어서, 토큰이 안 실리는 경로가 있으면 **그 기능만 조용히 401**이 난다.
      > 2026-09-02에 *"즐겨찾기·히스토리·설정은 잘 됐다"* 고 확인했다.
      > 채팅·음성은 2026-09-03에 여러 번 왕복해 확인됐다. **남은 건 사실상 없다** —
      > 다음에 이상하면 그때 본다.

- [x] ✅ **BL-17 — 재현하고 고쳤다** (2026-09-10). 승인 **재질문**을 거친 뒤의
      "네"가 안 먹던 것. **실기에서 두 번 재현됐고 원인이 로그에 있었다** —
      `승인 대기 확인 | next=없음`. `interrupt()`가 같은 노드에서 두 번째로 걸리면
      그 노드는 «다음에 실행할 노드»가 아니라 **«실행 중간에 멈춘 노드»** 라
      `next`가 비고 `tasks[].interrupts`에만 남는다.
      `test_hitl_graph` **114 → 120** → [BACKLOG BL-17](BACKLOG.md)
      > 🚨 **mock 114건이 전부 초록인데 실기가 깨졌다** — mock은 `Command(resume=)`를
      > 그래프에 **직접** 줘서 이 판정을 통째로 건너뛰고 있었다.

<details><summary><b>▶ 2026-09-03에 확인 끝난 것들 (재확인 불필요)</b></summary>

- ✅ **마이크로 삭제 승인** (13:17) — 텍스트 승인 질문 → 음성 *"그래 삭제해 줘"* → 한 번에.
  로그로 재개 경로까지 확인. 이 과정에서 BL-16이 드러나 함께 고쳤다
- ✅ **메모장에 입력** — *"메모장 열고 거기에 회의록이라고 적어줘"* → 메모장에 들어감
- ✅ **BL-15** — *"메모장 열어서 회의록이라고 써줘"* 한 문장에서 **뒷문장이 안 씹힘**
- ✅ **"새로 메모장 열어줘"** → 기존 창 전면화가 아니라 **새 탭**
- ✅ **좌표 인식 2건** — 파일 메뉴는 정확히(창 원점 포함), 없는 "저장 버튼"은 **못 찾음**
- ✅ **메모장이 쌓이지 않음** — 두 번 열어도 창 1개

</details>

### ② 다음 작업 ← Claude는 여기부터

> ## 📅 10월 이전 3주 계획 — 2026-09-08 확정
>
> | 주차 | 무엇 | 왜 이 순서인가 |
> |---|---|---|
> | **1주** | **웨이크워드 완주**(녹음 → 학습 → 런타임 교체) + UI 채팅창 전환 버그 | 9월을 **진짜로** 닫는다. 웨이크워드는 **상시 대기·완전 백그라운드의 선행조건**이라 하나를 풀면 셋이 풀린다 |
> | **2주** | ⭐ **포인팅 · 확대 + 설정창 길안내** | **프로젝트의 얼굴.** `find_ui_element`가 이미 화면 좌표를 주고 Electron에 오버레이 창이 있어 **싸다.** 아무것도 바꾸지 않아 **승인이 필요 없다**. ⚠️ 구현 전 ADR |
> | **3주** | **임베딩 캐시**(= 심사의 RAG 항목) + **도구 사용 실측** | 강의 개념 매핑의 마지막 구멍 + *"도구가 제대로 주어졌는지"* 를 로그로 잰다 |
>
> **버린 것 («안 함»으로 확정)**: 장기 메모리 · 루틴 학습 · 클라우드 동기화.
> 미룬 게 아니다 — *"포인팅을 제대로 만들려고 이 셋을 버렸다"* 가 한계점 분석이 된다.
> **새로 생긴 것**: `내보내기/가져오기` 파일(클라우드의 대안).
> 근거 전문은 `docs/planning/제품_방향.md`(🔒 로컬 전용).

> **2026-09-07에 아래 항목들이 전부 닫혔다.** 1주차의 시작점(녹음)은 사용자 몫이고,
> **기다리는 동안 Claude가 할 수 있는 것은 「곁들여」 목록**에 있다.
>
> ⚠️ *"코드가 들어갔다"와 "동작한다"는 다르다* 는 규칙은 그대로다. 다만 이번엔
> **다섯 기능이 전부 실기를 통과했으므로** 그 문장을 Phase 2에는 더 붙이지 않는다.
> 새로 들어간 것 중 미검증은 **BL-13 설정 화면**뿐이다(①).

- [x] **10월 「Plan-and-Execute」 설계** — [design/M3_계획수립노드.md](design/M3_계획수립노드.md)
      (2026-09-05). 구현 전 ADR을 요구하는 [WORKFLOW.md § 설계 결정](WORKFLOW.md) 규칙대로
- [x] **M3 ADR §6 1~5단계 구현** — `planner` 노드 신설 (2026-09-05).
      순수함수+상태 → 노드+라우팅 → 플래그 배선 → `tests/test_plan_node.py` **28건** → 문서.
      **기본 꺼짐**(`PLAN_ENABLED=false`)이고 `recursion_limit`은 켜졌을 때만 `10 → 24`.
      전체 mock **29파일 745개** 통과 → [DEVLOG 최상단](DEVLOG.md)
      > ⚠️ **실기 미검증** — 켜고 돌려 본 적이 없다. 확인 절차는 「① 계획 수립」에 있다
- [x] **BL-21 ② — 캐시 오염 차단** (2026-09-07). `_maybe_learn`이 **복합 명령을
      학습하지 않는다.** 통째로 막아도 잃는 게 없다 — 정상 실행된 계획 턴은 도구가
      2개 이상이라 애초에 학습 대상이 아니다. `test_cache_wire.py` **8 → 13**
- [x] **BL-21 ① — 완료 판정** (2026-09-07). ADR → [design/M3-1_단계완료판정.md](design/M3-1_단계완료판정.md).
      커서를 그대로 믿지 않고 `min(커서, 이번 턴 도구 호출 수)`로 **덜 낙관적인 쪽**을 믿는다.
      **실행은 강제하지 않는다** — 이미 끝난 단계를 오판해 강제하면 되돌릴 수 없는 동작을
      두 번 한다. `test_plan_node.py` **28 → 37**
- [x] ✅ **`plan_enabled` 기본 ON** (2026-09-07). 라이브에서 세 경우가 다 확인됐다 —
      도구 2개면 조용, 1개면 *"다만 이건 못 했어요: '계산기 닫기'."*, 승인 넘어 완주.
      끄려면 `.env`에 `PLAN_ENABLED=false` 한 줄
- [x] ✅ **화면 감시 잔여 2건** (2026-09-07) — **Phase 2가 닫혔다.**
      최소화 → `사유=capture_failed` + *"창이 안 보여서 멈췄어요"*,
      상한 → `사유=timeout` + *"못 봤어요 (화면 확인 0회)"*. **조용한 종료 없음**
- [x] ✅ **BL-13 — 웨이크워드 설정 UI** (2026-09-07). ⚙️ 설정에 호출어 입력란 + 토글.
      **API 키 저장과 분리**했고 반영에 10초 걸린다고 말한다. `test_wakeword_ui.py` **25건**
- [x] ✅ **웨이크워드 — 녹음 없이 완주** (2026-09-08). 1주차가 하루에 닫혔다.
      *"실제 녹음이 반드시 필요하다"* 는 **예상이었지 관측이 아니었고**, 재보니 병목은
      «합성이라서»가 아니라 **«화자가 2개라서»** 였다(1개 30.5% · 2개 83.0% · 11개 **98.1%**).
      TTS 목소리를 **14개**로 늘려 재학습 → **런타임 교체(ADR 3단계)** 까지 끝.
      `test_wakeword_kws.py` **21건** 신설 · 추론 **28.7ms**(Whisper base의 1/24)
      > ⚠️ **남은 건 사람이 한 번 불러 보는 것뿐**이다 → ①. 학습·평가가 전부 TTS라
      > 실제 마이크·조음은 아직 한 번도 안 들어갔다
- [ ] **UI 채팅창 전환 버그** (1주차) — 실행 후 클릭해도 채팅 화면으로 안 바뀌는 경우가 있다.
      ⚠️ **재현 조건 미확인** — 언제 그러는지 한 줄만 알려주면 잡는다
- [x] ✅ **포인팅 ADR** (2026-09-09) → [design/M4_포인팅_확대.md](design/M4_포인팅_확대.md).
      설계하며 **문서에 없던 충돌 2개**가 나왔다 — 오버레이가 ① 스크린샷에 찍혀 Vision을
      오염시키고 ② 화면 감시의 픽셀 비교를 **변화로 잡히게 한다.** 서버가 «표시 중»을
      알고 캡처를 기다리는 것으로 푼다(§5)
- [x] ✅ **포인팅 — 구현 (ADR §8 1~4단계)** (2026-09-09). `point_at_element` 신설(도구 40개),
      `core/pointer.py`(표시 수명) · Electron 전체화면 클릭통과 오버레이 ·
      감시가 표시 구간을 거른다. `tests/test_pointing.py` **39건** · 전체 mock **33파일 915개**
      > ⚠️ **실기 미검증.** 핵심 두 가지가 자동 테스트로 안 잡힌다 → ①
- [x] ✅ **확대 → 돋보기로 교체 · 2패스 정밀화** (2026-09-09, 사용자 선택).
      가장자리 카드는 **시선을 옮겨야 해서** 「어디 있는지」를 돕는 기능이 아니었다.
      이제 **대상 자리에 원형 돋보기**를 겹치고, 좌표는 **1차 주변을 잘라 다시 물어**
      다듬는다(실측 편차 36~43px, 요소 자체가 100×25px였다).
      `test_pointing.py` **51 → 60** → [ADR §6·§6-A](design/M4_포인팅_확대.md)
- [x] ✅ **돋보기 1차 실기 — 크게는 되고, 두 가지가 드러났다** (2026-09-09).
      **클릭은 가려져도 통과**한다(확인). 드러난 것:
      - 🚨 **2패스 정밀화가 효과 음수 → 껐다.** 요소 크기가 75×22 → 13~26px로
        무너지고 턴이 9초 → 13~17초가 됐다. 코드는 남기고 **기본만 끔**
      - 🚨 **돋보기가 대상을 가렸다** → **살짝 비켜서** 놓고 고리는 켜 둔다.
        가운데 십자도 뺐다(비켜 놓으면 «가운데 = 그 지점»이 아니다)
- [x] ✅ **창 상태 처리** (2026-09-09 사용자 요청) — 꺼져 있으면 **열고**, 최소화면
      **되살려 앞으로** 낸 뒤에 표시한다. **한 일은 반드시 말한다**
      (*"'메모장'이 꺼져 있어서 먼저 열었어요."*). 아무 일도 안 했으면 조용하다.
      ⚠️ `click_ui_element`는 이 경로를 타지 않는다 — 승인 범위를 넘기지 않는다
      → [ADR §6-B](design/M4_포인팅_확대.md)
- [x] ✅ **포인팅 3차 실기 — 통과**(2026-09-09). 빨라졌고(9초대) 안 가린다. 아래는 재확인 절차다
      ```
      메모장 닫고  →  메모장에서 파일 메뉴 크게 확대해서 보여줘
      ```
      1. *"'메모장'이 꺼져 있어서 먼저 열었어요."* 가 나오고 **메모장이 떠야** 한다
      2. 메모장을 **최소화한 뒤** 같은 명령 → *"…다시 띄웠어요."* + 앞으로 나와야 한다
      3. **돋보기가 대상을 안 가려야** 한다(고리는 대상 위, 돋보기는 그 옆)
      4. **9초대로 돌아왔어야** 한다(2패스를 껐다)
      > ⚠️ **좌표 정밀도는 그대로다** — 아래 「⏸️ 미룸」 참조
- [ ] ⏸️ **좌표 정밀도 — 미룸** (2026-09-09). Vision의 모델 한계라 **BL-23과 같은
      계열**이고, 남은 3주에 풀릴 성질이 아니다. **시연을 막지 않는다** —
      «화면에 표시한다»는 것 자체가 기능이고, 오차는 **한계점 분석**에 쓴다.
      재개하면 후보는 ① 조각 프롬프트 다듬어 2패스 되살리기 ② `_MAX_EDGE` 올리기
      ③ Windows UI Automation(좌표를 **Vision 없이** 얻는다 — 가장 유망하지만 큰 작업)
- [x] ✅ **⭐ 포인팅 · 확대 — 완료** (2026-09-09). ADR → 구현 → 실기 3차까지.
      고리·라벨·돋보기가 뜨고, **클릭이 통과**하고, 8초에 사라지고, 없는 것은 안 그리고,
      창이 꺼져 있으면 **열고** 최소화면 **되살린다**(한 일은 말한다).
      → [design/M4_포인팅_확대.md](design/M4_포인팅_확대.md) · `test_pointing.py` **70건**
      > ⏸️ 남은 것은 **Vision 좌표 정밀도 하나**이고 «미룸»으로 정했다(①).
      > 모델 한계라 BL-23과 같은 계열이고 **시연을 막지 않는다.**
- [x] ✅ **설정창 길안내 — 통과** (2026-09-09). *"블루투스 어디서 켜?"* → 설정을 열고
      짚어 준다. **새 코드 없이** 창 준비 기능([ADR §6-B](design/M4_포인팅_확대.md))으로
      따라왔고, 막고 있던 건 **유령 창**이었다([BL-26](BACKLOG.md)).
      > **⭐ 2주차(포인팅 · 확대 + 설정창 길안내)가 닫혔다.**
      > 남은 것은 Vision 좌표 정밀도 하나이고 «미룸»이다.
- [x] ✅ **임베딩 캐시 — 2026-09-11 완료.** «실행»이 아니라 **«제안»** 으로 판을 옮겨 성립시켰다
      → [M5 §5-2](design/M5_임베딩_캐시.md). 오프라인일 때 거절 대신 *"혹시 'X' 말씀이신가요?"*.
      실측: 놓친 명령 8개 중 **6 정답 · 1 오답 · 1 없음** · 잡담 8개에 **오제안 0**.
      ⚠️ **실기 미검증** — Wi-Fi 끄고 확인해야 한다(§6-5)
      <details><summary>▶ 2026-09-10까지의 경위 (결정 대기였다)</summary>

- [x] ⏸️ **임베딩 캐시 (3주차) — 사용자 결정 대기로 미룸** (2026-09-10). **기능을 막지 않는다.**
      ADR·구현·실측이 **전부 끝나 있다** → [design/M5_임베딩_캐시.md](design/M5_임베딩_캐시.md). 남은 건 **B냐 C냐** 하나다.
      설계하며 **현행 매칭의 분모를 처음 쟀고**, 문서에 없던 것 셋이 나왔다:
      - 🚨 **Stage 2(difflib 0.80)는 평가셋 32문장에서 «한 번도» 발화하지 않았다.**
        의미가 같은 문장이 0.71~0.76에 몰려 있고 임계는 0.80이다.
        **캐시의 재현율은 통째로 손으로 쓴 entity/action 사전이 내고 있었다**
      - 🚨 **`'그래'`가 `close_app`으로 학습돼 있다** → 신규 [BL-27](BACKLOG.md).
        승인 응답이 명령이 됐다. **임베딩을 넣으면 더 나빠지는 종류다**
      - 🚨 **`'메모장 말고 계산기 열어줘'` → 메모장을 연다.** 부정문은
        **임베딩으로도 안 고쳐진다** — 게이트로 **캐시를 포기**시킨다(ADR §3-4)
      - 재현율 20개 중 **8개**(전부 S1) · 지연 예산 캐시히트 **0.03~0.62s** vs LLM턴 **2.5~16.6s**
      **구현 순서**(ADR §6):
      - [x] ✅ **① 색인 청소** (2026-09-10) — [BL-27](BACKLOG.md) 해결.
        `is_learnable_utterance()`(L1·L2·L3) · `has_contrast_marker()` +
        `find()` **Stage 0 게이트** · `prune_unlearnable_dynamic()`(선별 제거).
        마이그레이션: 백업 후 엔트리 **44 → 40** · 동적 **7 → 3** · 시드 37 유지.
        `test_cache_learn` **15 → 38** · 전체 mock **34파일 992개**
        > 🚨 **ADR에 쓴 필터가 틀린 걸 구현 중에 쟀다** — «entity **와** action»은
        > 시드 9개·정상표현 11/12를 막았다. «둘 중 하나»로, «어절 2개»는
        > «글자 3개»로 정정(시드 `'음소거해줘'`가 1어절이다)
      - [x] ✅ **② `core/embedder.py`** (2026-09-10) — ONNX 로더. 캐시와 분리돼 있고
        모델이 없으면 조용히 폴백. encode **3.9ms**. `test_embedder` **20건**
      - [x] ✅ **③ 임계 실측** (2026-09-10) — `scripts/eval_embed_threshold.py`
      - ⏸️ **④ 배선 — 멈췄다. ⚠️ 사용자 결정 대기**
        > 🚨 **잰 결과가 «쓰면 안 된다»였다.** 마진(긍정최소−부정최대)이
        > **어느 설정에서도 음수**다 — 오매칭 0인 임계가 **존재하지 않는다.**
        > 지금 배선하면 **캐시가 `뻥카 치냐`를 실행한다**(코사인 0.910).
        > fp32도 int8과 같아 **양자화는 범인이 아니었다**(그 미지수는 닫혔다).
        > **e5-small까지 해 보고 닫았다**(2026-09-10) — 셋 중 제일 좋지만
        > 마진 **−0.040**으로 여전히 음수다. 검증기 V1·V2·V3 격자까지 훑은
        > 최선이 **오매칭 0에 3/17**인데 **현행이 8/17**이다(오매칭도 0).
        > → **«임베딩으로 실행을 결정한다»는 성립하지 않는다**([ADR §6-3-C](design/M5_임베딩_캐시.md))
        >
        > **남은 두 길 — 사용자 결정:**
        > **B(추천)** 임베딩을 **«제안»에만** 쓴다(*"혹시 이걸 말씀하신 건가요?"*).
        > 틀려도 사고가 아니라 **마진이 음수여도 성립하는 유일한 설계**이고,
        > 지표가 top-1(**12/17**)로 바뀐다. 심사의 RAG 항목도 이걸로 답이 된다.
        > **C** 사전을 넓힌다 — **천장도 쟀다**(2026-09-10, `scripts/eval_cache_recall.py`):
        > 홀드아웃 **7/16 → 9/16 · 오매칭 0 유지**. 안전하지만 **일반화하지 않는다**
        > (튜닝셋의 16/17은 거의 과적합이었다). 이득이 손으로 적은 낱말 수에 비례한다
        >
        > ⚠️ **B와 C는 경쟁이 아니라 보완이다.** C가 일반화 못 하는 자리가
        > 임베딩이 잘하는 자리이고(e5 top-1 12/17), 제안으로 쓰면 그 강점만 가져온다.
        > **둘 다 캐시를 포기하지 않는다** — 포기하는 건 «매칭 엔진 교체»뿐이다
      - [x] ✅ ④ 배선 · [x] ✅ ⑥ 문서 (2026-09-11) → [ ] ⑤ **실기만 남았다**(⚠️ 사용자)
      </details>
      > ⚠️ ③은 구현이 아니라 **측정**이다. 부정 문장은 **실제 로그에서** 가져온다 —
      > 웨이크워드 검증셋이 학습셋과 편향을 공유해 93.6%가 아무것도 보증하지 못한 자리다
- [x] ✅ **도구 사용 실측 — 완료** (2026-09-10) →
      [research/2026-09_도구사용_실측.md](research/2026-09_도구사용_실측.md) ·
      도구 [`scripts/analyze_tool_usage.py`](../scripts/analyze_tool_usage.py)
      - ① **등록 40개 중 13개**만 실사용됐다(테스트까지 넣으면 20개).
        ⚠️ *«27개가 쓸모없다»가 아니다* — **캐시가 대신 처리한 것**이 섞여 있다
        (캐시 히트는 `도구=없음`으로 찍힌다). 정말 한 번도 안 쓰인 건 5개다
      - ② 🚨 **답할 수 없었다** — 로그가 응답을 «13자»로만 남겨 «못 한 것»과
        «안 해도 됐던 것»을 못 갈랐다. **이번 실측의 최대 소득**이고
        ✅ **[BL-28](BACKLOG.md)로 같은 날 고쳤다**(`사유=` 태그 5종 +
        `[캐시 실행]` 줄). **다음 실측부터 ②에 바로 답이 나온다**
      - ③ 정정률 **13.3%**(10/75). 뜯어보니 **도구 선택이 틀린 건 거의 없고**
        6턴이 포인팅 좌표(⏸️ 기지의 한계) · 3턴이 BL-26(해결됨)
      - ④ 덤 — **캐시 히트 0.09s vs LLM 9.25s (약 100배)**. 차별점을 로그로 증명했다
      - 🚨 **로그의 3분의 2가 테스트였다.** `thread=` 접두사 필터를 표준으로 정했다
        (1회차 동향점검이 미뤄 둔 «로그 오염 처리 방침»)
      > **하지 않기로 한 것: 도구를 더 만들지 않는다.** «도구가 없어서 실패»한 사례가
      > **아직 하나도 확인되지 않았다**
- [x] ✅ **내보내기 / 가져오기** (2026-09-10) → [design/M6_내보내기_가져오기.md](design/M6_내보내기_가져오기.md).
      ⚙️ 설정에 버튼 두 개 + `GET /export` · `POST /import`.
      **클라우드를 안 하기로 한 것의 대안**이라 심사 답변이 된다.
      - 🚨 **핵심: 가져오기는 «학습의 다른 입구»다.** `is_learnable_utterance()`가
        `learn()`에만 걸려 있어서, 캐시 파일을 그대로 받으면 다른 PC의 오염된 엔트리가
        **BL-27 필터를 우회한다.** 들어오는 것도 **같은 게이트**를 통과시키고,
        버린 건 «몇 개를 왜» 말한다
      - 🔒 `.env`는 **빼는 게 아니라 allowlist라 넣을 수 없다.** 시드는 안 옮긴다
        (코드가 제공한다 — 낡은 시드가 새 시드를 덮으면 안 된다)
      - 병합은 **기존 우선**. 가져오기 전 **자동 백업**. 매니페스트가 없으면 **거절**
      - **도구로 만들지 않았다** — 되돌릴 수 없는 변경이라 HITL이 필요해지고,
        *"도구를 더 만들지 않는다"*(도구 사용 실측)와도 어긋난다
      `test_portable.py` **38건** + `test_portable_api.py` **36건**
- [x] 🚨 **덤 — 테스트가 사용자의 실제 캐시를 고치고 있었다** (2026-09-10, M6에서 드러남).
      `portable.py`가 경로를 하드코딩해 `PLUIZ_CACHE_FILE`을 무시했고 `_testenv`의
      보호가 무력화됐다. **BL-11이 막아 둔 사고가 «새 모듈이 규약을 안 따라서» 되살아났다.**
      네 파일이 같은 환경변수를 보게 고쳤다.
      > 같이 잡은 것: **캐시가 «모르는 키» 하나에 통째로 비워지던 것.**
      > `CacheEntry(**v)`가 TypeError를 내면 `except`가 삼키고 `self._cache = {}` —
      > 동적 학습분이 **조용히** 사라진다. M6가 다른 버전의 캐시를 들여오는 통로라
      > 위험이 커진 자리였다

**곁들여 할 수 있는 것** — 녹음이 없어도 Claude가 지금 시작할 수 있는 것들:

- [x] ✅ **계측 심기 — latency + token** (2026-09-08). `턴 완료` 로그가 이제
      `| 소요 1.83s | LLM 2회 | 토큰 in=1234 out=56`까지 남긴다.
      **11월 데이터가 0에서 시작하지 않는다** — 9~10월 두 달치가 저절로 쌓인다.
      - `usage_metadata`가 실제로 실려 오는지 **설치본에서 확인**했다
        (`langchain-google-genai` 4.2.4 `chat_models.py`가 응답마다 채운다)
      - **캐시 히트는 `LLM 0회(캐시) | 토큰 0`** — 차별점을 말이 아니라 로그로 증명한다
      - 못 잰 것은 `LLM ?회 | 토큰 미상`. **«0회»라고 적지 않는다** —
        안 부른 것과 못 잰 것을 같은 숫자로 쓰면 11월에 캐시 효과가 실제보다 커 보인다
      - 승인 질문으로 끝난 턴도 `승인 대기 | 소요 …`를 남긴다. 안 남기면 **HITL 턴만
        지연 통계에서 통째로 빠져** 평균이 낙관적으로 기운다
      - `tests/test_graph_agent.py` **11 → 17**. 그중 하나가 «지난 턴 토큰이 안 더해진다»다
        (절대규칙 6의 토큰판)
- [x] ✅ **`docs/research/` 신설 — 월간 동향 점검** (2026-09-08).
      [research/README.md](research/README.md)(항목 5개 · 규율 · 주기) +
      [1회차 = 기준선](research/2026-09_동향점검.md).
      ② 프레임워크 항목은 **자동화했다** → `python scripts/check_deps_drift.py --md`
      - 1회차에서 나온 것: 🔴 `anthropic` **메이저**(0.107 → 1.4) · 🟡 minor 9개.
        **하나도 안 올렸고 왜 안 올렸는지를 적었다** — lock을 만든 날 올리면 기록할
        조합이 없고, 시연 3주 전이다. 보안 패치는 예외로 못박음
      - ③ 경쟁 서비스 기준선 기록. 2회차에 볼 것은 **Copilot Actions의 프리뷰 졸업**이다 —
        정식 출시되면 「Windows 기본 탑재」라 차별화 서사를 다시 써야 한다
      - ⑤에서 **로그 오염을 발견**했다: `logs/pluiz.log`에 `open_app_fail`(테스트 mock
        도구 이름)이 10회 있다. 11월 측정에 직접 걸린다 → 처리 방침은 2회차에 결정

- [x] ✅ **BL-20 — 거부해도 안전한 호출은 살린다** (2026-09-09). ADR →
      [design/BL-20_거부후_안전호출.md](design/BL-20_거부후_안전호출.md).
      백로그의 해결 후보(«안전한 것은 마감하지 말고 tools로 보낸다»)는 **쓰지 않았다** —
      `ToolNode`가 마지막 `AIMessage`의 tool_calls를 **전부** 실행하므로 그대로 하면
      **거부당한 삭제까지 실행된다.** 대신 원본은 전부 마감하고 안전한 호출만
      **새 id로 재발행**한다. `test_hitl_graph.py` **59 → 88**
      > ⚠️ **라이브 미검증** — 위험+안전이 한 배치에 담기는 일이 실기에서 드물다.
      > mock이 그 배치를 강제로 만들어 검증한다(`MixedLLM`)
- [x] ✅ **BL-24 — 해결**(2026-09-10). ADR → 구현 → 테스트.
      **묻지 않은 삭제가 실행되던 것**을 구조로 막았다 — 질문이 위험 호출 «전부»를
      이름으로 부르고, 승인 시 **물어본 것만 새 id로 재발행**한다.
      **마지막 `AIMessage`에 없으면 실행될 수 없다**(조건문이 아니라 구조).
      `test_hitl_graph` **88 → 114** · 전체 mock **36파일 1071개**
      → [design/BL-24_다중위험호출_승인.md](design/BL-24_다중위험호출_승인.md)
      > 덤: `target_exists`가 **첫 호출 하나만** 보고 있었다 —
      > 같은 상황인데 **배치 순서에 따라 결과가 달라졌다.** 이제 각각 확인한다.
      > ⚠️ **라이브 미검증** — 위험 2개가 한 배치에 담기는 일이 실기에서 드물다
- [x] ✅ **BL-07 — 파일 찾기 UX** (2026-09-10). **막고 있던 건 근사 매칭이 아니라 띄어쓰기였다.**
      고치기 전에 오기억 16쌍으로 쟀다 — 현행 glob **3/8**, 공백·구분자만 걷어내면
      **7/8 · 오매칭 0**, difflib 0.70은 6/8. 그래서 difflib는 **«혹시 이건가요» 제안에만** 쓴다
      (정규화 매칭보다 성적이 나쁘고 `이력서`↔`이론서`가 0.667로 임계에 붙어 있다 —
      임베딩 ADR §6-3에서 «실행 결정에는 안 쓴다»고 정한 것과 같은 모양).
      - 위치를 안 주면 **세 곳을 훑고**, 시킨 곳이 아니었으면 **어디서 찾았는지 말한다**
      - 확장자로 못 찾으면 **빼고 다시** 찾는다 (LLM이 확장자를 지어냈을 수 있다)
      - 🚩 **원문 사례가 닫혔다**: *"학교 문서"* → *"혹시 이건가요? '학교문 (폴더)'"*.
        폴더는 결과 목록엔 안 넣고 **제안에만** 넣는다 — 도구 계약은 «파일 탐색»이다
      - 빠른 경로는 그대로다 — glob이 먼저 돌고(다운로드 0.002s) **0건일 때만** 내려간다
      `test_find_file.py` **28건** 신설 · 전체 mock **38파일 1167개**
- [x] ✅ **BL-10 — 수동 테스트 문서 통합** (2026-09-08). 3개(88+56건) →
      [testing/MANUAL_TESTS.md](testing/MANUAL_TESTS.md) **44건** 하나로.
      **회귀 12건이 제거된 구 엔진을 가리키고 있었다** — 실행할 수 없는 절차가
      「수동 테스트」로 남아 있었다. 웨이크워드 2건(⚠️ 미검증) 신설
- [x] ✅ **에러 핸들링 표준화 → [BL-29](BACKLOG.md)** (2026-09-10). **스타일 문제가 아니었다** —
      도구 반환 문자열의 접두사는 **코드 세 곳이 파싱하는 계약**인데 셋의 규칙이 서로 달랐다.
      🚨 `✗`(52곳, 가장 흔한 실패 표시)를 **T04가 못 봐서** 도구가 실패해도 LLM의
      *"메모장 열었어요!"* 가 그대로 나갔다. 🚨 `web.py` 넷은 `✗`가 아니라 **알파벳 `x`** 라
      셋 다 빠져나가 **실패가 캐시에 학습될 수 있었다**(BL-27 계열).
      판정을 `core/tool_result.py` 하나로 모으고 어긋난 16곳을 정리.
      → [design/BL-29_도구_결과_계약.md](design/BL-29_도구_결과_계약.md)
      > 🔒 **핵심은 «내일의 구멍»**: `test_tool_result.py`가 **도구 소스를 전수 스캔**해
      > 금지 마커나 마커 없는 실패가 생기면 거기서 깨진다. 미끼를 심어 **정말 잡는지 확인**했다.
      `test_tool_result.py` **35건** 신설 · 전체 mock **37파일 1139개**
- [x] ✅ **requirements 버전 고정** (2026-09-08) — `requirements.lock.txt` 신설.
      하한만 있으면 **코드를 안 고쳐도 어느 날 깨진다**(`langgraph>=0.2.0`이라고 적힌 채
      1.x가 돌고 있었다). CI도 lock에서 버전을 가져온다. `test_dependencies` **32 → 34**
- [x] **BL-19 원인 ② — 재시도를 `tool_choice` 강제로** (2026-09-07).
      설득(프롬프트)은 확률을 올릴 뿐이라 나쁜 구간에서는 재시도까지 같이 실패했다(0/10).
      **재시도 패스만** 강제로 묶는다 — 첫 패스는 그대로고, 못 묶는 provider·mock이면
      오늘 경로로 폴백한다. `test_screen_monitor.py` **126 → 136**, 전체 mock **29파일 755개**
      → [BACKLOG BL-19](BACKLOG.md)
      > ⚠️ **라이브 미검증.** mock은 배선만 보증한다 — 나쁜 구간의 Gemini가 강제를
      > 따르는지는 ①에서 갈린다. 안 켜지면 남는 수단은 **모델 교체**다
- [ ] **BL-18** — Vision 전체화면이 주 모니터만 본다. ⚠️ 좌표가 걸려 실측 없이 손대지 말 것

### ✅ 결정 끝났다 (2026-09-08) — 이제 «막혀 있는 것»이 없다

- [x] ✅ **제품 방향 3건 확정.** 근거는 `docs/planning/제품_방향.md` §3(🔒 로컬 전용).
      ① 클라우드 **안 한다** — 대신 `내보내기/가져오기` 파일 하나
      ② **«안 하기» 확정**: 장기 메모리 · 루틴 학습 · 클라우드 동기화
      ③ **포인팅·확대 = 2주차 메인** (구현 전 ADR)
      > ②는 *미룬 게 아니다.* *"3주 안에 포인팅을 제대로 만들려고 이 셋을 버렸다"* 가
      > 그대로 **한계점 분석**이 된다. ⚠️ 병렬 실행은 여기 안 넣었다 — 그건 «안 함»이
      > 아니라 **조건부 보류**다(BL-20 이후 읽기 전용 도구에 한해 재검토)
- [x] ✅ **Vision 로그 — 플래그 옵트인** (2026-09-08). 기본은 지금처럼 길이만,
      `.env`에 `VISION_LOG_RESPONSE=true`면 앞 200자를 남긴다. **로그는 마스킹을
      거치지 않으므로** 진단할 때만 켰다 끈다. `test_vision.py` **12 → 17**
- [x] ✅ **BL-22 잔여 — 고지가 한계를 말한다** (2026-09-08).
      *"다만 글자 한두 자처럼 아주 작은 변화는 놓칠 수 있어요."*
      감도를 올리는 쪽은 버렸다 — 임계를 내리면 **커서 깜빡임마다 화면이 나간다.**
      `test_screen_monitor.py` **138 → 139**
- [x] ✅ **푸시 시점 — `feature/byeonsoyun`에 올린다** (2026-09-08).
      `main`은 그대로 둔다. 아래 절 참조

### 🔁 라이브 테스트 재실행 방법 (필요할 때만)

```bash
python main.py                     # 다른 창에서. 토큰이 여기서 발급된다
python tests/test_regression.py    # 43개 (V-01~V-05 포함)
                                   # ⚠️ V-05는 BL-19 때문에 구간에 따라 FAIL한다.
                                   #    '시작하지 못했어요'면 그물이 동작한 것 —
                                   #    '지켜볼게요' 해놓고 안 켜지면 그게 회귀다.
                                   # ⚠️ 실제 화면이 외부로 나간다(V-01~V-05).
python tests/test_commands.py      # 40P / 15MANUAL
python tests/test_sprint1_2.py     # 55/55
git checkout cache/command_cache.json   # BL-11 — 서버에 PLUIZ_CACHE_FILE을 안 줬다면
                                        #   PLUIZ_CACHE_FILE=/tmp/pluiz_live.json python main.py
                                        #   로 띄우면 이 줄이 필요 없다 (2026-09-05)
```

> ⚠️ **서버를 두 번 띄우지 말 것.** 두 번째 인스턴스가 죽으면서 첫 번째의 토큰 파일을
> 지워 UI를 끊는 사고가 있었다. 2026-09-02에 막았지만(`port_in_use`) 하나만 띄우는 게 맞다.
>
> ⚠️ **`test_commands.py`는 간헐적으로 1건 실패한다.** 실행마다 실패 케이스가 다르고
> 개별로 돌리면 통과한다 — LLM 비결정성이다. **한 번 실패했다고 회귀로 판단하지 말고
> 그 케이스만 따로 재현해 볼 것.** (반복 실행은 앱을 띄웠다 닫으므로 사용자를 방해한다)

### ③ ✅ 웨이크워드 — 끝났다 (2026-09-08)

**대기 상태가 해제됐다.** 6일간 이 항목을 막고 있던 «사용자 녹음 2분 30초»는
**애초에 필요하지 않았다.** 전체 경위와 정정 근거는
→ [design/M2_웨이크워드_전용모델.md §4](design/M2_웨이크워드_전용모델.md)

- [x] ✅ **전용 모델 학습** — TTS 화자 **14개**(한국어 3 + 다국어 12)로 합성 1,600클립.
      검증 **95.0% @ 0.8**(오탐 1.82%) · **미학습 화자 98.1%**
- [x] ✅ **런타임 교체 (ADR 3단계)** — `services/wakeword.py`가 `KwsModel`을 기본 경로로
      쓴다. 추론은 **numpy 행렬곱**이라 sklearn이 런타임 의존성이 되지 않는다.
      Whisper 경로는 **폴백으로 살아 있다**(ADR §6)
- [x] 🚫 **오탐 임계값 실사용 조정 — 2026-09-11에 «막힌 길»로 판정됐다.**
      실기 오탐이 `0.990`~`1.000`이고 놓침이 `0.5`~`0.6`이라 **0.95로 올려도 오탐을
      하나도 못 막고 놓침만 나빠진다.** 임계값은 이 문제의 손잡이가 아니다
      → [BACKLOG BL-23](BACKLOG.md)의 완화책 셋(에너지 관문 · TTS 중 무시 · 재학습)
- [ ] ⚠️ **실기 확인** — ①의 맨 위 항목. **이것만 남았다**

<details><summary><b>▶ 녹음이 여전히 필요해지면 (①에서 실패했을 때만)</b></summary>

`--user-audio`는 **선택 인자로 그대로 남아 있다.** 실제 녹음을 섞으면 여전히 도움이 된다.

```bash
conda activate pluiz && export PYTHONIOENCODING=utf-8
```

**1단계 — 녹음.** "플루이즈"를 4초 간격으로 반복한다(2분 30초면 30~35번).
또렷하게/평소처럼/빠르게/느리게, 가까이/멀리, "헤이 플루이즈"·"플루이즈야"도 섞어서.

```python
import numpy as np, sounddevice as sd
SR, SECS = 16000, 150
a = sd.rec(int(SR*SECS), samplerate=SR, channels=1, dtype='float32'); sd.wait()
np.save('wake_voice.npy', a.flatten())
print('RMS', float(np.sqrt(np.mean(a**2))))   # 0.005 이상이면 충분
```
> ⚠️ 이 절차는 **150초를 통째로 녹음**한다 — 중간에 틀려도 되돌릴 수 없다.
> 여러 사람에게 나눠 받는 편이 낫다(ADR §7의 «한 사람 과적합»도 같이 줄어든다).

**2단계 — 재학습.** 합성음은 `cache/wakeword_tts/`에 있어 재사용된다.
```bash
python scripts/train_wakeword.py --user-audio wake_voice.npy
```
임계값별 표가 출력되고 `services/wakeword_model.npz`가 갱신된다. **런타임 배선은 이미 돼 있다.**

</details>

---

## 9월 — Phase 2: 화면 이해 (Vision)

로드맵상 이번 달 목표. 밀려 있던 "8월 Vision 기초"부터 따라잡는 중이다.

### 기초 (밀린 8월 작업) — 완료
- [x] **로깅 최소 도입** — `core/logger.py`. Vision 디버깅에 필요한 만큼만 (2026-09-02)
- [x] **`take_screenshot` → Gemini Vision 연결** — `describe_screen` 도구 (2026-09-02)

### 웨이크워드 — ✅ 완주 (2026-09-08). 마이크 실측만 남음
- [x] **웨이크워드를 "플루이즈"로 정정** — 코드엔 "소윤아"가 박혀 있었다 (2026-09-02)
- [x] **사용자 설정 가능하게** — `.env` `WAKE_WORDS` + `POST /api/wakeword`.
      재시작 없이 10초 안에 반영 (2026-09-02)
- [x] **🚨 Electron이 잘못된 python을 쓰던 문제** — `python`(anaconda base)엔
      sounddevice·faster-whisper가 없어 **웨이크워드가 한 번도 안 돌았다.**
      `resolvePython()`이 후보를 실제로 검사해 고르도록 수정 (2026-09-02)
- [x] **Whisper 경로 실측 튜닝** — 슬라이딩 윈도우(2초 창/0.6초 간격) · 환각 필터 ·
      base 모델(tiny보다 5배 빠름) · hotwords 끔 · 변형 거리 2. **감지 69%가 천장** (2026-09-02)
- [x] **전용 모델 학습 파이프라인** — `scripts/wakeword_data.py` · `scripts/train_wakeword.py`.
      openWakeWord 임베딩 + scikit-learn MLP, torch 불필요 (2026-09-02)
- [x] **UI에 웨이크워드 설정 화면** — ⚙️ 설정에 입력란 + 토글 (2026-09-07)
- [x] ✅ **전용 모델 학습 — 녹음 없이** (2026-09-08). TTS 화자 **14개**로 1,600클립.
      *"실제 녹음이 반드시 필요하다"* 는 ADR의 예상이 **측정으로 반박됐다**
      (화자 1개 30.5% · 2개 83.0% · 11개 **98.1%**) → [ADR §4](design/M2_웨이크워드_전용모델.md)
- [x] ✅ **런타임 교체 (ADR 3단계)** — `KwsModel`이 기본, Whisper는 폴백.
      호출어를 바꾸면 **자동으로 Whisper로 물러난다**(모델은 학습된 말만 안다).
      `test_wakeword_kws.py` **21건**

### Phase 2 본체
- [x] **실행 결과 시각적 검증** — `visual_verify` 노드 (2026-09-03).
      `VISUAL_VERIFY_TOOLS = {type_text}` — **거짓 성공이 실측된 도구만.**
      Vision의 답을 판정하지 않고 증거만 붙인다(정직 보고, 자동 재시도 없음)
- [x] **UI 요소 인식** — `find_ui_element(target, window)` 신설 (2026-09-03).
      Vision이 찾은 정규화 박스를 **화면 좌표**로 되돌린다(창 원점 포함).
      못 찾으면 **좌표를 지어내지 않는다** — 뒤집힌 좌표·화면 전체 박스·후보 여럿은
      전부 "못 찾음"으로 처리. 도구 **36개**
      > ✅ **실측 1건 통과** (2026-09-03 13:41) — 메모장 파일 메뉴를 창 안 (67,113)
      > = 가로 3.3%·세로 10.5%로 짚었고 **창 원점이 제대로 더해졌다**(712+67=779).
      > ✅ **실측 2건 통과** — 없는 요소("메모장의 저장 버튼")에는 좌표를 지어내지
      > 않고 사유와 함께 "못 찾았다"고 답했다.
      > ⚠️ 클릭으로 검증한 건 아니다. 작은 버튼·겹친 창·다중 모니터는 아직 모른다.
- [x] ⚠️ **좌표 기반 자동 클릭** — `click_ui_element(target, window)` (2026-09-03).
      **승인 필수**(`DANGEROUS_TOOLS`)로 결정. 세 겹으로 막았다 —
      ① 좌표를 인자로 안 받음(LLM이 지어낼 수 없다) ② 사람 승인
      ③ 못 찾음·화면 밖·창 밖·**캡처 뒤 창 이동**이면 누르지 않음. 도구 **37개**
      > ⚠️ **실기 미검증.** mock만 통과했다. 실제로 눌러본 적이 없다 → ① 참조

- [x] **화면 변화 모니터링** — `watch_screen` · `stop_watching` (2026-09-03).
      **Phase 2의 마지막 항목.** 도구 **39개**
      > **정한 것 4가지**(설계 전에 정하라고 적어 뒀던 것): ① 승인이 아니라 **고지**
      > (감시는 멈추면 끝나므로 되돌릴 수 있다) ② **5초 / 10분 / Vision 20회**
      > ③ 감지하면 **알리고 자동 종료** ④ 알림은 **채팅 + 창 띄우기 + 음성**
      > **전송량 방어는 픽셀 프리필터다** — 5초 캡처는 전부 로컬이고,
      > `SCREEN_WATCH_MAX_VISION_CALLS`가 곧 밖으로 나가는 화면 장수의 상한이다.
      > **어떤 이유로 멈추든 알린다**(상한·창 사라짐·판독 실패). 조용히 끝나면
      > 사용자는 아직 지켜보는 줄 안다.
      > ⚠️ **실기 미검증.** 확인 절차는 「🔵 ①」에 있다

### Vision 보강
- [x] **Vision 라이브 테스트 추가** — `test_regression.py` **V-01~V-05** (2026-09-04).
      실제 캡처 → 축소 → Gemini 왕복 → 좌표 환산이 **처음으로 자동 검증**됐다.
      V-01~V-04 실기 통과. **V-05를 만들다 BL-19이 드러났다.**
      > 화면에 확인용 문구를 띄워 두고 **그게 되돌아오는지**로 판정한다 —
      > 화면 설명만 보고는 진짜 읽었는지 구분할 수 없다.
      > ⚠️ 실제 화면이 외부로 나간다. 보이면 안 되는 게 떠 있을 때 돌리지 말 것.
- [x] **응답 지연 점검** — `agent_timeout` 30 → **45초** (2026-09-03).
      `visual_verify`가 턴당 **1회**로 묶여 있어 한 턴에 두 번 부르지 않는다
- [ ] **실사용 지연 실측** — 위는 계산값이다. 실기에서 체감 지연을 재고 45초가
      맞는지 확인할 것 (①의 화면 검증 항목에서 같이 본다)

---

## 10월 — Plan-and-Execute + 개인화

- [x] **계획 수립 노드** — `planner` 신설. 복합 명령을 **최대 2단계**로 쪼개 순서대로 실행하고
      **못 한 단계를 정직하게 말한다** (2026-09-05). 설계·결정 전문 →
      [design/M3_계획수립노드.md](design/M3_계획수립노드.md).
      ⚠️ **기본 꺼짐 · 실기 미검증** — 라이브 확인은 「🔵 ①」
- [ ] **병렬 실행 지원** — 독립적인 단계를 동시에.
      ⏸️ **미룸** — 근거 5가지는 [M3 ADR §5-2](design/M3_계획수립노드.md).
      요약: ① `ToolNode`가 이미 한 슈퍼스텝에서 병렬 실행하는데 측정된 적이 없다
      ② **BL-20이 먼저다**(2026-09-09 해결 — 이 선행 조건은 풀렸다)
      ③ `Send` 팬아웃 안의 `interrupt()`가 절대규칙 1과 충돌
      ④ 한 대의 데스크톱이라 포커스 경쟁(BL-12 계열)이 난다 ⑤ 음성 명령은 대체로 순차다.
      재검토 조건: 순차 계획이 라이브 증거를 남긴 뒤 **읽기 전용 도구에 한해**
- [ ] **장기 메모리** — 자주 쓰는 앱·명령을 세션 너머로 기억
- [ ] **루틴 학습** — *"매일 아침 켜는 앱"* 패턴 인식 → 선제 제안
- [ ] **임베딩 기반 캐시** — 캐시의 difflib(0.80)을 임베딩 유사도로 교체.
      **이게 이 프로젝트의 RAG다** → [ROADMAP § 강의 개념 매핑](ROADMAP.md)

---

## 11월 — 배포 품질

- [ ] **PyInstaller 패키징** — Python 서버를 실행파일로
- [ ] **electron-builder** — 설치형 `.exe`
- [ ] **자동 업데이트 메커니즘**
- [ ] **외부 서비스 연동** — Gmail · 카카오 API (선택적)
- [ ] **성능 최적화** — 응답 지연 목표치 설정 후 측정

---

## 밀린 기반 작업 (방학 계획 중 미착수)

급하진 않지만 계획에 있었고 안 한 것들. 위 작업 하다 걸리면 그때 처리해도 된다.

- [ ] **에러 핸들링 표준화** — 도구마다 예외 처리가 제각각 (`✗ …` vs `[오류] …`)
- [x] ✅ **requirements 버전 고정** — 2026-09-08 완료. 위 「곁들여」 절 참조
- [ ] **Playwright 조작 기능** — 현재 `crawl_page` 폴백으로 **읽기만** 한다. 클릭·입력 없음

---

## 품질 백로그 (BL) — 서로 막지 않음

상세는 전부 [BACKLOG.md](BACKLOG.md)에 있다. 여기선 존재와 순서만.

- [x] **BL-15** — fast_path 명령 절단. **2026-09-03 해결** — 잔여 명령이 있으면 캐시를
      포기한다. 빈도는 `[BL-15]` 로그로 계속 잰다(임베딩 캐시의 선행조건은 그대로)
- [x] **BL-12** — `type_text`가 확인 없이 성공을 보고하던 것. **2026-09-03 해결** —
      입력 전 대상 창 확인 + 어느 창에 넣었는지 보고
- [x] **BL-16** — 음성/텍스트가 서로 다른 대화였다(`/voice`의 `thread_id` 무시).
      **2026-09-03 해결** — `Form(...)` 선언. 승인이 발화 경로를 넘나들 수 있게 됐다
- [x] ✅ **BL-17** — 승인 **재질문 뒤** "네"가 안 먹던 것. **2026-09-10 실기 재현·해결**(위 「곁들여」 절에 원인).
      관측(`승인 판정` 로그)은 넣었다. **재현 방법은 「🔵 ①」에 있다**
- [x] ✅ **BL-35** — *"찾아볼게요"* 라고 답하고 **도구를 하나도 안 부르던 것**.
      **2026-09-11 해결** — BL-19과 같은 기계로 1회 재시도(`tool_choice="any"`).
      재시도 실패 시 **응답을 고쳐 쓰지 않는다** · **계획 중에는 안 건다**.
      `test_promise_retry.py` **39건**. ⚠️ 실기 미검증
- [ ] 🆕 **BL-36** — 계획이 **대화로 끝낸 단계를 «못 했다»고 보고한다**.
      2026-09-11 실기. *"추천해 드릴게요 … 다만 이건 못 했어요"* 가 한 응답에 같이 나온다.
      ⏸️ **시연 후.** 대본 7장면은 두 단계가 다 도구라 이 경로를 안 밟는다 → [BACKLOG BL-36](BACKLOG.md)
- [x] ✅ **BL-34** — 날씨 같은 «간단한 정보»에 **링크로 답하던 것**. **2026-09-11 해결** —
      `get_weather` 신설(open-meteo · 어제·오늘·내일 · 출처 명시 · 도구 **41개**).
      `fetch_web_info`도 본문을 읽어 붙이게 고쳤다. `test_weather.py` **54건**. ⚠️ 실기 미검증 → ①
- [x] ✅ **BL-33** — 음성 입력이 **8초에서 잘리던 것**(발화 종료 감지 없음).
      **2026-09-11 해결** — 타이머를 걷어내고 `AnalyserNode` RMS로 발화 끝을 본다.
      `test_voice_vad.py` **57건** 신설. ✅ **같은 날 실기 통과**(`[STT] 234자`) → ①
- [x] ✅ **BL-31** — `find_file`이 찾은 걸 `open_file`이 못 열던 것. **2026-09-10 해결** —
      못 찾았을 때만 같은 탐색을 한 번 한다. ⚠️ **삭제에는 붙이지 않았다**(의도된 비대칭)
- [x] ✅ **BL-32** — 승인해서 지웠는데 말하지 않던 것. **2026-09-10 해결** —
      위험 도구의 결과를 `output_guard` 접미로 **도구가 만든 문장 그대로** 싣는다
- [ ] 🆕 **BL-19** — 감시 도구를 **부르지도 않고** "지켜볼게요"라고 답하던 것.
      **2026-09-04 부분 해결** — 거짓 보고는 막았다(상태로 판정).
      **2026-09-07** — 재시도를 `tool_choice` 강제로 바꿔 원인 ②에 손댔다.
      ⚠️ **라이브에서 확인될 때까지 닫지 않는다** (①)
- [x] **BL-21** — 계획이 **건너뛴 단계를 완료로 기록**하고 그게 **캐시에 학습**되던 것.
      **2026-09-07 해결(①·②)** — 캐시는 복합 명령을 학습하지 않고, 보고는 커서 대신
      `min(커서, 도구 호출 수)`를 본다. ⚠️ **라이브 미검증** → ①
- [x] **BL-22** — 감시 프리필터가 **타이핑을 못 봤다**(대화상자 기준이었다).
      **2026-09-07 해결** — 임계 2% → 1%. ⚠️ 글자 1~2자는 여전히 못 잡는다(고지는 그대로)
- [x] **BL-20** — 위험 도구 하나를 거부하면 **같은 배치의 안전한 호출까지** 조용히
      취소되던 것. **2026-09-09 해결** — 안전한 호출을 **새 id로 재발행**해 살린다
      → [ADR](design/BL-20_거부후_안전호출.md). ⚠️ 라이브 미검증
- [x] ✅ **BL-24** — 위험 호출이 **둘**이면 질문은 하나만 나가고 승인 시 **둘 다** 실행되던 것
      (**묻지 않은 삭제**). 2026-09-09 발견(BL-20 ADR §7) · **2026-09-10 해결** —
      질문이 전부를 이름으로 부르고, 승인 시 **물어본 것만 재발행**한다
      → [ADR](design/BL-24_다중위험호출_승인.md). ⚠️ 라이브 미검증 → ①
      > 🔖 이 줄은 2026-09-11까지 `[ ]`로 남아 있었다. **위 「📅 2026-09-10에 닫힌 것」
      > 상자와 BACKLOG는 이미 «해결»이었다** — 같은 사실이 세 곳에 있어 한쪽만 갱신된
      > 이 문서가 경고하는 바로 그 사고다.
- [ ] 🆕 **BL-18** — Vision 전체화면 경로가 **주 모니터만** 본다. 보조 모니터의 오류는
      감시해도 **영영 못 보고, 못 봤다는 것도 모른다.** 창을 지정하면 우회된다.
      ⚠️ 좌표가 걸린 변경이라 실측 없이 손대지 말 것
- [x] **BL-11** — 테스트가 git 추적 파일(`cache/command_cache.json`)을 더럽히던 것.
      **2026-09-05 해결** — `PLUIZ_CACHE_FILE` + `_testenv`(로그와 같은 패턴).
      mock은 끝, 라이브는 서버에 주면 되는 **옵트인**. 고치다 보니 반대 방향이 더
      나빴다 — `test_bl15_truncation`(43건)이 **사용자의 실제 캐시를 읽고** 있었다
- [x] ✅ **BL-07** — 파일 찾기 UX. **2026-09-10 완료** (위 「곁들여」 절에 실측과 근거)
- [ ] **BL-04** — `/ws` 토큰 단위 스트리밍 · 도구 미호출 재시도 미이관
- [x] ✅ **BL-10** — 수동 테스트 문서 통합. 2026-09-08 완료 (위 「곁들여」 절 참조)

---

## ✅ 결정 대기 항목 — 전부 정해졌고 **실행까지 끝났다**(2026-09-09)

**막혀 있는 것이 없다.** 근거와 내용은 「🔵 ①」 위의 「✅ 결정 끝났다」 절에 있다.

- [x] ✅ **푸시 — `feature/byeonsoyun`에 올린다** (`main`은 그대로).
      **⚠️ 숫자는 `git rev-list --count origin/main..HEAD`로 세서 적을 것** —
      기억으로 적으면 어긋난다(2026-09-07에 36 → 58로 정정한 적이 있다)
- [x] ✅ **제품 방향 3건 · Vision 로그 · BL-22 잔여** — 위 절 참조
- [x] ✅ **팀 브랜치 `feature/ohdayoung` — 원격 삭제 완료**(2026-09-09).
      2026-06-07에 멈췄고 코드가 전부 `src/capstone_pluiz/`(2026-09-01에 제거된 구조)
      위에 있었다. **66파일 +7,606 −626 · main에 없는 커밋 21개.**
      병합 충돌이 아니라 서로 다른 프로젝트 둘이었다.
      팀원 통보는 사용자가 했고, 백업 태그를 만든 뒤 지웠다.
      > 🔑 **되살리는 법 — 어느 clone에서든 한 줄이다.**
      > ```bash
      > git push origin backup/feature-ohdayoung-2026-09-09^{}:refs/heads/feature/ohdayoung
      > ```
      > ✅ **태그는 원격에도 있다**(2026-09-09 push). 이 PC를 잃어도 복구된다 —
      > 브랜치 목록은 깨끗하고 커밋만 `refs/tags/`에 남아 GC되지 않는다.

---

## 🔒 시스템 동결 후 — 맨 마지막에 한 번

**기능 추가가 끝났다고 확정한 뒤에** 한다. 먼저 하면 기능이 바뀔 때마다 다시 해야 한다.

- [ ] **BL-08 데모 GIF 녹화** — ⚠️ **사용자가 직접 화면 녹화** (코드 작업 아님).
      추천 장면: ① "메모장 켜줘" → "그거 꺼줘"(맥락) ② 삭제 → 승인 질문(HITL)
      ③ 오프라인 캐시 즉시 실행 ④ **"지금 화면에 뭐 있어?"(Vision — 신규)**
- [ ] **발표자료 갱신** — `docs/presentation/`이 **2026.06.15에 멈춰 있다.**
      8~9월 작업(HITL·가드레일·캐시학습·엔진단일화·Vision)이 하나도 반영 안 됨

---

## ✅ 완료 (2026-09-02)

- [x] **🚨 위 수정의 회귀 2건 + 창 재사용 규칙** — `"응 지워 줘"`가 띄어쓰기 때문에
      승인이 아니라 *다른 명령*으로 읽혀 **승인이 루프를 돌았다**(사용자가 네 번 말함).
      또 취소 플래그가 턴을 넘어 새어 **삭제해 놓고 "취소했어요"라고 오보**했다.
      더불어 `open_app`이 기존 창을 두고 **새 창을 만들면서** "앞으로 가져왔다"고
      답하던 것을 고치고 **창 규칙을 명문화**했다
      → [ARCHITECTURE § 창 규칙](ARCHITECTURE.md#창-규칙--기존-창-재사용)
- [x] **🎤 음성 실기 피드백 3건 수정** — 자동 테스트가 전부 초록인데 실제로 쓰니
      막히던 것들. ① **`list_directory` 신설**(도구 35개) — *"바탕화면에 뭐 있어?"* 를
      답할 수단이 없었다(BL-07이 UX가 아니라 기능 구멍이었다) ② **HITL 4분류** —
      승인 대기 중 *"네이버 열어줘"* 가 재질문으로 처리돼 같은 말을 두 번 해야 했다
      ③ **없는 대상엔 승인을 묻지 않는다** — 묻고 승인받은 뒤에야 "없네요"라고 했다
      → [DEVLOG 최상단](DEVLOG.md)
- [x] **🔒 BL-14 로컬 API 접근 제어** — 아무 웹페이지가 `fetch` 한 줄로 PC 제어 명령을
      넣을 수 있던 구멍을 막았다. 기동 시 토큰 발급(`core/auth.py`) → Electron·테스트만
      파일로 수령 → 헤더/쿼리 검사. **보안이 4층 → 5층**이 됐다.
      고치면서 **CORS로는 애초에 못 막는다**는 것과 **`/ws`엔 CORS가 적용되지 않아
      fetch보다 큰 구멍이었다**는 걸 확인했다 → [BACKLOG 해결됨 BL-14](BACKLOG.md)
- [x] **🚨 HITL 오승인 수정** — `네이버 열어줘`·`음소거 해줘` 같은 일상 명령이
      삭제 승인으로 읽히던 문제. 3분류(`approve`/`reject`/`unclear`) + 재질문
- [x] **🚨 "턴 경계" 도입** (`current_turn_messages`) — 과거 턴의 도구 오류·호출이
      현재 응답과 캐시 학습을 오염시키던 문제. **P4 동적 학습이 thread당 1턴만
      동작하고 있었다.** ADR § 5-A + CLAUDE.md 절대규칙 6번에 기록
- [x] **페르소나를 Pluiz로 통일** — 프롬프트가 여전히 "소윤이"였다
- [x] **문서 숫자 출처 일원화** — 테스트/도구 개수가 문서마다 달랐다. README 상태표가 유일 출처
- [x] **라이브 3스위트 실측 검증** — `regression` 30/30 · `commands` 39P/0F · `sprint1_2` 55/55
- [x] **로드맵을 슬라이드에서 `docs/ROADMAP.md`로 승격** — 계획이 발표자료 안에 갇혀 있었다
- [x] **RAG 문구 정정** — README 2곳이 "적용했다"고 썼으나 코드 0건이었다
- [x] **로깅 최소 도입** (`core/logger.py`)
- [x] **Vision 도구 연결** (`describe_screen`, 34번째 도구)
- [x] **이 문서 신설** — 진행 상황이 세 문서에 흩어져 있어 한눈에 안 보였다
- [x] **웨이크워드 "플루이즈" 정정 + 사용자 설정 + Electron python 탐색 수정**

---

_최종 업데이트: 2026-09-11 밤 (🔴 **기한 9/22 시연** · [즉시/위험] **0건** · BL-33·34 실기 통과 · BL-35 해결 · ✅ **웨이크워드는 시연 후로 결정** — 9/15·16이 기능으로 재배정되고 재구축은 [M7](design/M7_웨이크워드_재구축.md) 9/23~ · ✅ **남은 «기능»은 임베딩 캐시 B 하나** · ✅ 오탐 완화 «헛깨어남 조용히 닫기» 적용 · **9/12·9/13·9/18·9/19가 비었다** · 배포·평가지표·발표자료는 기한 밖 · mock **44파일 1505개** 통과 · ✅ **로드맵의 «기능»이 전부 닫혔다** · 🆕 BL-36은 시연 후)_
