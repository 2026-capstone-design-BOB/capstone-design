# Pluiz v2 — 문서 허브

> 이 프로젝트의 모든 문서 진입점입니다. **작업을 시작하기 전 이 문서부터 읽으세요.**
> 처음이라면 [STRUCTURE.md](STRUCTURE.md) → [ARCHITECTURE.md](ARCHITECTURE.md) → [WORKFLOW.md](WORKFLOW.md) 순서를 권합니다.

---

## 지금 상태 (2026-09-10 기준)

**Pluiz** — 한국어 음성 명령으로 Windows PC를 제어하는 AI 에이전트. 졸업 캡스톤.

2026.06.15 데모 완료. 이후 강의 개념(LangGraph·HITL·OWASP 가드레일)을 구조에 적용하는
**Milestone 1**을 진행했고, **2026-09-02 라이브 실측(FAIL 0)으로 M1 완료를 확인**했다.
같은 날 코드 리뷰로 **HITL 오승인·"턴 경계" 결함을 수정**했고(mock 검증 완료,
**실기 확인은 남음** → [TASKS.md](TASKS.md) ①), 이어서 **BL-14(로컬 API 무인증)를
막아 보안이 4층 → 5층**이 됐다 → [ARCHITECTURE § 보안](ARCHITECTURE.md#보안--5층-방어).
**2026-09-03에 Phase 2(화면 이해)의 마지막 항목인 화면 변화 모니터링까지 넣어
9월 로드맵이 코드상으로는 닫혔다** — 보기 → 찾기 → 누르기 → 지켜보기.
**2026-09-04에 Vision 경로를 라이브로 처음 검증했고**(V-01~V-04 통과),
그 과정에서 **감시가 실사용에서 한 번도 켜진 적이 없다는 걸 발견해 고쳤다**(BL-19).
**2026-09-07에 다섯 기능이 전부 실기를 통과해 Phase 2가 진짜로 닫혔다** — 좌표 클릭·화면 감시 포함.
가는 길에 결함 셋을 만나 다 고쳤다: **BL-22**(프리필터가 글자 한 줄을 놓쳤다) ·
**BL-21**(계획이 건너뛴 단계를 완료로 세고 **캐시에 학습**했다) · `get_running_apps`가
계산기를 못 보던 것. **셋 다 mock이 초록인 채로 살아 있었다.**
10월 항목이던 **계획 수립도 라이브를 통과해 기본 켜짐**이 됐다
→ [M3](design/M3_계획수립노드.md) · [M3-1](design/M3-1_단계완료판정.md) · [M3-2](design/M3-2_미시도_단계_재시도.md) · [ROADMAP.md](ROADMAP.md).
**2026-09-09에 ⭐ 2주차(포인팅·확대 + 설정창 길안내)가 닫혔다** — *"그게 어딨지"* 하면
**화면에 고리로 표시**하고, `zoom=True`면 **돋보기**로 확대한다. 창이 꺼져 있으면 열고
최소화면 되살린다 → [M4](design/M4_포인팅_확대.md).
같은 날 **BL-20**(거부가 안전한 호출까지 취소)·**BL-25**(음성이 첫 번째만 됨)·
**BL-26**(안 한 걸 했다고 말함 — 유령 창)을 고쳤다.
⏸️ **미룸**: 웨이크워드 오탐/감지율([BL-23](BACKLOG.md)) · Vision 좌표 정밀도.
둘 다 **모델 한계**라 코드로 좁힐 폭이 작고 **시연을 막지 않는다** → [TASKS.md](TASKS.md) ①

| 항목 | 상태 |
|---|---|
| **엔진** | `PluizGraphAgent` **단일**. 구 엔진은 M1-P5에서 제거 → [design/M1_P5_엔진단일화.md](design/M1_P5_엔진단일화.md) |
| **M1 진행** | P0 진단 → P1 맥락 → P1.5 그래프 이관 → P2 HITL → P3 OWASP → P4 캐시학습 → **P5 엔진단일화 · 전부 완료** |
| **도구** | **41개** (앱 사전은 **17개 앱** — 2026-09-11에 그림판·작업 관리자·제어판·캡처 도구·돋보기 **5개 추가**, 오프라인 커버리지가 곧 차별점의 크기다 → [BL-42](BACKLOG.md)) (**승인 필수 3개** — 삭제 2 + `click_ui_element` · `describe_screen`·`find_ui_element`·`watch_screen`은 화면을 외부 LLM에 전송) |
| **화면 검증** | `visual_verify` 노드 — `type_text`가 **넣으려던 그 창**을 화면으로 재확인(2026-09-03). 기본 ON, `.env` `VISION_VERIFY_ENABLED=false`로 끔. **라이브 V-01~V-04 통과**(2026-09-04) |
| **화면 감시** | `watch_screen` — *"오류 뜨면 알려줘"*. 5초마다 **로컬** 픽셀 비교로 거르고 변화가 있을 때만 Vision. 최대 10분·화면 20장에서 자동 종료하고 **어떤 이유로 멈추든 알린다**(2026-09-03). 승인 대신 **고지**(요약되지 않게 원문 그대로 나간다). **2026-09-07 실기 통과** — 알림 도달(`detected`) · 최소화 시 `capture_failed` · 상한에서 `timeout` + *"못 봤어요"*. **조용히 끝나는 경로가 없다.** ⚠️ BL-19(모델이 도구를 안 부르는 구간)은 남아 있고 재시도를 `tool_choice` 강제로 바꿨다. ⚠️ BL-22 — 글자 1~2자는 커서 깜빡임과 구분되지 않아 못 잡는다. **2026-09-08부터 고지가 그 한계를 말한다**(*"글자 한두 자처럼 아주 작은 변화는 놓칠 수 있어요"*) — 코드 한계는 그대로지만 **거짓 약속은 없어졌다** |
| **계획 수립** | `planner` 노드 — 복합 명령을 **최대 2단계**로 나눠 순서대로 실행하고, **못 한 단계를 정직하게 말한다**(2026-09-05). replan 없음(LLM 왕복 턴당 1회). **기본 켜짐**(2026-09-07 라이브 확인 후) — 끄려면 `.env` `PLAN_ENABLED=false`. mock 37건 + 라이브 3경우(조용·보고·승인 완주) 확인. **2026-09-14부터 «안 해 본 단계»는 읽기전용 도구로 한 번 더 시도한다**(M-01) → [M3](design/M3_계획수립노드.md) · [M3-1](design/M3-1_단계완료판정.md) · [M3-2](design/M3-2_미시도_단계_재시도.md) |
| **도구 정직성** | `type_text`가 **입력 전에** 대상 창을 확인하고, 못 잡으면 입력하지 않는다(BL-12 해결). 캐시는 **해석 못 한 잔여 명령이 있으면 포기**한다(BL-15 해결) |
| **보안** | **5층 방어** — 0 로컬 API 접근 제어(토큰) · 1 규칙 · 2 하이브리드 LLM 판정 · 3 HITL · 4 출력 마스킹 → [ARCHITECTURE](ARCHITECTURE.md#보안--5층-방어) |
| **테스트** | mock **46파일 1672개** + 라이브 3스위트 (`test_dependencies` **34개**는 로컬 전용) — **이 숫자의 출처는 이 표 하나다**<br>⚠️ **숫자는 기억으로 적지 않는다** — `python scripts/run_mock_suite.py`가 세는 방식을 고정한다(2026-09-10 신설). 예전엔 서버 필요 파일을 넣느냐에 따라 합계가 수십 개씩 흔들렸다<br>2026-09-10 오전 +102: `test_cache_learn` 15→38(BL-27) · `test_graph_agent` 17→30(BL-28) · `test_hitl_graph` 88→114(BL-24) · `test_embedder` 20 신설(M5) · `test_log_format` 20 신설<br>2026-09-10 오후 +191: `test_tool_result` **35 신설**(BL-29) · `test_find_file` **28 신설**(BL-07) · `test_portable` **38 신설** + `test_portable_api` **36 신설**(M6) · `test_hitl_graph` 114→120(BL-17) · `test_plan_node` 37→52(BL-30)<br>2026-09-10 밤 +23: `test_find_file` 28→42(BL-31) · `test_hitl_graph` 120→129(BL-32)<br>2026-09-11 +166: `test_voice_vad` **62 신설**(BL-33 발화 종료 감지 + 버리는 녹음 누수) · `test_weather` **54 신설**(BL-34 날씨 도구) · `test_promise_retry` **39 신설**(BL-35) · `test_voice_vad` 57 → **78**(버리는 녹음 누수 · 헛깨어남 조용히 닫기) · `test_weather` 54 → **66**(타임아웃/연결실패 분리) · `test_cache_suggest` **37 신설**(M5 임베딩 제안)<br>2026-09-11 밤 +11: `test_hitl_graph` 129 → **136**(BL-38 «질문 없는 next»를 승인 대기로 읽지 않는다 + BL-17 회귀 방지) · `test_cache_suggest` 37 → **44**(BL-38·39 — 실패 턴 로깅 · 타임아웃→오프라인 판정 · 거짓 약속 금지 · BL-41 읽기 관문 판정) · `test_hitl_graph` 136 → **140**(BL-40 — 한 배치로 모으는 지시 + «위험 n개» 계측) · `test_cache_wire` 13 → **16**(BL-41 — 읽기가 막으면 쓰기도 막는다)<br>2026-09-11 2차 실기 +27: `test_cache_learn` 38 → **59**(BL-42 — 캐시 사전 ↔ 실행 사전 불변식 · 실기에서 죽었던 말투 9개 · S2 임계 0.83 경계) · `test_cache_suggest` 44 → **50**(BL-37 도구 이름 문지기 · entity 합의 · BL-42 제안 수락)<br>2026-09-11 3차 실기 +24: `test_brightness` **16 신설**(BL-45 — 읽을 수 없는 PC에서도 10%씩. 🚨 실제 화면을 바꾸지 않고 판정 논리만 본다) · `test_cache_learn` 59 → **67**(BL-44 — 밝기가 볼륨 트리거에 빼앗기지 않는다, **양방향**)<br>2026-09-12 +24: `test_offline_skip` **11 신설**(BL-46 — 오프라인이면 LLM을 안 부른다. **양방향** — «안 부른다»만 고정하면 오프라인 캐시를 죽이는 수정이 통과한다) · `test_bl15_truncation` 53 → **62**(BL-48 — 「하나」 단독은 군말이다, **양방향**) · `test_portable` 38 → **41**(BL-49 — 같은 초의 두 백업이 서로 다른 파일이다) · `test_dependencies` 34 → **35**(BL-47 — `wmi`가 직접 의존성으로 승격)<br>2026-09-12 밤 +40: `test_plan_node` 52 → **61**(BL-51 — 한 응답 안에서 말이 바뀌지 않는다. **문구가 아니라 모순을 본다**) · `test_cache_learn` 67 → **88**(BL-50 — 지시대명사 L4 게이트 + 기존 42개 전부 살아 있는지) · `test_log_format` 20 → **28**(BL-52 — `요청=`/`실행=` 분리 + **옛 형식 하위호환**) · `test_graph_agent` 30 → **32**(BL-52 로그 계약)<br>2026-09-14 +31: `test_find_file` 42 → **57**(D-01a — 한글 음차 «에이점 티엑스티»→`a.txt`. 🚨 **양방향** — '오이'·'이력서'가 **여전히** 찾아지는지를 같이 못 박는다. 후보 추가지 치환이 아니다) · `test_plan_node` 61 → **77**(M-01 — 안 해 본 단계만 재시도. 🚨 **묶인 도구 목록 자체**를 검사한다 — «위험 도구를 안 불렀다»가 아니라 «부를 수가 없다»를 본다)<br>🔖 **2026-09-12에 CLAUDE.md의 건수 둘이 낡아 있던 것을 정정했다**(`test_cache_learn` 38→67 · `test_hitl_graph` 136→140). **«한 사실은 한 문서에만»의 값이 여기서 나온다** — 합계는 이 표가, 개별은 실행이 답한다 |
| **라이브 실측** | `regression`에 **Vision 라이브 V-01~V-05 추가**(2026-09-04). **V-01~V-04 통과** — 실제 캡처 → Gemini 왕복 → 좌표 환산이 처음으로 자동 검증됐다. **V-05는 BL-19 때문에 구간에 따라 FAIL한다**(회귀 아님 — 실패해도 거짓말은 안 한다).<br>2026-09-03 기준 `commands` 40P/0F/15MANUAL · `sprint1_2` 55/55 → [DEVLOG](DEVLOG.md).<br>**2026-09-07 — Phase 2 다섯 기능 전부 실기 확인(종료).** 좌표 클릭·화면 감시 포함.<br>**2026-09-08 — 웨이크워드 전용 KWS 모델 실기 1·2차.** 실제 사람 목소리에 `prob=1.000`·`0.999`로 **깨어난다.** 오탐 없음(비발화 창 전부 0.015 이하). ⚠️ 다만 **부르는 만큼은 아니다** — 감지율 미측정 → [BL-23](BACKLOG.md).<br>✅ **2026-09-14 2차 리허설 — 미검증 둘이 닫혔다.** 웨이크워드 **감지율 5/10**(2026-09-09 **2/10**에서 올랐다. 분모가 생겼다 — 10월 [M7](design/M7_웨이크워드_재구축.md)의 출발 숫자) · **BL-13 설정 화면을 처음 눌러 봤고 결함이 나왔다**(→ BL-54). ⚠️ 남은 미검증: **D-01a `_romanize_ko`** — STT가 «에이점 티엑스티»를 `a.txt` 로 **먼저 바꿔 줘서 코드가 돌 기회가 없었다** → [TASKS.md](TASKS.md) ① |
| **CI** | GitHub Actions 3잡 — mock · 문서링크 · 비밀정보 가드 |
| **브랜치** | `main` = `develop` = 최신. 작업은 `feature/byeonsoyun`. 팀 브랜치 `feature/ohdayoung`은 **2026-09-09 원격 삭제** — 커밋은 태그 `backup/feature-ohdayoung-2026-09-09`에 원격까지 보존(복구법은 [TASKS.md](TASKS.md)) |
| **[즉시/위험]** | 🚨 **1건 미해결**(2026-09-14 **2차 리허설** 기준)<br>**🆕 BL-53 미해결** — 🚨 **승인이 STT 환각 하나에 깨졌다.** 승인 대기 중 사용자가 *"그래"* 라고 답했는데 `google 실패 → whisper 폴백` 이 **49자를 지어냈고**(`'크롬 엣지 카카오톡 탐색기 …'`) 그게 **그대로 승인 답변으로 소비**돼 `other_command` 가 됐다 — 삭제는 안 되고 «잡담» 응답이 나갔다. 🚨 **대본에 삭제 장면이 둘**이고, 터지면 «승인»이라는 간판이 깨진다. 고칠 자리는 환각이 아니라 **`hitl`에 «이게 질문에 대한 답이긴 한가»를 보는 문지기가 없는 것**이다. ⚠️ `other_command` 분기 자체는 없애면 안 된다(절대 규칙 11)<br>**🆕 BL-54**(🟢 ⏸️ 10월 M7로) — 호출어를 바꿔도 **KWS 모델은 계속 «플루이즈»를 듣는다.** BL-13이 만든 불일치 검사가 **시작할 때만** 돌고, 런타임 갱신은 `WAKE_WORDS` 목록만 바꾼다. 대본에 호출어를 바꾸는 장면이 없어 🟢지만, **`.env`를 바꾼 채 두면 부팅 때 Whisper 경로(감지율 천장 69%)로 떨어진다** → D-1 점검에 원복 확인을 넣었다<br>**✅ BL-50·BL-51·BL-52는 2026-09-12에 닫혔고 2차 리허설에서 BL-50이 실기 확인됐다**(캐시 새 항목 0개). 아래 «미해결»로 적힌 셋은 **그때의 기록이다**<br>**🆕 BL-51 미해결** — 🚨 **한 응답에서 말이 세 번 바뀐다.** *"✓ 'test.txt' 휴지통으로 옮겼어요. 다만 이건 못 했어요: 'test.txt 파일 지우기'. 그리고 'test.txt' 휴지통으로 옮겼어요."* — 했다 → 못 했다 → 또 했다. 세 조각이 각각 **LLM 응답 · M3-1 `missing_notice` · BL-32 `executed_danger_notice`** 에서 나와 겹쳤다. BL-32에 «응답이 이미 말했으면 붙이지 않는다»는 가드가 있는데 **문구가 글자 그대로 같지 않아 비교가 빗나간 것으로 보인다.** 🚨 **대본 7장면이 이 경우**이고, 하필 «정직하게 말한다»가 이 프로젝트의 간판인 자리다. ⚠️ **이중 삭제가 아니다** — 도구는 한 번만 돌았다(BL-52)<br>**🆕 BL-50 미해결** — 🚨 **지시대명사가 캐시에 학습된다.** 대본 2장면 *"그거 꺼줘"* 뒤에 `'그거 꺼줘'` → `close_app(메모장)` 이 동적 학습으로 박혔고, 실측하니 `'그거 꺼줘'`·`'그거 꺼'` 가 **LLM 없이** 메모장을 끈다. BL-27의 재발이 아니라 **다른 문**이다 — BL-27은 «승인 응답이 명령이 됐다»이고 이건 «**맥락에 묶인 지칭**이 고정 명령이 됐다»다. `is_learnable_utterance('그거 꺼줘')` → `None`(자격 있음): 「꺼」가 action이라 통과했고 **「그거」가 지시대명사라는 것은 아무도 안 본다.** 🚨 **대본을 돌 때마다 재오염된다** — 지우는 걸론 안 되고 **L4 지시대명사 게이트**가 필요하다. 오염분 1개 제거(백업 있음)<br>**🆕 BL-52 미해결** [TODO/품질] — 로그 `턴 완료 \| 도구=[...]` 가 **«실행»이 아니라 «요청»**이다(`_turn_tool_names`가 AIMessage의 `tool_calls`를 모은다). 거부한 턴에도 `도구=['delete_file','delete_file']` 이 찍힌다. 🚨 **실제로 오판을 만들었다** — 이 줄을 읽고 «승인+계획이면 삭제가 두 번 실행된다»고 판단했다가 거부 턴을 보고 정정했다. 덤: 「도구 사용 실측」이 이 필드를 세고 있어 **지금 그 통계는 실행이 아니라 요청을 센다**(승인이 낀 턴이 부풀어 있다)<br>**✅ BL-40 판정 완료**(2026-09-12 실기) — `승인 질문 \| 위험 2개 \| 대상=['날씨.txt','일정.txt']` 가 **한 번** 찍혔다. `위험 1개`가 연달아 둘이 아니므로 **A안(프롬프트 유도)이 먹었다.** ⏸️ 가 닫혔다<br>**✅ BL-46 라이브 확인**(2026-09-12) — 오프라인 실패 턴 **0.04~0.07초**. mock 실측(0.17초)보다 빨랐다<br>**BL-49 해결**(2026-09-12) — 🚨 **되돌릴 길을 만드는 함수가 되돌릴 길을 지우고 있었다.** `backup_file()`의 이름표가 **초 단위**라 같은 초에 두 번 부르면 첫 백업이 조용히 덮였다(`b1 == b2`). M6가 «가져오기 전 자동 백업»을 안전장치로 내세우는데 **그 안전장치가 자기를 지웠다.** 🔖 드러난 계기가 **전체 스위트의 간헐적 37/38**이었다 — 격리 5회·스위트 3회로 재현에 실패했지만 «환경 탓»으로 넘기지 않고 쫓아가서 나왔다.<br>**BL-46 해결**(2026-09-12) — 오프라인 실패 턴이 **8초**였다(상한을 끝까지 태운다). 판정은 `graph_agent`(바깥)·호출은 `graph`(안)이라 **플래그를 안으로 주입**했다 — `agent`가 `llm.invoke()` 직전에 보고 `OfflineSkip`을 올린다. **8.01초 → 0.17초** · `fast_path`보다 뒤라 **오프라인 캐시 히트는 그대로**(0.14초) → [ADR](design/BL-46_오프라인_LLM_생략.md)<br>**BL-47 해결**(2026-09-12) — 밝기 명령만 **캐시 히트인데 5~6초**였다(다른 캐시 히트는 0.02~1.08초). 원인은 LLM 상한이 아니라 **`wmi` 미설치로 인한 PowerShell 폴백 4.5초**. 🚨 그게 **BL-45의 진짜 원인**이기도 했다 — BL-45는 «읽을 수 없는 PC»라고 적었는데 PowerShell로 물으면 `40`이 나오는 **읽을 수 있는 PC였다.** `pip install wmi` → **0.04초**<br>**BL-48 해결**(2026-09-12) — 「하나」가 `_RESIDUAL_MODIFIERS`에 있어 *'메모장 하나 띄워봐'* 가 **캐시를 통째로 포기**했다. 「하나」가 «한 개 더»를 뜻하려면 「더」·「또」가 붙는데 **그 둘은 이미 목록에 있다** — 혼자 잡은 적이 없고 오탐만 만들었다. ⚠️ `_FILLER_TOKENS`로 **옮기면 `find()`가 깨진다**(매칭 경로도 탄다) — BL-43이 경고한 자리<br>**BL-38 해결**(2026-09-11 실기) — **오프라인 턴 하나가 세션을 영구 고장**시켰다. `agent_timeout`(45초)이 남긴 «실행 중간에 멈춘 그래프»(`next=('agent',)` · 질문 0)를 `_pending_interrupt`가 **«승인 대기»로 읽어** 그 뒤 모든 입력이 `Command(resume=)`로 소비됐다 — resume는 `fast_path`를 거치지 않으므로 **캐시에 있는 명령조차 안 돌았다.** 실기에서 17:32:51 이후 입력 **8개가 침묵**했다. 🚨 `_clear_thread()`가 이미 불렸는데 효과가 없던 이유는 **`asyncio.wait_for`가 `to_thread`의 워커를 취소하지 못해** 좀비가 체크포인트를 다시 쓰기 때문이다(45초 주기로 8번 반복된 것이 증거다) → 삭제를 **다음 턴으로** 미뤘다. 🚨 **M5 임베딩 제안이 실기에서 한 번도 발동하지 못했다** — 오프라인에서는 네트워크 오류보다 **타임아웃이 먼저** 나서 분기를 못 탔다(`_looks_offline()` 신설). 그리고 오프라인 문구가 *"앱 실행은 오프라인에서도 쓸 수 있어요!"* 라고 **거짓 약속**을 하고 있었다(실측: `'메모장 열어줘'`는 HIT, `'메모장을 띄어 보도록 하여라'`는 miss).<br>**BL-39 해결**(2026-09-11) — **실패한 턴이 로그에 한 줄도 없었다.** 탈출 경로 4개(타임아웃·네트워크·기타 예외·가드 차단)가 전부 `return`만 했다. ERROR 0건인데 턴 6개가 사라져 원인을 찾는 데 «입력과 턴 완료를 짝지어 빈 자리를 세는» 스크립트가 필요했다. `_dead_end()`가 **반드시 `턴 실패` 줄을 남긴다.** BL-28의 사각지대였다.<br>**BL-41 해결**(2026-09-11) — **읽기와 쓰기가 «복합 명령»을 다르게 판정**해 캐시가 실제로 오염됐다. 읽기(BL-15)는 `has_uncovered_command`로 막았는데 쓰기(BL-21)는 **더 좁은** `is_compound_command`만 봤다 → *"어 그 pc 밝기 올려 주고 오늘 저녁 메뉴 좀 추천해 주라…"* 가 `brightness_up` 단독으로 학습돼 있었다(«메뉴 추천»이 사라진 채). 🚨 읽기가 막아 주므로 **실행 사고는 없어서 조용히 쌓였다** — 그리고 쓰레기 패턴이 **M5 제안 후보 풀에 들어간다.** `graph_agent.py` 주석이 *"BL-15의 쓰기 쪽 짝"* 이라고 적어 놨는데 **짝이 아니었다** — BL-29(«읽는 쪽 셋이 서로 다르게 읽었다»)와 같은 모양이다. 오염 항목 1개 제거(42 → 41).<br>**BL-40 A안 적용·⏸️ 실기 판정 대기**(2026-09-11 사용자 지적) — 파일 N개 = **승인 N번**. BL-24의 버그가 아니라 모델이 삭제를 **여러 agent 턴으로 쪼개는** 별개 모양이다. 🚨 승인 피로가 승인을 무력화시킨다(실기 로그에 *'오'·'음'* 으로 대충 답한 것이 남아 있다). 프롬프트로 «같은 응답에 한꺼번에 호출하라»를 넣고(⚠️ **지시를 먼저·제약을 뒤에** — BL-19의 교훈) **«위험 n개» 계측**을 심었다 — `위험 1개`가 연달아 두 번이면 쪼개진 것, `위험 2개`가 한 번이면 먹은 것이다. **mock으로는 확인할 수 없다**(프롬프트는 확률만 올린다).<br>**BL-37 신규·⏸️**(2026-09-11) — 임베딩 제안의 V3 게이트가 **낱말 어휘에 의존해 새어나간다.** 실제 발화로 재측정: **정답 1 · 오제안 3 · 없음 2**(어제 적은 «8개 중 6»은 정제된 문장이었다)<br>**BL-35 해결**(2026-09-11) — *"찾아볼게요"* 라고 답하고 **도구를 하나도 안 부르던 것**. BL-12·19·26(«안 한 걸 했다고»)·BL-32(«한 걸 말하지 않는»)의 **세 번째 얼굴**이다. BL-19과 **같은 기계**로 한 번만 재시도한다(`tool_choice="any"`). 🚨 재시도도 실패하면 **응답을 고쳐 쓰지 않는다** — 그물의 최악은 «LLM 1회 낭비»여야 한다. 🚨 **계획 실행 중에는 안 건다**(이미 끝난 단계를 두 번 실행할 수 있다).<br>**BL-34 해결**(2026-09-11) — 날씨 같은 «간단한 정보»에 **링크로 답하던 것**. 검색 스니펫에는 실시간 기온이 **구조적으로 없다** — `get_weather` 신설(open-meteo, 어제·오늘·내일, **출처를 밝힌다**). 🚨 지오코딩이 «부산»을 경북(36.38, 128.37)으로 보내는 걸 실측해 **주요 도시는 표로 박았다**. ⚠️ 실기 미검증.<br>**BL-33 해결·실기 통과**(2026-09-11). 🚨 후속으로 «말이 없으면 STT를 안 부른다»가 **`mr.stop()`의 마지막 flush 때문에 지켜지지 않던 것**을 고쳤다(956바이트가 STT로 나가 *"인식하지 못했습니다"* 가 떴다) — 배열을 비우는 것으론 못 막고 플래그가 필요했다 — 음성 입력이 **8초에서 잘리던 것**(교수님 미팅 지적). 고정 타이머를 걷어내고 **발화 종료 감지**를 넣었다 — 같은 `MediaStream`에 `AnalyserNode`를 붙여 RMS를 보고, 말이 시작된 뒤 **무음 1.2초**면 끝낸다. **상한 30초**·**한 마디도 없으면 STT를 안 부른다**·**녹음 중 TTS는 미뤘다가 끝나면 내보낸다**(되먹임 차단). 링은 «남은 시간»에서 **입력 레벨**로 바뀌었다. ✅ **같은 날 실기 통과** — `[STT] 234자` 발화가 안 잘렸다(8초로는 불가능한 길이).<br>**BL-31 해결**(2026-09-10) — `find_file`이 찾은 걸 `open_file`이 못 열던 것. 못 찾았을 때만 **같은 탐색을 한 번** 한다. 🚨 **삭제에는 붙이지 않았다** — 여는 건 되돌릴 수 있지만 지우는 건 아니다(의도된 비대칭, 테스트로 고정).<br>**BL-32 해결**(2026-09-10) — 승인해서 지웠는데 **지웠다고 말하지 않던 것**. 위험 도구 결과를 `output_guard` 접미로 **도구가 만든 문장 그대로** 싣는다. «안 한 걸 했다고 말하는 것»(BL-12·19·26)의 거울상이다.<br>같은 날 **BL-24·27·28·29·17·30** 여섯을 닫았다. 그중 **BL-17·BL-30은 실기에서 나왔다** — mock 114건이 전부 초록인데 깨진 자리다.<br>**BL-26 해결**(2026-09-09) — «안 한 걸 했다고 말한 것». 뿌리는 **유령 창**이었다(정지된 UWP의 cloaked 창을 «있는 창»으로 셌다). ⚠️ 교훈: **«확인한다»는 절반이고 «무엇을 창으로 볼 것인가»가 나머지 절반**이다 — 1차 수정이 같은 유령에 속아 통과했다.<br>**BL-25 해결**(2026-09-09) — 음성이 첫 번째만 되던 것(레코더 두 개).<br>**BL-20 해결**(2026-09-09) — 위험 도구 거부가 같은 배치의 **안전한 호출까지** 조용히 취소하던 것. 안전한 호출을 **새 id로 재발행**해 살린다 → [ADR](design/BL-20_거부후_안전호출.md). ⚠️ **라이브 미검증**(위험+안전 배치가 실기에서 드물다).<br>**BL-24 해결**(2026-09-10) — 위험 호출이 **둘**이면 질문은 하나만 나가고 승인 시 **둘 다** 실행되던 것(**묻지 않은 삭제**). 질문이 전부를 이름으로 부르고, 승인 시 **물어본 것만 재발행**해 실행한다 — 마지막 `AIMessage`에 없으면 실행될 수 없다 → [ADR](design/BL-24_다중위험호출_승인.md). 덤으로 `target_exists`가 첫 호출만 보던 것(배치 순서에 따라 결과가 달라졌다)도 고쳐졌다. ⚠️ 라이브 미검증.<br>**BL-27 해결**(2026-09-10) — 캐시가 **승인 응답 「그래」를 `close_app`으로 학습**했던 것(승인 맥락이 아닐 때 「그래」라고 하면 시킨 적 없는 앱 종료가 실행됐다 — 캐시는 HITL을 안 거친다). **발화** 쪽 학습 자격(L1·L2·L3)과 부정·대조 게이트를 넣고 이미 박힌 4개를 **선별 제거**했다(동적 7 → 3). ⚠️ 교훈: **ADR에 쓴 필터가 틀린 걸 구현 중에 쟀다** — «entity와 action 둘 다»는 시드 9개를 막았다 → [M5 §3-3 정정](design/M5_임베딩_캐시.md)<br>**BL-19 부분 해결** — 거짓 보고는 막았고 재시도를 `tool_choice` 강제로(2026-09-07). 호출률 자체는 모델 쪽이라 그대로.<br>**BL-21 해결**(2026-09-07) — 계획이 건너뛴 단계를 완료로 세고 **캐시에 학습**되던 것 → [BACKLOG](BACKLOG.md) |

### 바로 시작하려면

1. [TASKS.md](TASKS.md) — **지금 뭘 하는 중이고 다음이 뭔지.** 체크리스트 ← **여기부터**
2. [ROADMAP.md](ROADMAP.md) — 어디로 가는가. 학기 목표와 계획 대비 실제 진행
3. [BACKLOG.md](BACKLOG.md) — 개별 항목 상세 (BL-xx)
4. [DEVLOG.md](DEVLOG.md) 최상단 — 직전에 무슨 일이 있었는지
5. [WORKFLOW.md](WORKFLOW.md) — 작업 루프 · 테스트 실행법 · 커밋 규칙

> ⚠️ **환경 두 가지를 먼저 확인하세요** (매번 걸리던 것)
> - 루트 `python`(anaconda base)에는 langgraph가 없다 → `conda activate pluiz`
> - Windows 콘솔이 cp949 → `PYTHONIOENCODING=utf-8`
> - 새 환경이면 `python tests/test_dependencies.py` 로 의존성부터 확인

---

## 작업별 라우팅

**무엇을 하려는지 찾아서, 지정된 문서를 읽고 시작하세요.**

| 하려는 작업 | 읽을 문서 |
|---|---|
| **새 도구 추가** | [ARCHITECTURE.md § 도구](ARCHITECTURE.md#도구-41개) → [WORKFLOW.md § 도구 추가 절차](WORKFLOW.md#새-도구-추가) |
| **버그 수정** | [BACKLOG.md](BACKLOG.md)에서 항목 확인 → [WORKFLOW.md § 작업 루프](WORKFLOW.md#작업-루프) |
| **새 파일을 어디 둘지 모를 때** | [STRUCTURE.md § 배치 결정 트리](STRUCTURE.md#새-파일-배치-결정-트리) |
| **에이전트 동작(그래프) 수정** | [ARCHITECTURE.md § 그래프](ARCHITECTURE.md#그래프-파이프라인) → [design/M1_아키텍처_설계.md](design/M1_아키텍처_설계.md) |
| **보안·가드레일 수정** | [ARCHITECTURE.md § 보안](ARCHITECTURE.md#보안--5층-방어) |
| **웨이크워드 재학습·데이터** | [design/M7_웨이크워드_재구축.md](design/M7_웨이크워드_재구축.md) ← **전시회(11/24) 대비 재구축 계획** |
| **캐시 매칭·학습 수정** | [ARCHITECTURE.md § 캐시](ARCHITECTURE.md#커맨드-캐시) → [design/M1_P4_캐시정책.md](design/M1_P4_캐시정책.md) |
| **구조를 바꾸는 큰 결정** | [design/](design/)에 ADR 먼저 작성 → [WORKFLOW.md § 설계 결정](WORKFLOW.md#설계-결정-adr) |
| **«이걸 왜 만드나»가 흔들릴 때** | `docs/planning/제품_방향.md` — 제품 판단과 열린 결정 (🔒 로컬 전용) |
| **미팅·보고 자료 준비** | `docs/meetings/` — 날짜별 동결 스냅샷. 가장 최근 것을 골라 새로 쓴다 (🔒 로컬 전용) |
| **테스트 작성·실행** | [WORKFLOW.md § 테스트](WORKFLOW.md#테스트) · 수동 항목은 [testing/](testing/) |
| **환경이 이상할 때 (도구가 조용히 안 됨)** | `python tests/test_dependencies.py` → [WORKFLOW.md § 의존성](WORKFLOW.md#️-의존성이-없으면-도구가-조용히-죽는다) |
| **커밋·브랜치·PR** | [WORKFLOW.md § 커밋 컨벤션](WORKFLOW.md#커밋-컨벤션) · [CONTRIBUTING.md](CONTRIBUTING.md) |
| **지금 뭘 하는 중인지 / 다음 작업 고르기** | [TASKS.md](TASKS.md) |
| **학기 목표·최종 시스템 방향 확인** | [ROADMAP.md](ROADMAP.md) |
| **발표·제출 자료 준비** | `docs/presentation/` (🔒 로컬 전용) |

---

## 문서 목록

### 개발 문서

| 문서 | 내용 | 갱신 주기 |
|---|---|---|
| [STRUCTURE.md](STRUCTURE.md) | 디렉토리 구조와 **왜 그렇게 배치했는가**. 새 파일 배치 결정 트리 | 구조 변경 시 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 시스템이 **어떻게 동작하는가**. 그래프 파이프라인·도구·보안·캐시 | 동작 변경 시 |
| [WORKFLOW.md](WORKFLOW.md) | **어떻게 작업하는가**. 작업 루프·테스트·DEVLOG 규칙·커밋 컨벤션 | 절차 변경 시 |
| [DEVLOG.md](DEVLOG.md) | 개발 일지. **단일 진실 공급원(SoT)** | 작업할 때마다 |
| [TASKS.md](TASKS.md) | **작업 체크리스트.** 순서대로 체크하며 진행 | 작업할 때마다 |
| [ROADMAP.md](ROADMAP.md) | **어디로 가는가.** 최종 시스템 목표·학기 계획·계획 대비 실제 | 학기 단위 |
| [BACKLOG.md](BACKLOG.md) | 미해결 항목. [즉시/위험] vs [TODO/품질] | 발견·해결 시 |
| [CONTRIBUTING.md](CONTRIBUTING.md) | 팀 협업 규칙 (브랜치 구조·작업 루틴·충돌 해결) | 거의 없음 |

> 🔒 **저장소에 없는 문서 3개**(`docs/planning/` · `docs/meetings/` · `docs/presentation/`)는
> 아래 「저장소에 없는 문서」 절에 있다. 로컬에는 있고 git에만 안 올라간다.

### 설계 문서 (ADR) — [design/](design/)

| 문서 | 내용 |
|---|---|
| [M1_아키텍처_설계.md](design/M1_아키텍처_설계.md) | 맥락 붕괴 버그의 근본 원인 진단 + To-Be 그래프 설계. **이 프로젝트에서 가장 중요한 문서** |
| [M1_P1.5_그래프이관_계획.md](design/M1_P1.5_그래프이관_계획.md) | 구 엔진 → 그래프 로직 1:1 이관 매핑 |
| [M1_P4_캐시정책.md](design/M1_P4_캐시정책.md) | 동적 학습의 자격·저장구조·정리·롤백 정책 |
| [M1_P5_엔진단일화.md](design/M1_P5_엔진단일화.md) | 구 엔진 제거 배경 + **기능 대응표·복원 방법**. 신 엔진에 문제가 생기면 여기부터 |
| [M2_웨이크워드_전용모델.md](design/M2_웨이크워드_전용모델.md) | Whisper로 웨이크워드가 안 되는 이유(실측) + 전용 모델 학습 설계. **구현 완료**(2026-09-08 런타임 교체). §4에 **«실제 녹음이 반드시 필요하다»가 측정으로 반박된 기록**이 있다 — 병목은 «합성이라서»가 아니라 «화자가 2개라서»였다 |
| [M3_계획수립노드.md](design/M3_계획수립노드.md) | 복합 명령을 단계로 쪼개 실행하는 `planner` 노드. **구현 완료**(2026-09-05). `recursion_limit: 10`이 이미 도구 3회를 상한으로 묶고 있다는 발견 포함. ⚠️ §6-3의 단계 완료 판정은 **M3-1이 대체한다** |
| [M3-1_단계완료판정.md](design/M3-1_단계완료판정.md) | 커서가 진행률이 아니라는 것 — 라이브에서 건너뛴 단계가 완료로 기록됐다(BL-21 ①). `min(커서, 도구 호출 수)`로 바꾸고 **실행 강제는 하지 않기로** 한 근거 (2026-09-07) |
| [M3-2_미시도_단계_재시도.md](design/M3-2_미시도_단계_재시도.md) | 계획의 **안 해 본 단계**만 한 번 더 시도한다(M-01). M3-1 §6이 열어 둔 문인데, **§6대로 `tool_choice` 강제를 쓰면 헛호출이 «못 했어요»를 침묵시킨다** — 그래서 강제 없이 **묶는 목록으로만** 보장한다 (2026-09-14) |
| [BL-20_거부후_안전호출.md](design/BL-20_거부후_안전호출.md) | 위험 도구를 거부해도 **같이 온 안전한 호출은 살린다**(BL-20). 백로그의 해결 후보가 **왜 그대로는 안 되는지**(`ToolNode`가 마지막 `AIMessage`의 tool_calls를 전부 실행한다)와 «새 id로 재발행»이라는 대안, 그리고 M3-1의 진행 근거가 부풀지 않게 막는 §5 (2026-09-09) |
| [M4_포인팅_확대.md](design/M4_포인팅_확대.md) | *"그게 어딨지"* → 화면에 표시한다(2주차 메인). **설계의 대부분은 그리기가 아니라 실패 방지다** — ① 오버레이가 클릭을 가로채는 것 ② 안 사라지는 것 ③ **틀린 곳을 자신 있게 가리키는 것**(절대규칙 9의 포인팅판). 그리고 §5: **우리 표시가 우리 스크린샷에 찍히고 화면 감시를 오작동시킨다** (2026-09-09) |
| [BL-24_다중위험호출_승인.md](design/BL-24_다중위험호출_승인.md) | 위험 호출이 **둘**일 때의 승인(BL-24). **구현 완료**(2026-09-10). 질문이 전부를 이름으로 부르고, 승인 시 «물어본 것만» 재발행한다 — **마지막 `AIMessage`에 없으면 실행될 수 없다**. §4에 **부분 승인을 하지 않기로 한 근거**(승인 피로가 승인 절차를 무력화한다)가 있다 |
| [BL-29_도구_결과_계약.md](design/BL-29_도구_결과_계약.md) | 도구 반환 문자열의 **첫 글자가 계약**이라는 것(BL-29). **구현 완료**(2026-09-10). 「에러 핸들링 표준화」가 스타일 문제인 줄 알았는데, **`✗`(52곳)를 T04가 못 봐서 거짓 성공이 사용자에게 나가고 있었다.** §4: 쓰는 쪽을 통일해도 **내일 새 도구가 어긋난다** — 그래서 읽는 쪽을 하나로 모으고 **소스 전수 스캔**으로 어긋남을 잡는다. §5-2-1에 **구현 중 ADR이 틀린 걸 알고 고친 기록** |
| [M6_내보내기_가져오기.md](design/M6_내보내기_가져오기.md) | 캐시·즐겨찾기를 zip 하나로 (클라우드를 «안 하기로» 한 것의 대안). **구현 완료**(2026-09-10). §3이 핵심 — **가져오기는 학습의 다른 입구다**: BL-27 필터가 `learn()`에만 있어서, 파일을 그대로 받으면 **오늘 고친 것이 zip 하나로 되돌려진다**. `.env`는 «빼는» 게 아니라 **allowlist라 넣을 수 없다** |
| [M5_임베딩_캐시.md](design/M5_임베딩_캐시.md) | 캐시 매칭을 difflib(0.80) → **로컬 임베딩 검색 + 검증기**로(3주차 · 심사의 «RAG» 항목). **설계 확정 · 구현 전**(2026-09-10). §2에 **현행 매칭을 처음 잰 분모**가 있다 — difflib Stage 2는 평가셋 32문장에서 **한 번도 발화하지 않았고**, 승인 응답 `'그래'`가 `close_app`으로 학습돼 있었다([BL-27](BACKLOG.md)). §3-2: **임베딩은 후보 생성기이지 결정자가 아니다** |
| [BL-46_오프라인_LLM_생략.md](design/BL-46_오프라인_LLM_생략.md) | 오프라인이면 **LLM을 아예 부르지 않는다**(BL-46). **구현 완료**(2026-09-12) — 실패 턴 **8.01초 → 0.17초**. §2가 핵심: **판정은 `graph_agent`(바깥)에 있고 호출은 `graph`(안)에 있다.** 바깥에서 그래프를 건너뛰면 `fast_path`가 안에 있어 **오프라인 캐시 실행이 통째로 죽고**, 바깥에서 `fast_path`를 한 번 더 부르면 **도구가 두 번 실행된다**. §3-1: 이제 오프라인 오판이 **턴의 생사**를 가르므로 단락 직전에 **캐시 없이 한 번 더** 확인한다 |

### 기술 동향 점검 — [research/](research/)

월 1회 같은 항목 5개를 본다. **«안 했으면 왜»까지 적는 것**이 이 폴더의 규율이다.

- [research/README.md](research/README.md) — 고정 항목 · 기록 규율 · 주기(9월 말 · 10월 말 · 11월 중순)
- [2026-09_동향점검.md](research/2026-09_동향점검.md) — **1회차 = 기준선.**
  `anthropic` 메이저 1개(안 올림 · 사유 기록) · computer use/Copilot Actions 공식 문서 기준선 ·
  로그에서 도구 사용을 훑어본 **미리보기**(⚠️ 테스트 턴을 안 거른 숫자다)
  → 제대로 잰 것은 [2026-09_도구사용_실측.md](research/2026-09_도구사용_실측.md)이다

### 테스트 문서 — [testing/](testing/)

자동 검증이 불가능한 UI·음성·시각 확인 항목. 자동 테스트는 [`../tests/`](../tests/) 참조.

- [REHEARSAL_2.md](testing/REHEARSAL_2.md) — 🎬 **2차 리허설 한 판의 기록.** 대본 9장면 + 🆕 4건(M-01·D-01a) + 남은 3건 ← **지금 여기부터**
- [LAUNCH_CHECKLIST.md](testing/LAUNCH_CHECKLIST.md) — **37건.** `launch.bat` 띄우고 **지금 돌려볼 것만** · 1차 결과가 채워져 있다
- [MANUAL_TESTS.md](testing/MANUAL_TESTS.md) — **77건.** 사람 눈·귀·손이 필요한 것 **전체 카탈로그**

> **`REHEARSAL_2`는 «한 판»이고 나머지 둘은 «누적»이다.** 회차 문서는 돌고 나면
> 답을 `LAUNCH_CHECKLIST`의 해당 줄로 **옮기고** DEVLOG에 기록한다 —
> 두 곳에 답이 따로 남으면 다음 세션이 어느 쪽을 믿을지 모른다.

> **누적 둘의 역할이 다르다.** `MANUAL_TESTS`는 **상시 카탈로그**(통과 기록이 있는 회귀 50건 포함)이고,
> `LAUNCH_CHECKLIST`는 그중 **«아직 아무도 안 본 것»만 돌리는 순서로 세운 진행표**다(2026-09-11 신설).
> 기대 동작의 근거는 카탈로그와 BACKLOG에만 쓰고 진행표는 **링크만** 한다.

> 2026-09-08에 세 문서(`TEST_CASES` 88건 · `MANUAL_TEST_CASES` 56건 · `MANUAL_TESTS`)를
> **하나로 합쳤다**(BL-10). 자동 검증되는 항목과 **제거된 구 엔진을 가리키던 회귀 12건**을
> 걷어냈다. Vision·승인·계획의 확인 절차는 [TASKS.md](TASKS.md) ①에 있고 **옮겨 적지 않는다.**

### 🔒 저장소에 없는 문서 — 로컬 전용

아래 세 폴더는 **개인 작성용이라 `.gitignore`에 있다**(2026-09-08 결정).
저장소가 public이기 때문이다. **로컬 작업 트리에는 그대로 있으므로 열어서 읽으면 되고,**
여기서는 **링크가 아니라 경로로만** 적는다 — 링크로 걸면 CI 링크 검사와 GitHub 양쪽에서
깨진다(체크아웃에는 이 폴더들이 없다).

| 경로 | 내용 |
|---|---|
| `docs/planning/제품_방향.md` | **무엇을 만들고 싶은가**와 **아직 안 정한 것**(⏸️ 열린 결정). computer use와의 비교, 10월 이전 3주 제안 |
| `docs/meetings/` | **미팅 브리프.** 날짜별로 **동결된 스냅샷**이라 나중에 갱신하지 않는다.<br>가장 최근: `2026-09-11_제품_기능_현황.html` — **무엇이 되고·무엇이 안 되고·왜 안 되는가** 전체 그림(되는 것 15 · 미검증 37 · 안 되는 것 8 · 버린 것 7 · 전시회 11/24까지 일정) |
| `docs/presentation/` | 2026.06.15 데모 시점 산출물 — `STUDY_GUIDE.md`(발표용 기술 가이드) · `pluiz_QnA.md`(예상 질문 답변서) · `pluiz_evolution.md`(V0 → V1 → V2) · `pluiz_presentation.html`(reveal.js 슬라이드 + 다이어그램 8개) |

> ⚠️ `docs/presentation/`은 **과거 스냅샷이다.** 슬라이드에 방학·2학기 로드맵이 들어
> 있었으나 계획은 [ROADMAP.md](ROADMAP.md)가 담당한다. 슬라이드는 갱신하지 않는다.
>
> ⚠️ **이 폴더들은 2026-09-08 이전 커밋 히스토리에는 남아 있다.**
> `docs/presentation/`은 그 시점에 **이미 원격에 올라가 있었다.**

### 아카이브 — [../archive/](../archive/)

대체됐지만 히스토리 참고용으로 보존.

- [core_agent_v1.py](../archive/core_agent_v1.py) — 구 엔진 `PluizAgent`(771줄). 대응표는 [M1_P5](design/M1_P5_엔진단일화.md#4-3-구-엔진의-주요-구성-뭘-찾아볼지)
- [HANDOFF.md](../archive/HANDOFF.md) — 2026.06.22 인수인계 스냅샷 (구 엔진 기준)
- `ui.html` — Electron 이전의 브라우저 단독 UI

---

## 문서 원칙

**한 사실은 한 문서에만 쓴다.**

과거에 `CLAUDE.md`가 코드와 크게 어긋난 적이 있는데, 원인은 같은 내용이 여러 문서에 중복돼 한쪽만 갱신됐기 때문이다. 그래서:

- 구조는 `STRUCTURE.md`, 동작은 `ARCHITECTURE.md`, 절차는 `WORKFLOW.md`가 **단독으로** 책임진다.
- [`../CLAUDE.md`](../CLAUDE.md)는 내용을 담지 않고 **여기로 안내만** 한다.
- [`../README.md`](../README.md)는 외부 방문자용 소개이므로 상세를 담지 않고 이 허브로 링크한다.
- 중복이 필요해 보이면, 중복 대신 **링크**한다.
