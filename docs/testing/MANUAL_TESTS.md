# Pluiz v2 — 수동 테스트 (사람 눈·귀·손이 필요한 것만)

> 📍 [문서 허브](../README.md) · 자동 테스트는 [`../../tests/`](../../tests/) ·
> 개수 출처는 [README 상태표](../README.md)

**여기엔 자동으로 검증할 수 없는 것만 있다.** 코드가 검증할 수 있는 것은 전부 뺐다 —
테스트 코드가 진실이고, 문서가 같은 것을 또 주장하면 한쪽만 낡는다.

> ### 이 문서가 하나로 합쳐진 이유 (2026-09-08, BL-10)
>
> 전에는 `TEST_CASES.md`(88건) · `MANUAL_TEST_CASES.md`(56건) · `MANUAL_TESTS.md`가
> **자동/수동 경계를 서로 다르게 잡은 채** 셋 다 부분적으로 낡아 있었다.
> 겹치는 항목이 많았고, **회귀 항목 12건 중 상당수가 2026-09-01에 제거된 구 엔진**
> (`core/agent.py` · `[PluizAgent]` 로그)을 가리키고 있었다. 셋을 이 문서 하나로 합쳤다.
>
> **뺀 것과 그 이유** — 보안 필터 11건(`test_injection.py` 23건이 검증) ·
> 캐시 경로 13건(`test_cache_*.py` · `test_commands.py`) · 조사 처리 5건
> (`test_dependencies.py`가 `을(를)` 하드코딩을 검사) · 구 엔진 회귀 12건(대상이 없다) ·
> 파일 생성 3건(`test_commands.py`).

---

## 🚩 지금 확인해야 하는 것 — 우선순위

**전체 77건 중 «아직 아무도 안 본» 것은 아래 27건**이다. 나머지는 통과 기록이 있는
상시 회귀 항목이라 급하지 않다.

| 순서 | 무엇 | 왜 이 순서인가 | 몇 분 | 절 |
|---|---|---|---|---|
| **0** | 🔴 **2026-09-11에 들어간 것 11** | 하루에 **BL-33·34·35 셋**이 들어갔다. 셋 다 실기가 시작점이었고 **mock 1,451개가 초록인 채로 살아 있던** 결함이다 | 15분 | [§15](#15-오늘-들어간-것--2026-09-11-11건--미검증) |
| **1** | 🔴 **2026-09-10에 들어간 것 6** | 코드가 들어갔는데 **한 번도 안 돌려 봤다** | 15분 | [§12](#12-오늘-들어간-것--2026-09-10-6건--미검증) |
| **2** | 🟡 **승인·거부 5** | 되돌릴 수 없는 동작이 걸려 있다. **틀리면 피해가 남는다** | 10분 | [§13](#13-승인--거부-5건) |
| **3** | 🟡 **웨이크워드 2** | 09-07에 들어갔는데 **눌러 본 적이 없다** | 5분 | [§6](#6-웨이크워드--2건--미검증) |
| **4** | 🟡 **계획 수립 5** | 기본 켜짐인데 라이브를 안 봤다 | 10분 | [§14](#14-계획-수립-m3--5건) |
| ⚪ | 상시 회귀 50건 | 통과 기록이 있다. **이상할 때만** | — | §1~§11 |

> ### 이상하면 «그 증상만» 적으면 된다
> 전부 적을 필요 없다. **어떤 항목이 · 무엇을 기대했는데 · 뭐가 나왔는지** 한 줄이면
> 충분하고, 로그가 필요하면 `logs/pluiz.log`의 해당 줄만 있으면 된다.
>
> ⚠️ **«코드가 들어갔다»와 «동작한다»는 다르다.** 2026-09-04에 mock 92개가 전부
> 초록인데 화면 감시가 **한 번도 켜진 적이 없었다**(BL-19). 그 뒤로 이 프로젝트는
> 둘을 따로 센다 — 이 문서가 그 «따로»에 해당한다.

---

## 실행 준비

```bash
conda activate pluiz          # ⚠️ 루트 python(anaconda base)엔 langgraph가 없다
launch.bat                    # 서버 + Electron UI 동시 실행
```

> ⚠️ **`start.bat`이 아니다.** `start.bat`은 **서버만** 띄운다(UI 없음).
> 서버를 두 번 띄우지 말 것 — 두 번째 인스턴스가 죽으면서 첫 번째의 토큰 파일을
> 지워 UI를 끊는 사고가 있었다.

**판정**: 기대 동작을 **전부** 충족해야 ✅. 하나라도 어긋나면 ❌ 이고,
**그 증상만** 적어 두면 된다(로그는 `logs/pluiz.log`).

---

## 1. UI 상태 전환 (Electron) — 8건

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| U-01 | idle pill 단클릭 | active view로 확장 | ☐ |
| U-02 | idle pill 더블클릭 | 확장 + **마이크 녹음 자동 시작** | ☐ |
| U-03 | active 헤더 더블클릭 | idle pill로 축소 | ☐ |
| U-04 | active 상태에서 `ESC` | idle pill로 축소 | ☐ |
| U-05 | `Alt+Space` | idle ↔ active 토글 | ☐ |
| U-06 | ✕ 버튼 | 앱 완전 종료 (프로세스가 사라진다) | ☐ |
| U-07 | ⚙️ 버튼 (idle 상태) | 설정 오버레이 열림 | ☐ |
| U-08 | ⚙️ 버튼 (active 상태) | 설정 오버레이 열림 | ☐ |

---

## 2. 탭 — 히스토리 · 즐겨찾기 — 6건

> ⚠️ 인증(BL-14)이 붙어 있어, 토큰이 안 실리는 경로가 있으면 **그 기능만 조용히 401**이 난다.
> 화면에 아무 일도 안 일어나면 그게 증상이다.

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| H-01 | 📋 클릭 | 히스토리 탭 열림 (채팅·입력창 숨김) | ☐ |
| H-02 | 히스토리 항목 클릭 | 채팅 탭으로 전환 + 그 명령이 입력창에 채워짐 | ☐ |
| H-03 | 「전체 삭제」 + 확인 | 목록이 비워짐 | ☐ |
| H-04 | ⭐ → 「+ 추가」 → 이름·명령 저장 | 목록에 추가됨 | ☐ |
| H-05 | 즐겨찾기 항목 클릭 | 채팅 탭 전환 + 그 명령이 **즉시 전송** | ☐ |
| H-06 | **앱 재시작 후** ⭐ 열기 | 이전에 추가한 항목이 남아 있음 (영속 저장) | ☐ |

---

## 3. 서버 · 연결 · 설정 UI — 4건

| ID | 상황 | 기대 동작 | 결과 |
|---|---|---|---|
| C-01 | 서버 켜진 상태로 UI 실행 | idle pill 상태 점(●)이 **초록** | ☐ |
| C-02 | **서버 없이** UI만 실행 | 상태 점 **빨강** + "서버 연결 중…" | ☐ |
| C-03 | API 키 없이 서버 실행 후 UI | 설정 오버레이가 **자동으로** 열림 (취소 버튼 없음) | ☐ |
| C-04 | 설정창에서 provider + 유효한 키 → 「저장하고 시작」 | "✓ 저장 완료!" → 1초 뒤 닫힘 → 상태 점 초록 전환 | ☐ |

---

## 4. 음성 입력 (마이크가 있어야 한다) — 4건

> ⚠️ **2026-09-11에 «8초»가 없어졌다**(BL-33). 이 표는 카운트다운을 전제로 쓰여
> 있었다. 발화 종료 감지로 바뀐 뒤의 항목은 **[§15](#15-오늘-들어간-것--2026-09-11-11건--미검증)** 에 있다.

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| V-01 | 🎙️ 클릭 → *"메모장 열어줘"* | **입력 레벨 링** → 말을 멈추면 알아서 끊김 → 인식 텍스트 → 메모장 실행 → TTS | ☐ |
| V-02 | 🎙️ 클릭 → **말 없이 둔다** | 6초쯤 뒤 *"말이 없어서 그만 들었어요"* → ready 복귀. 🚨 **«인식하지 못했습니다»가 뜨면 안 된다** | ☐ |
| V-03 | 녹음 중 ⏹️ 클릭 | 즉시 중단 → 음성 처리 시작 | ☐ |
| V-04 | 아주 작은 소리(사실상 무음) | **서버로 보내지 않고** ready 복귀 | ☐ |

---

## 5. TTS 재생 (스피커) — 4건

| ID | 상황 | 기대 동작 | 결과 |
|---|---|---|---|
| T-01 | 텍스트로 명령 | 채팅에 표시되면서 **동시에** 음성 재생 | ☐ |
| T-02 | 음성으로 명령 | 응답 텍스트 + 음성 재생 | ☐ |
| T-03 | 긴 응답(50자 이상) | 끊기지 않고 **끝까지** 재생 | ☐ |
| T-04 | 마이크를 켠 순간 TTS가 재생 중이면 | TTS가 **중단**된다 | ☐ |

---

## 6. 웨이크워드 — 2건 ⚠️ 미검증

> **아직 아무도 확인한 적이 없다.** 설정 화면은 2026-09-07에 들어갔고 배선 테스트
> 25건은 통과했지만 **마우스로 눌러 본 적이 없다.** → [TASKS.md](../TASKS.md) ①

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| K-01 | *"플루이즈"* 라고 부른다 | 창이 앞으로 나온다 | ☐ |
| K-02 | ⚙️ → 호출어를 다른 말로 바꾸고 **저장** → **10초 대기** → 그 말로 부른다 | 바뀐 말에 반응한다. 저장 응답이 *"10초 안에 반영"* 을 **말해 준다** | ☐ |

> 10초가 걸리는 건 웨이크워드가 Electron이 띄운 **별도 프로세스**라 `.env`를 주기적으로
> 다시 읽기 때문이다. 이걸 모르면 저장 직후 불러 보고 **고장이라고 생각한다.**

---

## 7. 시스템 수치 — 눈으로 확인해야 한다 — 4건

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| S-01 | *"볼륨 50으로 맞춰줘"* | 작업표시줄 볼륨이 **50%** | ☐ |
| S-02 | *"소리 좀 크게 해줘"* | 약 10% 증가 | ☐ |
| S-03 | *"화면 밝기 올려줘"* | 밝아진다 | ☐ |
| S-04 | *"밝기 내려줘"* | 어두워진다 | ☐ |

---

## 8. 브라우저가 **실제로** 열리는가 — 3건

> 도구가 성공을 반환하는 것과 브라우저가 뜨는 것은 다르다. 그래서 남긴다.

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| W-01 | *"유튜브 열어줘"* | 기본 브라우저로 youtube.com | ☐ |
| W-02 | *"유튜브에서 아이유 검색해줘"* | 검색 결과 페이지가 열림 | ☐ |
| W-03 | *"서울시청에서 강남역 가는 길 알려줘"* | 지도 **경로** 검색이 열림 | ☐ |

---

## 9. 키보드 입력이 **그 앱에** 들어가는가 — 4건

> BL-12가 여기서 나왔다 — 도구는 *"✓ 입력 완료"* 라는데 **엉뚱한 창**에 들어갔다.
> 지금은 입력 전에 대상 창을 확인하고, `visual_verify`가 화면으로 재확인한다.
> **그래도 눈으로 봐야 한다** — 자동 검증이 볼 수 없는 마지막 한 칸이다.

| ID | 사전 조건 | 입력 | 기대 동작 | 결과 |
|---|---|---|---|---|
| I-01 | 메모장 열어 둠 | *"메모장에 안녕하세요 라고 써줘"* | **메모장에** 글자가 들어간다 | ☐ |
| I-02 | 아무 앱 활성 | *"엔터 눌러줘"* | 포커스된 앱에 Enter | ☐ |
| I-03 | 편집 가능한 앱 활성 | *"저장 단축키 눌러줘"* | Ctrl+S | ☐ |
| I-04 | 텍스트 있는 앱 활성 | *"Ctrl+A 눌러줘"* | 전체 선택 | ☐ |

---

## 10. 파일 · 앱이 **실제로** 뜨는가 — 5건

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| F-01 | *"최근에 열었던 파일 보여줘"* | 탐색기가 최근 항목 폴더를 연다 | ☐ |
| F-02 | *"바탕화면에 할일.txt 만들고 메모장으로 열어줘"* | 파일 생성 **+ 메모장이 그 파일을 연다** | ☐ |
| A-01 | *"크롬 열어줘"* (이미 실행 중일 때) | **새 창을 만들지 않고** 기존 창이 앞으로 온다 | ☐ |
| A-02 | *"새로 메모장 열어줘"* | 이번엔 **새 창**이 뜬다 | ☐ |
| A-03 | *"설정 열어줘"* | Windows 설정 앱이 열린다 | ☐ |

> A-01·A-02가 한 쌍인 이유 — 예전에 `open_app`이 기존 창을 두고 **새 창을 만들면서**
> *"앞으로 가져왔다"* 고 답했다. → [ARCHITECTURE § 창 규칙](../ARCHITECTURE.md#창-규칙--기존-창-재사용)

---

## 11. Vision — 좌표 클릭 · 화면 감시 · 포인팅 — 6건

> **2026-09-07·09에 전부 통과했다.** 재확인이 필요할 때 쓰는 절차로 남겨 둔 것이다.
> (2026-09-10에 TASKS ①에서 이 문서로 옮겨 왔다 — BL-10의 나머지 절반)

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| P-01 | *"메모장에서 파일 메뉴 눌러줘"* | **승인 질문**이 뜨고 → *"응"* → 파일 메뉴가 **실제로 열린다** | ☐ |
| P-02 | *"메모장에서 인쇄 버튼 눌러줘"* (없는 것) | **아무 데도 안 눌리고** *"찾지 못해 클릭하지 않았습니다"* | ☐ |
| P-03 | *"메모장 지켜보다가 글자 생기면 알려줘"* → 메모장에 타이핑 | 고지(5초마다·10분/20회 상한·"그만 봐") → **5~15초 안에** 오버레이가 앞으로 + 소리 | ☐ |
| P-04 | 감시 중 그 창을 **최소화** | **창이 깜빡이면 안 된다.** *"창이 안 보여서 멈췄어요"* 알림 | ☐ |
| P-05 | 감시 중 *"그만 봐"* | 즉시 멈춤 + 헤더의 👁 배지가 사라진다 | ☐ |
| P-06 | *"메모장 닫고"* → *"메모장에서 파일 메뉴 크게 확대해서 보여줘"* | *"'메모장'이 꺼져 있어서 먼저 열었어요"* + 고리·돋보기가 뜨고 **대상을 안 가린다** · 9초대 | ☐ |

> ⚠️ **최소화된 창에서는 클릭이 안 된다**(캡처 불가). 활성화하면 된다.
> ⚠️ P-03이 안 켜지면 `logs/pluiz.log`에 `[BL-19] … 1회 재시도(강제=watch_screen)`가
> 찍히고 **그 다음엔 켜져야 한다.** *"지켜볼게요"* 라고 해 놓고 안 켜지면 **그게 회귀다.**
> ⚠️ **좌표 정밀도는 Vision의 모델 한계**라 오차가 있다 — 고리와 *"추정한 위치예요"*
> 라벨이 그래서 있다. 대상 근처를 가리키면 통과다.

---

## 12. 오늘 들어간 것 — 2026-09-10 (6건) 🔴 미검증

> **여섯 다 코드가 들어갔고 mock은 통과했지만, 한 번도 안 돌려 봤다.**
> 여기부터 보는 게 가장 값이 크다.

| ID | 입력 / 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| N-01 | 안 깔린 앱을 시킨다 — *"한글 열어줘"* | 🚨 **"열었어요"라고 하면 안 된다.** 못 열었다고 정직하게 답한다 <span>(BL-29)</span> | ☐ |
| N-02 | 파일 이름을 **띄어쓰기 다르게** — *"개발 착수서 찾아줘"* (실제는 `개발_착수서`) | 찾아낸다. 위치를 안 줬으면 **어디서 찾았는지 말한다** <span>(BL-07)</span> | ☐ |
| N-03 | **없는 이름**을 댄다 — 비슷한 파일이 있는 이름으로 | *"혹시 이건가요? '…'"* 가 뜬다. 폴더면 **(폴더)** 라고 붙는다 <span>(BL-07)</span> | ☐ |
| N-04 | ⚙️ 설정 맨 아래 → **내보내기** | `pluiz-export-날짜.zip` 이 받아진다 <span>(M6)</span> | ☐ |
| N-05 | 그 파일로 **가져오기** → 확인 | *"이미 있는 건 그대로 뒀어요"* + **백업했다고 말한다** <span>(M6)</span> | ☐ |
| N-06 | 아무 말이나 몇 마디 → `logs/pluiz.log` | `\| 사유=잡담` 이 보인다. 앱을 켜면 `[캐시 실행]` 줄도 <span>(BL-28)</span> | ☐ |

> ⚠️ **N-05는 상태를 바꾼다.** 자기가 방금 내보낸 파일로만 하세요.
> 서버가 자동 백업을 남기지만 **되돌릴 일을 안 만드는 게 낫다.**
> ⚠️ **엉뚱한 zip을 넣어 보는 것도 확인이다** — 아무 zip이나 넣으면
> *"Pluiz 내보내기 파일이 아닙니다"* + *"아무것도 바꾸지 않았어요"* 가 나와야 한다.

---

## 13. 승인 · 거부 (5건) 🟡

> **되돌릴 수 없는 동작이 걸려 있다.** 틀리면 말로 끝나지 않고 **파일이 사라진다.**
> 미리 `a.txt` · `b.txt` 를 바탕화면에 만들어 두고 하세요.

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| D-01 | *"a.txt랑 b.txt 지워줘"* → **"아니"** | 질문이 **둘 다 이름을 부르고 «2개»** 라고 말한다. 거부하면 **둘 다 안 지워진다** <span>(BL-24)</span> | ☐ |
| D-02 | 같은 명령 → **"응"** | **둘 다** 지워진다 (하나만 지워지면 실패다) <span>(BL-24)</span> | ☐ |
| D-03 | *"메모장 열고 a.txt 지워줘"* → **"아니"** | 삭제는 취소되지만 **메모장은 열린다.** 열렸다고 **말해 준다** <span>(BL-20)</span> | ☐ |
| D-04 | 승인 대기가 **아닐 때** *"그래"* 라고 한다 | **아무 앱도 안 꺼진다** <span>(BL-27 · 09-10 한 번 통과)</span> | ☐ |
| D-05 | 🎤 *"그 파일 지워줘"* → 승인 질문 → 🎤 *"베이"*(알아들을 수 없는 말) → 🎤 *"네"* | 재질문이 뜨고, 그 다음 *"네"* 에 **삭제된다.** *"죄송해요…"* 가 뜨면 [BL-17](../BACKLOG.md) 재현 | ☐ |

> ⚠️ **D-01을 먼저 하세요.** 거부가 먼저 확인돼야 D-02에서 파일을 잃어도 놀라지 않는다.
> ⚠️ D-05는 **음성**이어야 한다 — BL-17은 텍스트에서는 재현된 적이 없다.
> 이제 로그에 `승인 판정 | 2번째 | 답변='네' → …` 가 찍힌다. **그 줄만 알려주면**
> 판정 문제인지 승인 대기가 사라진 건지 바로 갈린다.

---

## 14. 계획 수립 (M3) — 5건 🟡

> **기본 켜짐**인데 라이브에서 세 경우를 다 본 적은 없다.
> **응답 문구로 판정하지 말 것** — 실제로 떴는지 **상태**로 본다.

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| M-01 | *"메모장 열고 계산기도 열어줘"* → 이어서 *"지금 켜져 있는 앱 알려줘"* | **둘 다 실제로 떠 있다** (문구가 아니라 상태로 확인) | ☐ |
| M-02 | 위 명령의 로그 `logs/pluiz.log` | `[Plan] 계획 2단계` · `1/2 단계 완료` · `2/2 …` | ☐ |
| M-03 | 한 단계만 됐을 때 | 응답 끝에 **"다만 이건 못 했어요: …"** 가 붙는다 | ☐ |
| M-04 | *"메모장 열고 a.txt 지워줘"* → **"응"** | **삭제까지 완주**한다 (계획이 승인 중단을 살아남는지) | ☐ |
| M-05 | *"메모장 말고 계산기 열어줘"* | **계산기가** 열린다. 메모장이 열리면 실패다 <span>(BL-27 부정문)</span> | ☐ |

> 끄려면 `.env`에 `PLAN_ENABLED=false`. 캐시가 더러워졌으면
> `git checkout cache/command_cache.json` ([BL-11](../BACKLOG.md)).

---

## 15. 오늘 들어간 것 — 2026-09-11 (11건) 🔴 미검증

> **⚠️ 먼저 `main.py`와 Electron을 다시 띄우세요.** `core/graph.py`·`main.js`가 바뀌었습니다.
> ```
> conda activate pluiz && export PYTHONIOENCODING=utf-8
> launch.bat
> ```
> **콘솔을 열어 두세요**(`Ctrl+Shift+I`) — `[VAD]` 줄이 임계값 조정의 유일한 근거입니다.

### 15-A. 음성 종료 감지 (BL-33) — 5건

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| W-01 | 🎙️ → **일부러 길게** 한 문장으로: *"메모장 열고 거기에 오늘 회의에서 나온 이야기를 정리해서 적어줘"* | **끝까지** 들어간다. 채팅의 인식 텍스트에 **뒷말이 남아 있어야** 한다 | ☐ |
| W-02 | 🎙️ → 짧게 *"메모장 켜줘"* → **입을 다문다** | **1초쯤 뒤 알아서 끊긴다.** 남은 초를 기다리지 않는다 | ☐ |
| W-03 | 🎙️ → **아무 말도 안 한다** | 6초 뒤 *"말이 없어서 그만 들었어요"*. 🚨 **«⚠️ 음성을 인식하지 못했습니다»가 뜨면 회귀다** | ☐ |
| W-04 | 🎙️ → 말하면서 **다른 창을 클릭**하고 계속 말한다 | 그래도 **말이 끝나면 끊긴다**(30초까지 안 간다) <span>backgroundThrottling</span> | ☐ |
| W-05 | 녹음 중 링을 본다 | 말하기 전엔 **회색**, 말이 잡히면 **빨강** + 목소리 크기에 따라 찬다 | ☐ |

> **W-03이 오늘 가장 중요합니다.** 헛깨어남(§15-D) 뒤에 저 경고가 뜨던 것이
> 사용자에게 보이던 최악의 증상이었고, 원인은 오탐이 아니라 **`mr.stop()`이 마지막
> 조각을 flush**하던 것이었습니다.
>
> 🔑 **콘솔에 이 두 줄이 찍힙니다. 이상하면 이것만 알려주세요:**
> ```
> [VAD] 잡음 바닥 0.0031 ㅡ> 시작임계 0.0200
> [VAD] 종료 사유=endpoint 최대RMS=0.0847
> ```
> - 말이 **중간에 끊기면** → `KEEP_RMS`를 내리거나 `SILENCE_MS`를 올린다
> - **안 끝나면** → `KEEP_RMS`를 올린다 (또는 `사유=max`가 찍힌다)
> - **말했는데 `사유=nospeech`** → `START_RMS`가 목소리보다 높다. 최대RMS를 보고 내린다

### 15-B. 날씨 (BL-34) — 4건

| ID | 입력 | 기대 동작 | 결과 |
|---|---|---|---|
| X-01 | *"오늘 날씨 어때"* | **숫자가 나온다**(기온·습도·바람). 🚨 *"확인해볼게요"* 나 **링크면 회귀다** | ☐ |
| X-02 | *"어제 날씨는 어땠어"* | **어제** 값이 나온다. 오늘 값이 나오면 안 된다 | ☐ |
| X-03 | *"부산 날씨"* · *"대구 날씨"* | **그 도시** 값이다. ⚠️ 대구가 서울과 같으면 지명 해석이 깨진 것 | ☐ |
| X-04 | Wi-Fi를 끄고 *"오늘 날씨"* | *"✗ …가져오지 못했습니다"* 라고 **정직하게**. 🚨 **숫자를 지어내면 최악이다** | ☐ |

> ⚠️ **실제 기온이 맞는지는 창밖을 보면 됩니다.** mock은 네트워크를 안 써서
> **그건 한 번도 확인된 적이 없습니다.**
> 표에 없는 지명(동·읍·해외 도시)은 *"(지명을 검색해 찾은 위치라 다른 곳일 수 있어요)"*
> 가 붙는 것이 **정상**입니다 — 숨기지 않는 것이 설계 의도입니다.

### 15-C. «하겠다»고 말만 하는 것 (BL-35) — 1건

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| Y-01 | 대화 중 *"빨리 좀 해 봐"* · *"그거 알려줘"* 처럼 **재촉**해 본다 | 🚨 *"확인하고 있어요"* · *"찾아볼게요"* 로 턴이 **끝나면 안 된다.** 실제로 답하거나 *"할 수 없다"* 고 말해야 한다 | ☐ |

> **이 항목은 «안 보이는 것»이 정상입니다.** 로그에 `[BL-35]` 줄이 **뜨지 않으면**
> 그물이 뜰 일이 없었다는 뜻이고, BL-34가 그렇게 만듭니다.
> `[BL-35]` 가 찍혔다면 **그 다음 줄의 `도구=`가 비어 있지 않은지** 보세요 —
> 재시도가 값을 했다는 뜻입니다.

### 15-D. 웨이크워드 오탐 — 분모를 만든다 (BL-23) — 1건

| ID | 동작 | 기대 동작 | 결과 |
|---|---|---|---|
| Z-01 | **5분간 그냥 둔다**(평소처럼 일하면서). 끝나고 `logs/pluiz.log`에서 `→ WAKE` 를 센다 | 몇 번인지 **숫자를 적는다.** 부른 적이 없는데 뜬 것이 **헛깨어남**이다 | ☐ |

```bash
grep -c "→ WAKE" logs/pluiz.log          # 전체 깨어난 횟수
grep "→ WAKE" logs/pluiz.log | tail -20  # prob 값과 시각
```

> **왜 세는가**: 2026-09-09에 «놓침»은 *10번 중 2번*으로 분모를 만들어 판단할 수
> 있었지만, **오탐은 아직 분모가 없습니다**(*"자주 그런다"* 뿐). 5분에 몇 번인지
> 알면 완화책을 어디까지 할지 정할 수 있습니다.
>
> 🚨 **임계값 조정은 이미 «막힌 길»로 판정됐습니다** — 오탐이 `0.990`~`1.000`인데
> 놓침은 `0.5`~`0.6`이라 임계를 올리면 오탐은 그대로고 놓침만 나빠집니다.
> ⏸️ 완화책 셋(에너지 관문 · TTS 중 무시 · 재학습)은 → [BACKLOG BL-23](../BACKLOG.md)
>
> 💡 **곁들여 보면 좋은 것**: 헛깨어남이 **TTS가 말하는 중**에 몰리는지.
> 몰린다면 학습 표본이 전부 TTS라는 가설이 맞고, «TTS 중 무시»가 바로 답이 됩니다.

---

## 판정 요약

| 절 | 항목 | 상태 | 통과 / 전체 |
|---|---|---|---|
| 1 | UI 상태 전환 | ⚪ 회귀 | / 8 |
| 2 | 히스토리 · 즐겨찾기 | ⚪ 회귀 | / 6 |
| 3 | 서버 · 연결 · 설정 | ⚪ 회귀 | / 4 |
| 4 | 음성 입력 | ⚪ 회귀 | / 4 |
| 5 | TTS 재생 | ⚪ 회귀 | / 4 |
| 6 | **웨이크워드** | 🟡 **미검증** | / 2 |
| 7 | 시스템 수치 | ⚪ 회귀 | / 4 |
| 8 | 브라우저 | ⚪ 회귀 | / 3 |
| 9 | 키보드 입력 | ⚪ 회귀 | / 4 |
| 10 | 파일 · 앱 | ⚪ 회귀 | / 5 |
| 11 | Vision · 클릭 · 감시 · 포인팅 | ⚪ 회귀 | / 6 |
| 12 | **오늘 들어간 것 (09-10)** | 🔴 **미검증** | / 6 |
| 13 | **승인 · 거부** | 🟡 **미검증** | / 5 |
| 14 | **계획 수립 (M3)** | 🟡 **미검증** | / 5 |
| **15-A** | **음성 종료 감지 (BL-33)** | 🔴 **미검증** | / 5 |
| **15-B** | **날씨 (BL-34)** | 🔴 **미검증** | / 4 |
| **15-C** | **«하겠다»만 하는 것 (BL-35)** | 🔴 **미검증** | / 1 |
| **15-D** | **웨이크워드 오탐 — 분모 만들기** | 🔴 **미측정** | / 1 |
| | **합계** | | **/ 77** |

**미검증 27건**(🔴 17 · 🟡 10)이 지금 값이 큰 것이고, 나머지 50건은 통과 기록이 있다.

> ⚠️ **2026-09-11에 숫자를 다시 셌다.** 헤딩(63) · 요약(66) · §13·§14의 «4건 vs /5»가
> 서로 어긋나 있었다. 테스트 개수에서 이미 겪은 사고와 같은 모양이라
> (→ [README 상태표](../README.md)) **세어서 맞췄다.**

---

_2026-09-11 (§15 신설 — BL-33·34·35 · §4의 «8초» 전제 제거 · 합계 재집계)_
_2026-09-08 (BL-10 — 세 문서를 하나로. 자동 검증되는 항목과 구 엔진 회귀는 제거)_
_2026-09-10 — TASKS ①에 흩어져 있던 확인 절차를 여기로 **옮겨 왔다**(§11·§13·§14).
BL-10에 «남은 정리»로 적혀 있던 나머지 절반이다. [즉시/위험]이 0이 돼서
«지금 하는 중»과 «상시 카탈로그»가 더는 섞이지 않는다. 44건 → **66건**._
