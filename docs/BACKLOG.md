# Pluiz v2 — 백로그 (Backlog)

> 📍 [문서 허브](README.md) · 관련: [DEVLOG.md](DEVLOG.md) · [WORKFLOW.md](WORKFLOW.md)

> **분류 원칙**
> - **[즉시/위험]** 구조적 결함·상태/기억 오염 등 방치 시 technical debt가 되어
>   이후 개발의 토대를 흔드는 항목 → **발견 즉시 수정**.
> - **[TODO/품질]** 이미 동작하는 기능의 정확도·말투·프롬프트 튜닝 등 품질 개선 →
>   **개발 목표를 모두 마친 뒤 일괄 정리**. (다른 개발을 막지 않음)
>
> 새 항목 발견 시 위 두 태그 중 하나로 분류해 기록한다.

---

## [즉시/위험] — 미해결
현재 없음. (맥락 버그(P1), 타임아웃 부재(BL-01) 해결 완료)

---

## [TODO/품질] — 개발 목표 완료 후 정리

### BL-02 — 캐시 부정어 오매칭
- **증상**: "계산기 **말고** 다른거 열어" → 캐시가 `계산기 열어줘`로 오매칭해 계산기를 엶.
- **원인**: 커맨드 캐시가 entity(계산기)+action(열어) 키워드만 보고 부정어("말고")를 무시.
- **성격**: 매칭 정확도 엣지케이스(규칙 튜닝). 구조 위험 아님 → 미룸.
- **해결 후보**: `_COMPOUND_CMD`(캐시 바이패스 패턴)에 "말고" 추가 →
  부정어 포함 시 캐시 건너뛰고 LLM이 처리하게. (agent.py)
- **발견**: 2026-07-30 (P1 검증 중)

### BL-03 — 보안필터 공백 없는 변형 미포착
- **증상**: "rm-rf"(공백 없이) → `security.py` 정규식 `\brm\s+-[rRfF]` 이 미포착.
  실기에선 LLM이 대신 거절해 안전했으나, 코드필터가 1차로 잡는 게 바람직.
- **성격**: 보안 견고성. LLM 백스톱 존재 + 기존부터 있던 갭 → 위험도 중간, TODO로.
- **해결 후보**: 공백 선택적 패턴(`\brm\s*-[rrf]`) 및 유사 변형 보강. (security.py)
- **발견**: 2026-08-10 (P1.5-e 실기)

### BL-04 — 그래프 코어 미이관 항목 (P1.5 후속)
- 도구 미호출 재시도(RETRY_PROMPT)를 PluizGraphAgent에 미이관. 실기 문제 없었음.
- /ws 토큰 단위 스트리밍 미구현(현재 단일 청크). 음성 경로 영향 없음.
- **성격**: 품질/UX. 필요 시 노드/엣지로 추가.

### BL-07 — 파일 찾기 UX 개선
- 파일명/확장자가 모호하면 에이전트가 헤맴("개발착수서" 확장자 모를 때 등).
  find_file/open_file에 부분매칭·후보 제시·확장자 자동탐색 등 개선 여지.
- **성격**: 품질/UX. (2026-08-12 실기 중 관찰)

### BL-06 — 테스트 스위트 새 엔진 기준 갱신
- `test_commands.py`/`test_regression.py`가 옛 엔진 기준으로 작성됨(일부 판정어·대상이
  구식). 새 엔진 수렴 시 그래프 엔진 기준으로 갱신·정리 필요.
- **성격**: 테스트 유지보수. (2026-08-11 R-07·R-11 오탐 정리하며 발견)

### BL-05 — 구 엔진 완전 제거 (엔진 단일화)
- 현재 신 엔진(그래프)이 기본, 구 엔진(`core/agent.py`)은 비상 폴백으로 보존 중.
- 새 엔진 며칠 실사용 무결 확인 + BL-04 판단 후 → **`core/agent.py` 삭제,
  `core/factory.py`·`USE_GRAPH` 플래그 제거**하여 그래프 단일 엔진으로 수렴.
- **성격**: 구조 정리(개발 목표 안정화 후). 서두르지 말 것(롤백 낙하산 유지 목적).

---

_최종 업데이트: 2026-08-10_
