# Pluiz v2 — 백로그 (Backlog)

> 📍 [문서 허브](README.md) · 관련: [DEVLOG.md](DEVLOG.md) · [WORKFLOW.md](WORKFLOW.md)

> **분류 원칙**
> - **[즉시/위험]** 구조적 결함·상태/기억 오염 등 방치 시 technical debt가 되어
>   이후 개발의 토대를 흔드는 항목 → **발견 즉시 수정**.
> - **[TODO/품질]** 이미 동작하는 기능의 정확도·말투·프롬프트 튜닝 등 품질 개선 →
>   **개발 목표를 모두 마친 뒤 일괄 정리**. (다른 개발을 막지 않음)
>
> 새 항목 발견 시 위 두 태그 중 하나로 분류해 기록한다.

---

## [즉시/위험] — 미해결

**BL-19 — 부분 해결.** 거짓 보고는 막았고, 2026-09-07에 재시도를 `tool_choice` **강제**로
바꿨다(설득 → 구조). **라이브 미검증** — 나쁜 구간에서 실제로 켜지는지는 아직 못 봤다.
**BL-21 — 신규(2026-09-07).** 계획이 건너뛴 단계를 완료로 기록하고, 그게 **캐시에 학습된다**.
(BL-14가 2026-09-02에, BL-16이 2026-09-03에, **BL-20이 2026-09-09에**, **BL-24·BL-27·BL-28·BL-29·BL-17·BL-30이 2026-09-10에** 해결됨)

### BL-21 — 계획이 **건너뛴 단계를 완료로 기록**하고, 그 결과가 **캐시에 학습된다**
- **증상(실측)**: 2026-09-07 라이브. `"메모장이랑 계산기 열어줘"` →
  계획은 2단계로 제대로 섰는데 **메모장만 열리고 2/2 완료**로 끝났다.
  *"이건 못 했어요"* 도 붙지 않았다.
  ```
  13:23:22 [Plan] 계획 2단계 | 메모장 열기 / 계산기 열기
  13:23:26 턴 완료 | 도구=['open_app'] | 계획 2/2      ← open_app 1번인데 2/2
  13:26:03 턴 완료 | 도구=없음        | 계획 2/2      ← 도구 0개인데 2/2
  ```
- **원인**: 완료 판정이 **"그 패스에서 도구를 안 불렀다 = 이 단계 끝"**
  (`core/graph.py` agent 노드, [ADR §6-3](design/M3_계획수립노드.md)의 세 번째 결정).
  커서를 반드시 전진시켜 무한루프를 막는 대신, **모델이 단계를 그냥 씹으면
  시스템은 그걸 성공으로 읽는다.** M3가 막으려고 만들어진 바로 그 실패다.
- **왜 [즉시/위험]인가 — 결함이 영구화된다**: 성공한 턴은 캐시가 학습한다.
  같은 세션에서 실제로 이렇게 박혔다:
  ```
  "메모장이랑 계산기 열어줘"        → [open_app(메모장)]        ← 계산기가 없다
  "메모장 열고 testing.txt 지워달라고" → [open_app(메모장)]        ← 삭제가 없다
  ```
  다음부터 같은 말을 하면 **캐시 히트라 LLM을 아예 거치지 않는다.**
  틀린 답이 고쳐질 기회 없이 굳는다. (이번 것은 `git checkout`으로 되돌렸다)
- **성격**: [즉시/위험]. BL-12·BL-15·BL-19의 *"확인하지 않고 됐다고 말한다"* 계열인데,
  **말로 끝나지 않고 상태에 남는다**는 점에서 한 단계 더 나쁘다.
- **해결 후보**: ① 단계 패스에서 도구를 안 부르면 **한 번 강제**한다
  (BL-19 원인 ②에 쓴 `tool_choice`와 같은 수법) → 그래도 안 부르면 **미완료로 기록**하고
  `output_guard`가 말한다. ② 미완료 단계가 있는 턴은 **캐시 학습 대상에서 제외**한다.
  ⚠️ ①은 "이미 끝난 단계"에 중복 실행을 부를 수 있다(`open_app`은 창 규칙상 무해하지만
  일반적으로는 아니다). **ADR이 필요하다** — 완료 판정은 M3 설계의 뼈대다.
- **①·② 모두 2026-09-07에 해결했다.** ①은
  [M3-1 ADR](design/M3-1_단계완료판정.md) — **커서를 그대로 믿지 않는다.**
  커서는 진행률이 아니라 자기루프를 끝내려고 무조건 전진하는 **루프 제어 값**인데
  그걸 보고에 그대로 썼다. 이제 `steps_covered = min(커서, 이번 턴 도구 호출 수)`로
  **덜 낙관적인 쪽**을 믿는다. 한 신호만 보면 반드시 하나가 깨진다 —
  커서만 보면 «건너뜀»이, 도구 수만 보면 «승인 거부»가 깨진다(거부 턴은 호출이
  나와 있고 실행만 안 된다). **실행은 강제하지 않는다**: 이미 끝난 단계를 오판해
  강제하면 되돌릴 수 없는 동작을 두 번 한다. 회귀 `tests/test_plan_node.py` §9(9건).
- **②는 2026-09-07에 해결했다** — `_maybe_learn`이 **복합 명령을 학습하지 않는다**
  (`core/graph_agent.py`). 계획이 선 턴이거나 `is_compound_command`면 그 자리에서 멈춘다.
  **통째로 막아도 잃는 게 없다**: `_is_learnable`이 `len(tool_calls) != 1`을 이미 거절하므로
  정상 실행된 계획 턴(도구 2개 이상)은 애초에 학습 대상이 아니다 —
  **여기 도달하는 복합 턴은 실패한 턴뿐이다.** 계획이 꺼져 있어도 같은 일이 나므로
  발화로도 본다. 읽는 쪽에서 *"잔여 명령이 있으면 캐시를 포기한다"* 고 정한
  **BL-15의 쓰기 쪽 짝**이다. 회귀 `tests/test_cache_wire.py` §7(5건).
- **상태**: **해결 (2026-09-07)** — 다만 `PLAN_ENABLED`는 **여전히 기본 꺼짐**이다.
  라이브에서 ① *"메모장이랑 계산기 열어줘"* 에 문구가 붙는지와
  ② *"메모장 열고 계산기도 열어줘"* 에 **안 붙는지**(거짓 실패 확인)를
  **둘 다** 본 뒤에 기본값을 정한다 → [TASKS](TASKS.md) ①

### BL-23 — 웨이크워드가 **실제 사람 목소리에서 자주 놓친다** — **10번 중 2번**(2026-09-09 측정)
- **증상**: 전용 KWS 모델로 교체한 뒤 사용자가 *"변화를 잘 모르겠다"* 고 했다.
  깨어나기는 하는데(`prob=0.999`) **부르는 만큼 깨어나지 않는다.**
- **측정된 것** (`logs/pluiz.log`, 2026-09-08 23:20):
  - ✅ **에너지 관문 수정은 효과가 있었다** — «무음 스킵»이 사실상 사라지고
    (620회 → 순수 무음 1회) 모든 창이 모델에 도달한다
  - ✅ **오탐이 없다** — 발화가 아닌 창은 전부 `prob=0.000~0.015`
  - ⚠️ **놓침이 «아깝게»가 아니라 «완전히»다** — 14초 구간에서 창 20여 개 중
    `0.999` 하나, 나머지는 **0.015 이하**. 중간값이 없다(양극단만 있다)
- **그래서 무엇이 아닌가** (이걸 먼저 적는 이유: 헛다리를 막는다)
  - ❌ **임계값 조정으로는 안 된다.** 놓친 창이 0.015 이하라 임계를 0.1로 낮춰도
    안 잡히고, 대신 오탐만 생긴다
  - ❌ **에너지 관문 문제도 아니다.** 이미 고쳤고 로그로 확인됐다
  - ❌ **«학습을 더»도 아니다** — 미학습 합성 화자에는 이미 98.1%다
- **가장 유력한 원인**: **합성음(TTS)과 실제 사람 발화의 간극.** ADR §7이 최우선
  위험으로 적어 둔 바로 그것이다. 학습·평가가 **전부 TTS**라 실제 마이크·방 울림·
  조음이 한 번도 안 들어갔다. 양극단 분포가 그 모양과 맞는다 —
  모델이 «TTS스러운 발화»에만 확신하고 나머지는 확신 있게 배제한다.
  - 부차 가능성: 창 정렬(2초 창 · 0.6초 hop) · 발화 길이 · 마이크 주파수 특성
- 📏 **측정됐다 — 10번 중 2번** (2026-09-09 15:38, `logs/pluiz.log`). **분모가 생겼다.**

  | 확률 | 창 수 | 무엇 |
  |---|---|---|
  | `0.996` · `0.888` | 2 | **깨어남** |
  | `0.512` · `0.410` | 2 | 같은 발화의 인접 두 창 — **아깝게 놓침** |
  | `0.071` `0.053` `0.046` `0.039` | 4 | 반응은 했으나 미미 |
  | `0.010` 이하 | 39 | 사실상 무반응 |

- 🚨 **정정 — «놓친 창이 전부 0.015 이하»는 과일반화였다.** 위 표의 `0.512`가
  반례다(코드가 `임계×0.5` 이상을 `(놓침)`으로 INFO에 남겨 잡혔다).
  2026-09-08의 한 번 관측을 «중간값이 없다»로 굳혔던 것이다.
  **다만 결론은 바뀌지 않는다**: 임계를 0.40으로 내려도 2 → 3이고, 그 아래는 곧바로
  `0.071`이라 임계로 건질 게 없다. **틀린 것은 근거이지 판정이 아니다.**
- ✅ **에너지 관문도 이번엔 범인이 아니다** — 57창 중 «무음 스킵»은 10회뿐이고
  **47창이 실제로 점수를 받았다**(2026-09-08의 620회와 상황이 다르다).
  즉 모델이 **듣고 나서** `0.000`을 냈다. 남는 설명은 **TTS ↔ 실제 마이크 간극**이다.
- **녹음이 정당화됐고, 해 봤고, 🚨 실패했다** (2026-09-09). 되돌렸다.
  1. ✅ 녹음 도우미 신설 → `scripts/record_wakeword.py`. 기존 절차(`sd.rec(150초)` 한 줄)는
     **쓸 만한 녹음인지 학습을 돌려 봐야 알 수 있었다.** 학습기와 같은 규칙으로
     즉시 판정한다 — 학습기는 RMS > 0.006인 **모든** 구간을 호출어로 배우므로
     기침·잡음이 섞이면 그게 그대로 오탐이 된다
  2. ✅ 사용자 녹음 — 143초 · 발화 33번 · 소리 비율 18% (판정 통과). **파일은 남아 있다**
  3. 🚨 **재학습한 모델이 아무 말에나 깨어났다.** 실기에서 즉시 드러났다:

     | | 점수받은 창 | WAKE | WAKE 비율 |
     |---|---|---|---|
     | 기존 모델 (15:38) | 47 | 2 | **4.3%** |
     | 재학습본 (16:16) | 15 | 13 | **86.7%** ← 대부분 `prob=1.000` |

  4. ↩️ **되돌렸다** — `git checkout services/wakeword_model.npz`. 2026-09-08 학습본
     (2/10 · 오탐 0)이 다시 설치돼 있다. **어제보다 나빠진 것은 없다**

- 🚨 **원인이 좁혀졌다 — «TTS 간극»이 아니라 «음성 표본에 실제 마이크가 없다»**

  ```
  실제 마이크 · 양성 : 1,722개   ← 사용자 녹음
  실제 마이크 · 음성 :     0개   ← ★ 여기가 비어 있었다
  TTS 합성음        : 양성 2,100 · 음성 3,822
  ```

  학습셋에서 «진짜 마이크 오디오»가 **전부 양성**이면 모델이 단어를 배울 이유가 없다.
  **«합성음이냐 실제 마이크냐»만 구분해도 만점**이라 그 지름길로 간다.
  마이크에 대고 무슨 말을 하든 `1.000`이 나온 게 이 모양이다.

- 🚨 **검증셋이 이 결함을 볼 수 없는 눈이었다** — 이번에 가장 비싸게 배운 것이다.
  재학습본의 검증 성적은 **감지 93.6% · 오탐 2.35%로 멀쩡했다.** 검증셋의 음성
  표본도 **전부 TTS**라 지름길이 거기서도 통했기 때문이다.
  **학습셋과 같은 편향을 공유하는 검증셋은 아무것도 보증하지 않는다.**
  → 이 저장소가 반복해서 고쳐 온 «확인하지 않고 됐다고 말하는 것»의 학습판이다.
  경고는 `scripts/train_wakeword.py`의 `_balance_negatives` 위에 박아 뒀다.

- **다음 조치 — 같은 마이크로 «플루이즈가 아닌 말»을 녹음해 음성에 넣는다**
  - 2~3분, 평소 말투로 아무 말이나(책 읽기·잡담). **«플루이즈»만 안 나오면 된다**
  - `record_wakeword.py`에 `--negative` 옵션을 붙이면 된다(미구현)
  - ⚠️ 클래스 개수 균형(`_balance_negatives`)은 넣어 뒀지만 **그것만으로는 안 된다** —
    개수를 맞출 뿐 지름길은 그대로다. 2026-09-09에 그걸 실측으로 확인했다
- ⏸️ **여러 사람 녹음은 미룸**(2026-09-09 사용자: 여건이 안 됨). ADR §7의
  «한 사람 과적합»은 **아직 열려 있다.** `--append`로 나중에 더할 수 있다
- 🚨 **오탐도 있다 (2026-09-09 저녁 실측).** 되돌린 09-08 모델(2/10)이 사용자가
  **웨이크워드가 아닌 말**을 하는 동안 `0.850~0.989`로 **여러 번 깨어났다**
  (17:13:52 · 17:14:36 · 17:14:41 · 17:15:18 · 17:15:46).
  «오탐 0»은 09-08 조용한 방 기준이었고, **사람이 말하는 중에는 성립하지 않는다.**
  그리고 이 오탐이 BL-25의 잠재 결함을 드러냈다 — 자동 녹음이 겹쳐 STT가 죽었다.
  ⚠️ 이 모델은 **양방향으로 불안정하다**: 부를 땐 2/10, 안 부를 땐 깨어난다.
- 📏 **상시 문턱 근처다 (2026-09-09 17:24~17:26 실측).** 평범한 명령을 말하는 동안
  확률이 계속 올라간다 — `0.318` · `0.537(놓침)` · `0.124` · `0.498(놓침)`.
  **오탐이 «가끔»이 아니라 늘 아슬아슬한 상태**이고, 그래서 임계값을 올리는 것만으로는
  감지율(이미 2/10)을 더 깎을 뿐이다.
- 🔍 **가설: 자기 목소리(TTS)가 깨운다.** 미확정이지만 정황이 맞는다.
  ```
  17:25:39  턴 완료 | 응답 62자      ← TTS가 읽기 시작
  17:25:44  prob=1.000 → WAKE       ← 5초 뒤, 재생이 끝날 무렵
  ```
  62자를 읽으면 대략 5초다. 음성 비서에서 흔한 문제(자기 발화 barge-in)다.
  **확인 방법은 싸다** — 볼륨을 끄거나 이어폰을 꽂고 몇 턴 돌려 턴 직후의 WAKE가
  사라지는지 본다. 맞으면 «TTS 재생 중에는 웨이크워드를 쉬게 한다»가 답이고,
  그건 임계값과 무관하게 고칠 수 있다.
  ⚠️ **다른 WAKE 하나(17:24:47 `0.877`)는 TTS 없이 났다** — 원인이 하나가 아닐 수 있다.
- **되돌리기**: 새 모델이 더 나쁘면 `git checkout services/wakeword_model.npz`
  (2026-09-08 학습본 = 2/10 · 오탐 0). 그것도 아니면 `.env`에 `WAKEWORD_BACKEND=whisper`.
  ⚠️ 다만 사용자가 *"아까보단 낫다"* 고 했으므로 **Whisper로 되돌리는 게 개선은 아니다.**
- **상태**: **미해결.** 측정 끝(2/10) · 녹음 끝 · **재학습 실패 후 되돌림**.
  다음은 «실제 마이크 음성 표본»이고, 그건 녹음이 한 번 더 필요하다
  → [ADR §7](design/M2_웨이크워드_전용모델.md)

### ✅ BL-25 — 음성 녹음이 **레코더 두 개**를 만들어 STT가 통째로 죽는다 — 2026-09-09 해결
- **증상(실측)**: 음성 인식이 **첫 번째만 되고 그 뒤로는 전부 실패**했다.
  사용자: *"내가 다시 마이크 눌러서 얘기해도 인식 못하고… 기존 마이크 입력 스트림
  시간보다 더 적어지고."*
  ```
  17:13:36   74,045 bytes → ✅ 성공
  17:13:56  128,327 → 빈 결과     17:14:07  131,849 → 빈 결과
  17:14:39  131,255 → 빈 결과     17:14:44  134,274 → 빈 결과
  ```
  **크기가 ~130KB로 균일한 것이 단서였다** — 말이 든 녹음은 길이가 제각각이다.
- **원인**: `startMic()`의 경쟁 조건. `isRec = true`가 **`await getUserMedia` 뒤**에
  있어서, 기다리는 동안 `isRec`가 `false`다. 그 사이 `startMic()`이 또 불리면
  **가드를 통과해 레코더가 둘** 생긴다. 전역 `mr`은 두 번째로 덮이고,
  **참조를 잃은 첫 번째가 같은 `chunks` 배열에 계속 쓴다** →
  **webm 스트림 두 개가 섞인 블롭** → 디코딩 실패 → STT 빈 결과.
- **방아쇠**: 웨이크워드가 **5초 간격으로 두 번**(17:14:36 · 17:14:41) 떠서
  `activate(true)`가 두 번 돌았다. 사용자가 말한 *"아무것도 안 했는데 혼자 마이크
  활성화"* 가 이것이다(WAKE → 자동 녹음 시작은 설계된 동작이다).
- **해결**: `micStarting` 플래그를 **await 전에 동기로** 세운다. 그리고 콜백이
  전역 `mr`이 아니라 **자기 자신(`rec`)** 을 보게 해, 전역이 바뀌어도 남의
  `chunks`에 쓰지 않는다. `stopMic`도 `isRec` 대신 **레코더 상태**를 본다.
- **왜 오래 안 보였나**: 웨이크워드가 잘 안 깨어나던 동안에는 `startMic`이 겹칠
  일이 거의 없었다. **BL-23(오탐)이 이 잠재 결함을 드러낸 것**이다.
- 🚨 **진단이 6시간 늦어진 이유**: `services/stt.py`가 `print`만 하고 `log`를 안 써서
  `logs/pluiz.log`에 STT 줄이 **한 줄도 없었다.** 처음 증상을 들었을 때 나는
  «웨이크워드 간섭이 아니다」라고 **틀린 결론**을 냈다(당시 로그엔 WAKE가 없었지만
  그건 시점 문제였다). 로깅을 심자 **한 번에 갈렸다.**
  → 콘솔 `print`는 진단 수단이 아니다. 그 창은 대개 닫혀 있다.

### ✅ BL-26 — **«뻥카»** — 안 한 걸 했다고 말했다. 결함 두 개가 겹쳤다 — 2026-09-09 해결
- **증상(실기)**: *"블루투스 어디서 켜는지 알려줘"* 에 설정을 **열지 않고** 고리를
  화면 아무 데나 그렸고, 이어서 *"설정 그거부터 열어야 되는 거 아니니"* 에
  **«✓ 설정 창을 앞으로 가져왔습니다»** 라고 답했다. **설정은 뜨지 않았다.**
  사용자: *"설정창 띄워주지도 않고 거짓말도 하네. 심각한데?"* — 맞는 말이다.
- **원인 ① — `open_app`이 확인 없이 성공을 보고했다** (`tools/app_control.py`)
  ```python
  subprocess.Popen(_UWP_SHELL_COMMANDS[app_key], shell=True)
  time.sleep(0.5)
  return f"✓ {name} 창을 앞으로 가져왔습니다."   # ← 창이 떴는지 안 본다
  ```
  두 가지가 겹쳤다. **㉠ `_is_running`이 True인데 창이 없었다** — Windows가
  `SystemSettings.exe`를 **창 없이 살려 둔다.** «프로세스가 있다»와 «창이 보인다»는
  다른 사실이고 사용자가 원한 건 뒤쪽이다. **㉡ UWP 앱은 0.5초로는 안 뜬다.**
  그리고 문구도 틀렸다 — 포커스는 이미 실패했으므로 «앞으로 가져온》 게 아니다.
- **원인 ② — 불평이 명령으로 실행됐다** (`core/command_cache.py`)
  *"아니 설정 창은 애초에 실행 중이지 않았는데 뭘 가져와 너 왜 나한테 뻥카 치냐"* 가
  `설정 열어줘`에 **Stage-1 intent로 매칭**돼 `open_app`이 **실제로 실행됐다.**
  「설정」+「실행」이 문장 **어디에든** 있으면 intent가 잡히는데, 기존 어미 검사는
  명령형으로 끝나는 어절이 없어 못 걸렀다.
- **해결**
  - `_await_window()` + `_launched_or_honest()` — 실행 **뒤** 창을 확인하고 본 대로
    답한다. 안 뜨면 *"실행을 시도했지만 창이 나타나지 않았습니다"*. 확인 없이
    성공을 보고하던 **네 자리**를 전부 고쳤다
  - 캐시에 **커버율 가드** — Stage-1 히트에서 커버 안 된 실질 어절이 3개 이상이면
    «명령어를 스쳐 지나가는 긴 문장»으로 보고 LLM에 넘긴다.
    ⚠️ 어미 목록을 늘리는 땜질은 하지 않았다(그 원칙이 코드 주석에 있다)
- **임계는 실측으로 정했다**: 캐시 패턴 **44개 중 막히는 것 0개**, 사고 문장은
  각각 **5개·9개**가 남는다. 여유가 크다. 오판 방향도 안전하다 —
  잘못 막아도 **LLM이 문장 전체를 읽으므로** 느려질 뿐 틀리지 않는다.
- **검증**: `test_bl15_truncation.py` **43 → 53**. 전체 mock **33파일 956개**.
  ⚠️ **실기 미검증** — 설정이 실제로 뜨는지는 사람이 봐야 한다.
- 🚨 **원인 ③ — 진짜 뿌리는 «유령 창»이었다** (2026-09-09 2차, 실측)
  1차 수정 뒤에도 증상이 그대로였다. 재 보니 설정 창이 **존재하는데 안 보이는**
  상태였다:
  ```
  IsWindowVisible : True        ← «보인다»고 나온다
  IsIconic        : False       ← 최소화도 아니다
  GetWindowRect   : 1536x912    ← 크기까지 정상
  제목            : '설정'
  DWMWA_CLOAKED   : 2           ← 실제로는 **DWM이 가려 둔 창**
  ```
  Windows는 **정지된 UWP 앱의 창을 닫지 않고 cloak** 한다. `IsWindowVisible`만
  보던 코드가 이 유령을 **있는 창으로 셌고**, 그래서 `_focus_window`가 «성공»하고
  `open_app`이 «앞으로 가져왔습니다»라고 답했다. **1차 수정(창 확인)도 이 유령을
  «창이 떴다»로 셌다** — 검증을 넣어도 세는 기준이 틀리면 소용이 없다.
  → `is_window_cloaked()` 신설. `find_hwnd_for_app`의 **프로세스 경로와 제목 폴백
    둘 다**에서 거른다(폴백이 유령 `ApplicationFrameWindow`를 집고 있었다).
  → 실측 확인: `find_hwnd_for_app('설정')` **264282 → 0**, `_focus_window` **True → False**,
    그리고 `open_app("설정")`이 **실제로 열고** *"✓ 설정을 열었습니다"* 라고 답한다.
- **검증(2차)**: `tests/test_window_truth.py` **13건 신설**. ⚠️ 이 스위트는
  **Windows API를 실제로 부른다** — 유령 창은 mock으로 만들 수 없고, 만들 수
  있었다면 애초에 이 결함도 없었다. 전체 mock **34파일 969개**.
- **교훈**: 두 결함 다 이 저장소가 반복해서 고쳐 온 **«확인하지 않고 됐다고 말하는
  것»**(BL-12·BL-19·BL-21·BL-23)이다. 이번엔 **둘이 겹쳐서** 사용자에게는
  «대놓고 거짓말»로 보였다. 그리고 **1차 수정이 실패한 이유가 더 중요하다** —
  «확인한다»를 넣었는데 **확인의 기준(가시성)이 틀려서** 그대로 통과했다.

### ✅ BL-24 — 위험 호출이 **둘**이면 질문은 **하나**만 나간다 — 2026-09-10 해결
- **증상(잠재)**: 한 배치에 `[delete_file(a.txt), delete_file(b.txt)]`이 오면
  `hitl`은 **첫 번째에 대해서만** 묻는다(`dcall = next(...)`, `core/graph.py`).
  승인하면 `ToolNode`가 마지막 `AIMessage`의 tool_calls를 **전부** 실행하므로
  **b.txt는 사용자가 승인한 적 없이 삭제된다.**
- **성격**: [즉시/위험] — BL-20과 같은 계열이다. BL-20이 *"거부가 너무 넓다"* 였다면
  이건 *"승인이 너무 넓다"* 이고, **이쪽이 더 위험하다**(되돌릴 수 없는 동작이 늘어난다).
- **발견 시점**: 2026-09-09, [BL-20 ADR](design/BL-20_거부후_안전호출.md) §7을 쓰다가.
  **실기에서 관측된 적은 없다** — BL-20과 같은 전제(위험 호출 2개 배치)가 필요하다.
- **해결 후보**: BL-20이 만든 **재발행 기계를 그대로 쓴다** — 승인된 호출만
  `reissue_message()`로 다시 내보내면 나머지는 **구조적으로 실행될 수 없다**.
  질문을 위험 호출 수만큼 반복할지(승인 n회)는 별도 판단이 필요하다.
- **해결**: [design/BL-24_다중위험호출_승인.md](design/BL-24_다중위험호출_승인.md).
  **질문이 위험 호출 «전부»를 이름으로 부르고, 승인 시 «물어본 것만» 재발행해 실행한다.**
  - 질문: `'a.txt' 파일과 'b.txt' 파일 2개를 정말 삭제할까요?`
    ⚠️ 1개일 때는 **기존 문구와 글자 그대로 같다**(조사까지). 테스트로 고정했다
    ⚠️ 3개 이상은 «외 N개»로 줄이되 **개수는 반드시 남긴다** —
      목록이 길면 사람은 안 읽고, 개수가 «묻지 않은 삭제»를 알아챌 유일한 단서다
  - 🔒 승인 분기가 **messages를 안 건드리던 것**이 결함의 절반이었다.
    이제 원본을 전부 마감하고 **물어본 것만 새 id로 재발행**한다.
    **마지막 `AIMessage`에 없으면 실행될 수 없다** — 조건문이 아니라 구조다
    (BL-20의 «조건문이 아니라 메시지 모양이 막는다»와 같은 자리)
- **덤으로 고쳐진 것**: `target_exists`가 **첫 호출 하나만** 보고 있었다.
  그래서 같은 상황인데 **배치 순서에 따라 결과가 달라졌다** —
  `[없는것, 있는것]`이면 묻지도 않고 끝났고, `[있는것, 없는것]`이면 묻고 둘 다 실행됐다.
  이제 **각각** 확인해 있는 것만 묻고 있는 것만 실행한다
- 🚨 **구현 중에 한 번 틀렸다.** «없어서 못 지운 것»을 재발행 **뒤에**
  `AIMessage`로 붙였더니 그게 마지막 `AIMessage`가 돼서 **ToolNode가 도구를
  못 봤다 — 승인했는데 아무것도 실행되지 않았다.** BL-20 §3-4가 «거부 사실은
  문구가 아니라 플래그로»라고 한 것과 **같은 이유**다.
  `missing_targets` 플래그 + `missing_notice()` 접미로 바꿨다
- **검증**: `test_hitl_graph.py` **88 → 114**. 전체 mock **36파일 1071개**(1045 → 1071).
  ⚠️ **라이브 미검증** — 위험 호출 2개가 한 배치에 담기는 일이 실기에서 드물다.
  mock이 그 배치를 강제로 만들어 검증한다(`TwoDeleteLLM`)
- **안 한 것**: **부분 승인**(a는 지우고 b는 두기). 승인 피로가 승인 절차 자체를
  무력화한다 → [ADR §4-A](design/BL-24_다중위험호출_승인.md).
  사용자는 말로 다시 명령하면 되고, 그건 `other_command` 경로가 이미 받는다.

### ✅ BL-17 — **재질문 뒤 승인 대기를 놓친다** — 2026-09-10 **실기 재현·해결**

- **6일간 «재현 시도» 상태였다.** 2026-09-10 실기에서 **두 번** 재현됐다:
  ```
  승인 판정 | 1번째 | 답변='가니' → unclear
  승인 대기 | 입력='가니'              ← 재질문은 정상으로 걸렸다
  승인 대기 확인 | next=없음           ← 🚨 여기서 «대기 아님»으로 읽었다
  ```
  그래서 사용자의 `'그래'`가 **승인이 아니라 새 명령**이 됐다. 한 번은 승인 대기가
  통째로 사라져 *"네? 어떤 작업을…"* 으로 끝났고, 한 번은 질문이 처음부터 다시 떠서
  **같은 삭제를 두 번 승인**해야 했다.
- **원인은 그래프가 아니라 «판정 신호»였다.** `interrupt()`가 **같은 노드 안에서
  두 번째로** 걸리면(=재질문) 그 노드는 «다음에 실행할 노드»가 아니라
  **«실행 중간에 멈춘 노드»** 다. `next`는 비고 `tasks[].interrupts`에만 남는다:

  | 신호 | 1차 질문 | 재질문 |
  |---|---|---|
  | `next` | `('hitl',)` | **없음** 🚨 |
  | `tasks[].interrupts` | 1 | **1** |

- **해결**: `_pending_interrupt()`가 둘 다 본다. 로그에 «대기 중인 질문» 개수도 남긴다.
- 🚨 **왜 mock 114건이 전부 초록이었나** — mock은 `Command(resume=)`를 **그래프에
  직접** 준다. 실기는 그 전에 이 판정을 거치는데, **테스트가 그 자리를 통째로
  건너뛰고 있었다.** 그래서 회귀는 `_pending_interrupt()`를 **직접 부른다**
  (복사본을 만들면 또 어긋난다 — BL-29에서 배운 것과 같은 모양).
- **검증**: `test_hitl_graph.py` **114 → 120**. 로그에 `승인 판정 | 2번째`가
  이제 찍힌다 — 실기에 **없던 줄**이다.
- **성격**: [즉시/위험]. (2026-09-03 관찰 → 2026-09-10 재현·해결)

### ✅ BL-30 — 같은 동작을 **여러 대상에 하면 승인이 두 번** 뜬다 — 2026-09-10 해결

- **증상(실기)**: *"a.txt, b.txt 지워줘"* → 계획이 2단계로 쪼개
  `'a.txt' 삭제할까요?` → 승인 → `'b.txt' 삭제할까요?` → 승인. **두 번 물었다.**
  사용자: *"두 파일 지워달라고 하면 꼭 뒤 파일과 관련해서는 실행이 안 되는 것 같아"*
- **더 나쁜 경우** — 2단계가 **위험 도구**면 LLM이 그 단계에서 도구를 아예 안 불렀다:
  ```
  [Plan] 1/2 '메모장 최소화'  →  2/2 'b.txt 파일 지우기'
  턴 완료 | 도구=['minimize_window']        ← 삭제를 안 불렀다
  ```
  M3-1이 *"못 했어요"* 라고 **정직하게 보고**했지만(그 층은 제대로 동작했다),
  사용자는 **두 번 말해야** 했다.
- **해결**: `parse_plan`이 «같은 동작 · 다른 대상»이면 **계획을 버린다**.
  그러면 LLM이 한 배치에 담고 **[BL-24](#)가 이미 만든 질문**이 그대로 받는다 —
  *"'a.txt' 파일과 'b.txt' 파일 2개를 정말 삭제할까요?"* **새 기계가 필요 없다.**
- ⚠️ **범위를 좁혔다 — `_BATCHABLE_ACTIONS = {"삭제"}`.**
  처음엔 «같은 동작이면 무엇이든» 묶었는데, 그러면 `'메모장 열기' / '계산기 열기'`
  까지 한 단계가 된다. 열기는 **쪼개도 피해가 없고**(승인이 없다) 쪼개야 M3-1이
  *"계산기는 못 했어요"* 를 말할 수 있다 — **그게 M3의 존재 이유다.**
  실제 피해(승인 두 번 · 위험 단계 미실행)는 **승인이 붙는 동작에서만** 난다.
- ⚠️ **부분 승인(«a만 빼고 지워»)은 넣지 않았다** — [BL-24 ADR §4](design/BL-24_다중위험호출_승인.md)의
  *"승인 피로가 승인 절차 자체를 무력화한다"* 를 유지한다(2026-09-10 사용자 확인).
  말로 다시 명령하면 되고, 그 경로는 `other_command`가 이미 받는다.
- **검증**: `test_plan_node.py` **37 → 52**. 끝단(질문 1회 · 둘 다 삭제 ·
  거부하면 둘 다 유지)까지 그래프로 돌린다.
- **성격**: [즉시/위험]. (2026-09-10 실기 발견·해결)

### ✅ BL-29 — 도구의 **실패 표시를 읽는 쪽 셋이 서로 다르게 읽는다** — 2026-09-10 해결

- **어디서 나왔나**: 「에러 핸들링 표준화」(TASKS ②)를 하려고 실태를 쟀더니
  **스타일 문제가 아니었다.** 도구가 반환하는 문자열의 접두사는 장식이 아니라
  **코드 세 곳이 파싱하는 계약**이다 — 캐시 학습 금지 · T04 거짓성공 차단 ·
  시각검증 스킵. 그 셋의 규칙이 서로 달랐다.
- **증상 ① 🚨 거짓 성공이 사용자에게 나간다.** `✗`는 도구가 가장 많이 쓰는 실패
  표시(52곳)인데 **T04(`verify_output`)가 `✗`를 안 봤다.** 도구가
  *"✗ '메모장' 앱을 찾을 수 없습니다"* 를 반환해도 LLM이 *"메모장 열었어요!"* 라고
  답하면 **그대로 나갔다.** BL-12·BL-19·BL-26과 같은 계열이다.
- **증상 ② 🚨 실패한 턴이 캐시에 학습된다.** 11곳이 A·B·C **전부**를 빠져나갔다.
  `web.py`의 넷은 `✗`(U+2717)가 아니라 **알파벳 `x`** 였다 — 눈으로는 구별되지 않는다.
  BL-27·BL-21과 같은 계열: 틀린 것이 캐시에 굳으면 **고쳐질 기회가 없다.**
- **해결**: 판정을 `core/tool_result.py` **하나**로 모으고(`tool_failed()`),
  읽는 쪽 셋이 그걸 부른다. 쓰는 쪽은 어긋난 16곳을 `✗`로 정리했다.
  → ADR [design/BL-29_도구_결과_계약.md](design/BL-29_도구_결과_계약.md)
- **🔒 핵심은 «내일의 구멍»을 막은 것**: `tests/test_tool_result.py`가
  **도구 소스를 전수 스캔**한다. 금지 마커(ASCII `x`)를 쓰거나, 실패처럼 보이는데
  마커가 없으면 **거기서 깨진다.** 쓰는 쪽 135곳을 오늘 통일해도 내일 새 도구가
  어긋나면 같은 일이 다시 난다 — `web.py`가 정확히 그렇게 됐고 **누구도 틀리려고
  하지 않았다.** 규율은 확률을 올릴 뿐이고 **보장하는 건 구조다**(BL-19의 교훈과 같은 모양).
  - ⚠️ 일부러 미끼(`x 검색 실패` · 마커 없는 실패)를 심어 **정말 잡는지 확인**했다.
    통과하는 것만 보면 검증기가 눈을 감고 있어도 모른다.
- **덤**: `test_sprint1_2.py`가 **정규식을 복사해서** 검사하고 있었다 — 복사본이
  통과해도 진짜가 틀릴 수 있는 바로 그 구조다. 이제 진짜 함수를 부른다.
  같은 파일의 `@tool` 개수 검사도 3에 고정돼 낡아 있었다(`click_ui_element` 추가 이후).
- **검증**: `tests/test_tool_result.py` **35건** 신설 · 전체 mock **37파일 1139개** · 실패 0
- **성격**: [즉시/위험]. (2026-09-10 발견·해결)

### ✅ BL-28 — 도구를 안 부른 턴의 **사유가 로그에 없다** — 2026-09-10 해결

- **증상(실측)**: [도구 사용 실측](research/2026-09_도구사용_실측.md) §2.
  실사용 76턴 중 도구 0개가 21턴이고, 그중 **13턴이 «LLM이 말만 한» 턴**이다.
  명령처럼 보이는 후보 7턴이 나왔는데 **무엇이 진짜 실패인지 확정할 수 없다** —
  `턴 완료` 줄이 응답을 **«응답 13자»** 로만 남기기 때문이다.
  ```
  09-07 13:23:45  '메모장 닫아줘'          → 도구=없음   ← 못 한 건가? 이미 닫혀 있던 건가?
  09-07 13:26:03  '메모장 열고 testing.txt는 지워줘'  → 도구=없음
  ```
- **원인**: 로그가 **결과의 «양»만** 남기고 **«종류»를 안 남긴다.**
- **성격**: [TODO/품질] — 동작 결함이 아니라 **관측 불가**다. 다만 이걸 못 고치면
  3주차 항목 «도구 사용 실측 ②»에 **영원히 답할 수 없고**, 11월 지표도 반쪽이 된다.
- **해결 후보**: 도구 미호출로 끝난 턴에 **분류 한 단어**를 붙인다 —
  `사유=잡담` · `사유=거절` · `사유=이미충족` · `사유=못함`.
  ⚠️ **응답 전문을 남기자는 게 아니다.** 그건 개인정보가 로그로 새는 길이고
  [보안 방향](ARCHITECTURE.md)과 반대다. 한 단어면 된다.
- **곁들여**: **캐시 실행도 어떤 도구를 돌렸는지 남긴다**(`캐시 → open_app`).
  지금은 캐시 히트가 `도구=없음`으로 찍혀, 캐시가 처리한 도구들이
  «한 번도 안 쓰인 27개»에 **잘못 들어간다**(실측 §1).
- **해결**: `_reason_note()` 신설 — 도구를 하나도 안 부른 턴에 **한 단어**를 붙인다.
  `사유=캐시` · `차단` · `승인거부` · **`못함`** · `잡담`. «못함»만 세면 ②에 **바로 답이 나온다**.
  - ⚠️ **응답 전문은 안 남긴다.** 분류는 프로세스 안에서 하고 로그에는 한 단어만
    나간다 — 개인정보가 로그로 새면 안 된다.
  - ⚠️ **도구가 하나라도 돌았으면 안 붙인다.** 그 턴은 «안 부른 턴»이 아니다.
  - «캐시» 판정이 문구 판정보다 **먼저**다. 캐시 응답에 «없습니다»가 들어가도 그건 처리된 턴이지 실패가 아니다.
- **곁들여**: `fast_path`가 `[캐시 실행] 패턴=… | 도구=[…]` 를 남긴다.
  캐시 히트는 `도구=없음`으로 찍혀 **볼륨·밝기·스크린샷·시간·배터리가 «한 번도 안 쓰인 도구»로 집계되고 있었다** — 실제로는 캐시가 돌리고 있었는데도.
  분석기가 이 줄로 되찾아 합친다.
- **검증**: `test_graph_agent.py` **17 → 30** · `test_log_format.py` **20건 신설** · 전체 mock **36파일 1045개**(1012 → 1045).
  합성 로그로 **생산자 → 파서 → 리포트**를 종단 확인했다.
- ⚠️ **라이브 미검증** — 실제 서버가 찍은 줄로는 아직 안 봤다. 다음 실사용 세션의 로그에 `사유=`가 보이면 확인된다.

### ✅ BL-27 — 캐시가 **「그래」를 명령으로 학습**했다 · 부정문(`말고`)을 못 본다 — 2026-09-10 해결

- **증상(실측)**: 2026-09-10, 사용자의 실제 `cache/command_cache.json`(44개 · 동적 7개)
  사본으로 잰 결과다. **원본은 건드리지 않았다.**
  ```
  '그래'                    → close_app(계산기)  1.00 [S2-sim]  «그래»
  '그래요'                  → close_app(계산기)  0.80 [S2-sim]  «그래»
  '메모장 말고 계산기 열어줘'  → open_app(메모장)   0.90 [S1-intent]
  ```
  「그래」는 **승인 응답**이다(2026-09-09 18:55:56 로그에 승인 자리에서 쓴 줄이 있다).
  승인 대기가 아닐 때 「그래」라고 하면 **계산기가 꺼진다.** 캐시는 HITL을 거치지 않고
  도구를 **바로** 실행하므로 승인 질문도 뜨지 않는다.
- **같은 파일에 있는 다른 쓰레기**: `'오시가 된거야 다시' → get_current_time`(STT 오인식),
  `'계산기 말고 메모장 열어줘' → open_app`.
- **원인 ①(학습)**: `_is_learnable()`은 **도구가 화이트리스트인지**만 본다.
  **발화가 명령의 꼴인지는 보지 않는다.** 그래서 승인 응답·잡담·STT 오인식이
  그대로 «패턴»이 되고, 한 번 박히면 `hit_count`만 늘며 **영구화된다.**
- **원인 ②(부정문)**: Stage 1이 문장에서 **처음 걸린 entity 하나**만 집는다.
  `has_uncovered_command()`도 못 잡는다 — 남는 어절이 「말고」뿐이라 명령의 꼴이 아니다.
- **성격**: [즉시/위험] — BL-21과 같은 계열(**결함이 캐시에 박혀 영구화된다**)이고,
  이쪽은 **되돌릴 수 없는 동작은 아니지만 사용자가 시킨 적 없는 실행**이다.
- **발견 시점**: 2026-09-10, [M5 임베딩 캐시 ADR](design/M5_임베딩_캐시.md) §2를 쓰려고
  현행 매칭의 분모를 재다가. **실기에서 사고로 관측된 적은 없다** — 「그래」를
  승인 대기가 **아닌** 자리에서 말해야 터진다.
- **해결 후보(초안 — ①은 아래에서 실측으로 정정됐다)**:
  ① 학습 필터 L1~L3(~~intent 없는 발화~~·~~1어절~~·부정 토큰은 학습 금지)
  ② 부정·대조 게이트를 Stage 1·2 **양쪽**에
  ③ 이미 박힌 7개는 **선별 제거**가 필요하다 — `clear_dynamic()`은 전부 지운다
- **해결**: [M5 ADR §3-3 · §3-4 · §6-1](design/M5_임베딩_캐시.md). **임베딩보다 먼저** 했다 — 색인이 더러우면 어떤 자를 써도 샌다.
  - `is_learnable_utterance()` 신설 — **발화** 쪽 자격 L1·L2·L3. `_is_learnable()`(도구 쪽)과 짝이다. 거절 시 **어느 필터인지** 로그에 남는다
  - `has_contrast_marker()` + `find()` 맨 앞의 **Stage 0 게이트** — 한 곳에서 막아 Stage 1·2와 모든 호출자에 같은 판정이 간다
  - `prune_unlearnable_dynamic()` — **선별** 제거. `clear_dynamic()`은 전부 지워 쓸 만한 것까지 날린다
- 🚨 **필터의 초안이 틀렸다는 걸 구현 중에 쟀다.** ADR이 쓴 «intent(entity **와** action) 추출 실패 시 거절»은 **시드 37개 중 9개, 정상 표현 12개 중 11개**를 막았다. «둘 중 하나»로 고쳤다. «어절 2개 이상»도 시드 `'음소거해줘'`(1어절)에 걸려 **글자 3개 이상**으로 고쳤다 → [ADR §3-3 정정 상자](design/M5_임베딩_캐시.md)
- **마이그레이션**: `cache/command_cache.backup-20260910.json` 백업 후 선별 제거. 엔트리 **44 → 40**, 동적 **7 → 3**, 시드 37 유지. 제거: `'그래'`(L2) · `'오시가 된거야 다시'`(L1) · `'오늘 날짜 알려줘'`(L1) · `'계산기 말고 메모장 열어줘'`(L3)
- **검증**: `test_cache_learn.py` **15 → 38**. 전체 mock **34파일 992개** 통과. 시드 37개 전수 확인 — **죽은 시드 0개**. 파일을 다시 읽어 `'그래'`·`'그래요'` → **LLM** 확인
- **남은 한계**: L1은 캐시 어휘에 없는 **정상 표현의 학습도 막는다**(`'충전 얼마나 남았어'`·`'오늘 날짜 알려줘'`). **미스는 LLM이 2.5초에 처리하지만 오매칭은 엉뚱한 실행**이라 정밀도를 위에 둔 결과다. 임베딩이 들어오면 **매칭**은 사전에서 풀려나지만 **학습**은 여전히 사전에 묶여 있다 → M5 §6-3 뒤에 다시 본다

### ✅ BL-20 — 위험 도구 하나를 거부하면 **같은 배치의 안전한 호출까지** 조용히 취소됐다 — 2026-09-09 해결
- **증상**: LLM이 한 `AIMessage`에 `[open_app, delete_file]`을 담으면, 삭제를 거부했을 때
  **메모장도 열리지 않았다.** 응답은 *"네, 삭제를 취소했어요."* 뿐이라
  **앱이 안 열렸다는 사실은 아무도 말하지 않았다.**
- **원인**: `route_after_agent`가 **하나라도** 위험하면 배치 **전체**를 `hitl`로 보내고
  (이건 옳다), `hitl._close_calls`가 매달린 호출을 **전부** 같은 사유로 마감했다
  (이게 틀렸다 — **멈추는 것과 취소하는 것을 구분하지 않았다**).
- **성격**: [즉시/위험] — 조용한 실패. 이 프로젝트가 반복해서 데인
  *"안 한 걸 했다고 말하는 것"* 의 거울상(**안 된 걸 말하지 않는 것**)이다.
- 🚨 **백로그에 적혀 있던 해결 후보(«안전한 것은 마감하지 말고 tools로 보낸다»)는
  쓰지 않았다.** 설치본 `langgraph 1.2.4`의 `ToolNode._parse_input`이
  **뒤에서부터 마지막 `AIMessage`의 tool_calls를 전부** 실행하기 때문에,
  그대로 하면 **방금 거부당한 삭제까지 실행된다.**
- **해결**: 원본 호출은 **전부** 마감하고(짝 불변식), 안전한 호출만 **새 id로 재발행한
  `AIMessage`** 로 다시 내보낸다. 마지막 `AIMessage`에 안전한 호출만 남으므로
  거부된 삭제는 **구조적으로** 실행될 수 없다 — 조건문이 아니라 메시지 모양이 막는다.
  - `reject`·`not_found`만 재발행한다. `unclear`(안전 기본값)·`other_command`(새 턴)는
    하지 않는다 → [ADR §4](design/BL-20_거부후_안전호출.md)
  - 거부 사실은 문구가 아니라 `deletion_cancelled` 플래그로 전달한다(§3-4)
  - 재발행분은 `turn_tool_call_count()`가 **세지 않는다** — 안 그러면 진행 근거가
    부풀어 M3-1의 «승인 거부 → 못 한 단계 보고»가 조용해진다(§5)
- **설계**: [design/BL-20_거부후_안전호출.md](design/BL-20_거부후_안전호출.md)
- **검증**: `tests/test_hitl_graph.py` **59 → 88**. 전체 mock **32파일 876개** 통과.
  ⚠️ **라이브 미검증** — 오늘 실기에서 위험+안전 배치가 드물어 재현이 어렵다.
  mock이 그 배치를 강제로 만들어 검증한다(`MixedLLM`).
- **파생**: 이 설계를 쓰다가 **BL-24**를 발견했다(위 항목).

### BL-19 — 감시 도구를 **부르지도 않고** "지켜볼게요"라고 답한다 (부분 해결)
- **증상**: `"메모장 지켜보다가 오류 뜨면 알려줘"` → 도구 실행 **0건**인데
  *"메모장을 지켜보다가 오류 메시지가 뜨면 바로 알려드릴게요!"* 라고 답했다.
  `"그만 봐"` → 역시 0건인데 *"화면 감시를 중단했어요."*
  **아무도 화면을 보고 있지 않았다.** 사용자는 지켜보는 줄 알고 자리를 뜬다.
- **어떻게 드러났나**: 2026-09-04 Vision 라이브 테스트(V-05)를 만들면서.
  `logs/pluiz.log`의 `턴 완료 | … | 도구=없음 | 응답 32자` 한 줄로 갈렸다.
  **Phase 2의 마지막 기능이 코드상 "완료"인 채 실사용에서 한 번도 켜진 적이 없었다.**
- **왜 기존 방어가 못 잡았나**: `verify_output()`은 ① 응답이 **비었을 때**와
  ② 도구가 `[오류]`를 냈는데 성공처럼 답할 때만 본다. 이건 **응답이 비지 않았고
  도구가 아예 안 돌았다** — 둘 다 비켜간다. `_NOTHING_HAPPENED_MSG`가 막으려던
  거짓말의, 빈 응답이 아닌 판본이다.
- **원인 ①(고침)**: 시스템 프롬프트가 **금지문으로 시작**했다 —
  *"…할 때만 쓰세요. 스스로 판단해서 시작하지 마세요."* 도구 docstring에도 같은
  문장이 한 번 더 있어 **이중으로 눌렀다.** 모델이 안전한 쪽(=아무것도 안 하고
  말로만 답하기)으로 기울었다. → **해야 할 일을 먼저, 제약은 뒤로** 재배치.
- **원인 ②(못 고침 — 모델 쪽)**: 프롬프트를 고쳐도 **호출률이 시간대에 따라
  크게 흔들린다.** 2026-09-04 측정(temperature=0, 같은 코드):

  | 구간 | watch_screen 호출 |
  |---|---|
  | 좋은 구간 | 10/10 · 6/6 · 3/3 |
  | 중간 | 4/5 · 2/5 |
  | 나쁜 구간 | 0/10 · 0/4 |

  같은 구간에 `describe_screen`·`find_ui_element`는 **정상 동작**했고, 도구 개수를
  39개 → 3개로 줄여도, 시스템 프롬프트를 빼도 달라지지 않았다.
  나쁜 구간에서는 모델이 함수 호출 대신 **말로 풀어썼다**(`**Tool Call` 을 본문에
  텍스트로 출력한 적도 있다). 우리 쪽에서 통제할 수 없는 변동으로 본다.
- **해결 — 세 겹. 마지막 겹이 핵심이다**:
  1. **프롬프트 재배치** — 호출 지시가 먼저, 금지문은 괄호로 뒤에.
     `tools/vision.py` docstring의 중복 억제문도 정리.
  2. **1회 재시도** (`needs_watch_retry` → `agent` 노드) — 감시 요청인데 도구를
     안 불렀으면 지시를 덧붙여 **한 번만** 다시 묻는다. BL-04이 "미이관"이라고
     적어 둔 *도구 미호출 재시도*가 여기서 처음 들어왔다.
     **2026-09-07 — 이 겹을 설득에서 강제로 바꿨다.** 재시도 패스만
     `llm.bind_tools(tools, tool_choice="watch_screen")`(중단 요청이면 `stop_watching`)로
     묶는다. 나쁜 구간에서는 재시도까지 같이 실패했는데(0/10), 설득은 확률을 올릴 뿐이고
     강제는 구조다. ⚠️ **첫 패스는 건드리지 않는다** — 평범한 대화까지 도구를 부르게 된다.
     묶지 못하는 provider·mock이면 `None`이라 **오늘 경로(설득)로 폴백**하고,
     강제 호출이 예외를 던져도 같은 자리로 내려온다(**턴을 죽이지 않는다**).
     → `core/graph.py` `_forced_llm`, 회귀 `tests/test_screen_monitor.py` §13(10건)
  3. **정직성 그물** (`detect_watch_lie` → `output_guard`) — **말이 아니라 상태로**
     판정한다. ① 이번 턴 도구 0개 ② 모니터 꺼짐 ③ 사용자가 감시를 요청했다
     ④ 응답이 해줬다고 말한다 → 넷 다 맞으면 정직한 말로 교체한다.
     ①②가 결정적이고 정규식은 **범위만** 좁힌다(판정하지 않는다).
     ⚠️ `fast_hit` 턴은 제외한다 — `fast_path`는 도구를 실제로 실행하고도
     messages에는 AIMessage 하나만 남겨서(절대규칙 2) "도구 0개"로 보인다.
- **곁들여 드러난 것 — 고지가 요약돼 사라졌다**: 도구가 불린 뒤에도 LLM이
  고지를 *"알려드릴게요!"* 한 줄로 줄여 **간격·상한·중단법이 통째로 사라졌다.**
  감시를 **승인이 아니라 고지**로 하기로 한 근거가 그 고지다. 그래서
  `watch_notice_to_deliver()`가 도구가 만든 문장을 **원문 그대로** 내보낸다.
- **남은 것 (2026-09-07 갱신)**: `tool_choice` 강제가 들어갔지만 **라이브에서 확인하지
  않았다.** mock이 보증하는 것은 *배선*(강제가 걸리는가 · 무엇을 강제하는가 ·
  못 쓰면 폴백하는가)뿐이고, **나쁜 구간의 Gemini가 강제를 실제로 따르는지**는
  실기에서만 갈린다. BL-19 자체가 *"mock이 전부 초록인데 한 번도 켜진 적 없었다"* 는
  결함이었으므로 **여기서 닫지 않는다.** 확인 절차는 [TASKS.md](TASKS.md) ①.
  강제해도 안 켜지면 남는 수단은 **모델 교체**다. 그때까지 그물(겹 3)은 그대로 둔다 —
  실패하면 *"시작하지 못했어요"* 라고 말한다.
- **회귀 방지**: `tests/test_screen_monitor.py` — 프롬프트 배치 순서 · 그물 ·
  재시도 · 고지 원문 전달 (§11·§12). 라이브는 `test_regression.py` V-05가
  **말이 아니라 `/ws`의 `watch_state`로** 확인한다. (2026-09-04)

### BL-22 — 감시 프리필터가 **타이핑을 못 본다** (2026-09-07 해결, 잔여 한계 있음)
- **증상(실측)**: `"메모장 지켜보다가 글자 생기면 알려줘"` → 감시는 정상으로 돌았는데
  글자를 써도 알림이 오지 않았다. `13:20:07 감시 시작` … `13:22:28 감시 종료 |
  사유=stopped | **Vision 0회**` — 2분 21초 동안 프리필터가 한 번도 통과시키지 않았다.
- **BL-19과 다른 결함이다**: 도구는 **첫 패스에서 정상 호출**됐다
  (`도구=['watch_screen']`). 오늘 넣은 강제 재시도는 쓰이지도 않았다.
  "안 켜졌다"가 아니라 **켜졌는데 못 봤다**이다.
- **원인**: `_CHANGED_RATIO = 0.02`가 **대화상자 기준**으로 잡혀 있었다
  (주석에도 *"대화상자는 화면의 5% 이상"* 이라고 쓰여 있었다). 1000x700 창에
  실제로 그려 재본 값(32x32=1024셀):

  | 화면 변화 | 바뀐 셀 | 비율 | 2%(옛) | 1%(새) |
  |---|---|---|---|---|
  | 상태표시줄 시계 | 1 | 0.10% | 무시 | 무시 |
  | 커서 1개 깜빡임 | 2 | 0.20% | 무시 | 무시 |
  | **글자 한 줄(12자)** | 13 | 1.27% | **놓침** | **감지** |
  | 글자 두 줄 | 21 | 2.05% | 감지 | 감지 |
  | 대화상자 | 140 | 13.70% | 감지 | 감지 |

- **해결**: `_CHANGED_RATIO` **0.02 → 0.01**. 한 줄(1.27%)이 걸리고 커서(0.20%)와는
  **6배**가 벌어진다. 회귀는 `tests/test_screen_monitor.py` §4(실측 셀 수로 못 박음).
- **⚠️ 남은 한계**: **글자 1~2자(0.29%)는 여전히 못 잡는다.** 커서 깜빡임(0.20%)과
  구분되지 않아 구조적으로 불가능하다 — 더 낮추면 커서 때문에 Vision을 계속 부른다.
- **잔여 해결 (2026-09-08)** — **고지가 그 한계를 말한다.**
  *"다만 글자 한두 자처럼 아주 작은 변화는 놓칠 수 있어요."* 한 줄을 `_watch_notice`에
  넣었다. 감도를 더 올리는 쪽은 **버렸다** — 임계를 내리면 커서 깜빡임마다 화면이
  외부로 나간다(OWASP LLM02). **할 수 있는 것만 말하기로 한 것**이고,
  이 프로젝트의 *«안 한 걸 안 했다고 말한다»* 를 **사전에** 적용한 판본이다.
  고지가 다섯 가지를 말하게 됐다(③이 새로 들어왔다) →
  [ARCHITECTURE § 화면 변화 모니터링](ARCHITECTURE.md#화면-변화-모니터링--watch_screen-2026-09-03).
  회귀: `test_screen_monitor.py` **138 → 139**(§9에 "놓칠 수 있"을 못 박았다).
- **상태**: **해결** — 코드 한계는 그대로지만 **거짓 약속은 없어졌다.**

### ✅ BL-17 — 승인 재개가 한 번 안 먹은 것 — **2026-09-10 해결**
- 원인·해결은 이 문서 위쪽의 «BL-17 — 재질문 뒤 승인 대기를 놓친다» 항목에 있다.
  (같은 사실을 두 곳에 적으면 한쪽만 갱신돼 어긋난다)
- 한 줄 요약: **`next`만 보고 판정해서** 재질문 뒤의 대기를 놓쳤다.


### BL-15 — fast_path가 명령을 **조용히 절단**한다 (측정 지표 없음)
- **증상**: `core/fast_path.py`의 `is_compound_command()`는 손으로 쓴 정규식 목록이라
  여기를 빠져나가면 캐시가 **0.90 확신으로** 실행하고 LLM은 문장을 보지도 못한다.
  - 예: `"메모장 열어서 회의록 적어줘"` → `열어서`가 `_COMPOUND_CMD`에 없다
    → entity(메모장)+action(open) 히트 → **메모장만 열고 뒷문장은 사라진다**
  - 사용자는 "왜 안 적혔지?"만 겪고, 로그에는 `[S1-intent]` 성공으로 남는다
- **왜 문제인가**: BL-02(부정어 오매칭)·BL-12(type_text 거짓 성공)와 **같은 계열**이다.
  캐시가 틀렸다는 걸 아무도 모른다.
- **2026-09-03 — 해결됨.** 재기 전에 실기에서 먼저 터졌다:
  *"아니 그냥 새로 메모장 열어서 거기에 회의록이라고 써줘"* → `(notepad, open)` 0.90
  히트 → **메모장만 열리고 "회의록 써줘"가 통째로 증발.** "새로"(=새 탭)도 LLM이
  문장을 못 봐서 반영되지 않았다.
  - **해결**: `CommandCache.has_uncovered_command()` — 걸린 entity·action이 차지한
    어절을 지우고, **남은 말에 명령형 어미가 있으면 캐시를 포기**하고 LLM으로 보낸다.
    `열어서`를 정규식에 추가하는 땜질이 아니다 — 그러면 `띄워서`·`실행해서`에서 또 터진다.
  - **Stage 1(intent) 히트에만** 적용한다. Stage 2는 문장 전체 유사도라 뒷문장이
    붙으면 점수가 알아서 떨어진다. 이 구분을 빼면 `"음소거 해줘"`·`"스크린샷 좀 찍어줘"`
    같은 정상 명령이 무더기로 캐시를 못 탄다(만들면서 실제로 오탐 5건이 났다).
  - **파라미터를 바꾸는 수식어도 같이 막는다** — 캐시 엔트리는 도구 이름만 저장하고
    **파라미터를 저장하지 않아**(P4 오염 방지) `new=True`를 표현할 수 없다.
    `"새로 메모장 열어줘"`가 `open_app(app='메모장')`으로 히트해 **기존 창을 앞으로
    가져와 놓고 "새로 열었다"고 답했다.** 어절 완전일치로만 보므로 `"새로고침"`은 안 걸린다.
  - **빈도는 계속 잰다** — 포기할 때마다 `[BL-15]` 로그를 남긴다. 임베딩 캐시(10월)의
    "좋아졌다"를 숫자로 말할 근거가 여기서 쌓인다.
  - 검증: `tests/test_bl15_truncation.py` 37개(정상 유지 20 + 절단 차단 8 + 경계 9).
- **성격**: 정직성 + 관측성. (2026-09-02 코드 리뷰 중 발견, 2026-09-03 실기에서 재현)

### BL-11 — 라이브 테스트가 git 추적 파일(`cache/command_cache.json`)을 더럽힌다

> **2026-09-03 — 로그 쪽은 해결됐다.** mock 테스트가 `logs/pluiz.log`에 남기던 줄
> (`[BL-15] … 'x'`, `RuntimeError: API 키 없음(테스트)` 스택트레이스)이 **실기 진단을
> 방해했다.** 로그는 "실기에서 무슨 일이 있었나"의 유일한 근거인데 거기에 테스트
> 잡음이 섞이면 그 근거를 못 믿는다. `PLUIZ_LOG_DIR` 환경변수 + [`tests/_testenv.py`](../tests/_testenv.py)로
> 테스트는 임시 경로에 쓴다. **캐시 파일 쪽(아래)은 그대로 남아 있다.**
- **증상**: `tests/test_commands.py` · `tests/test_regression.py`를 돌리면 캐시 파일이
  modified 상태가 된다. `hit_count`·`last_used` 갱신은 물론, **테스트가 학습시킨
  동적 항목이 그대로 남는다.** (2026-09-02 실측: `"오늘 날짜 알려줘"`, `"메모장 가동해줘"`)
- **왜 문제인가**: `"메모장 가동해줘"`는 G-04가 캐시 미스를 만들려고 지어낸 표현이라
  제품 데이터가 아니다. 매 실행마다 `git status`가 더러워져 실제 변경과 섞이고,
  방치하면 테스트 잔여물이 커밋에 딸려 들어간다.
- **해결 (2026-09-05) — 위 1번으로 갔다.** `PLUIZ_CACHE_FILE` 환경변수
  (`core/command_cache.py:34`) + [`tests/_testenv.py`](../tests/_testenv.py)가 임시 경로로 돌린다.
  `PLUIZ_LOG_DIR`과 **완전히 같은 패턴**이라 새로 배울 것이 없다.
  - **mock 쪽은 끝났다.** `import _testenv` 한 줄이면 되므로 **새 테스트는 아무것도
    안 해도 안전하다.** 이미 각자 `CACHE_FILE`을 갈아끼우던 3개
    (`test_cache_learn`·`test_cache_api`·`test_cache_synonym`)도 그대로 동작한다.
  - **라이브 쪽은 서버가 쓰므로 여기서 못 막는다.** 서버 기동 시 주면 된다:
    `PLUIZ_CACHE_FILE=/tmp/pluiz_live.json python main.py`
    → [WORKFLOW § 라이브 테스트](WORKFLOW.md)에 적어 뒀다. **안 주면 예전과 같으므로
    `git checkout cache/command_cache.json` 안내는 남겨 둔다**(줄어든 게 아니라 선택지가 생긴 것).
- **고치다가 드러난 것 — 방향이 반대인 더 나쁜 문제**: `tests/test_bl15_truncation.py`가
  `get_cache()`로 **사용자의 실제 캐시를 읽고** 있었다. 오염을 걱정했는데 실은
  **테스트가 사용자 상태에 의존**하고 있었던 것이다 — 어떤 명령이 학습돼 있느냐에 따라
  같은 테스트가 통과했다 실패했다 할 수 있다. 43건짜리 스위트다.
  이제 시드만 든 새 캐시를 받으므로 **결정적이다.**
- **검증**: 캐시 관련 8스위트 + 그래프 2스위트 회귀 0
  (`cache_learn 15/15` · `cache_api 12/12` · `cache_synonym 15/15` · `bl15_truncation 43/43` ·
  `cache_wire 8/8` · `bl02_bl03 40/40` · `fast_path 11/11` · `graph_agent 6/6` ·
  `hitl_graph 59/59` · `graph_context 15/15`).
  **전체 mock 28파일 712개 통과**, 실행 후 `git status cache/` 깨끗.
- **상태**: ✅ **mock 해결 · 라이브는 옵트인** (2026-09-05)
- **성격**: 테스트 위생. (2026-09-02 라이브 검증 중 발견)

### BL-12 — `type_text`가 입력 성공을 **확인하지 않고** 성공을 보고한다
- 현재 `type_text`는 `pyautogui` 호출이 예외를 안 내면 무조건
  `"✓ 텍스트 입력 완료"`를 반환한다. **실제로 입력됐는지는 보지 않는다.**
  라이브 `INPUT-02`도 그래서 MANUAL로 남아 있다.
- **2026-09-02 실측** — Vision(`describe_screen`)으로 검증해 보니 도구는 `"✓ 입력 완료"`,
  Vision은 `"본문 0자, 비어 있음"`. 세 번 반복해도 같았다.
  - **직접 원인은 포커스였다.** 백그라운드 스크립트에서 `SetForegroundWindow`가
    거부돼(Windows 제한) 메모장이 전면에 오지 못했고, 키는 그때 포커스돼 있던
    **다른 창으로 갔다.** → 실사용 경로(사용자가 앞에 있는 상태)의 버그라는 증거는 아니다.
  - **그러나 도구가 그 사실을 전혀 모른 채 성공을 보고한다는 점은 그대로 문제다.**
- **해결 방향** (2단계):
  1. `type_text`가 입력 **전에 대상 창이 실제로 포커스됐는지 확인**하고, 아니면
     성공을 보고하지 말 것 (`✗ 포커스 확보 실패` 반환)
  2. 테스트는 `Ctrl+A` → `Ctrl+C` → `get_clipboard_text` 왕복 또는 `describe_screen`으로 검증
- **⚠️ 작업 시 주의**: `pyautogui`는 **포커스된 창**에 키를 보낸다. 백그라운드 스크립트에서
  `type_text`를 돌리면 사용자가 보고 있던 창에 글자가 들어간다.
  **자동화 스크립트에서 함부로 실행하지 말 것.**
- **2026-09-03 — 해결됨(해결 방향 1·2 모두).**
  - `type_text(text, target)` — **입력 전에** 대상 창을 찾고 포그라운드인지 확인한다.
    확인이 안 되면 **입력하지 않고** 어디에 들어갈 뻔했는지 알린다. 성공해도
    **어느 창에 넣었는지 밝힌다.**
  - `visual_verify` 노드가 **입력하려던 그 창**을 화면으로 재확인한다.
  - 계기: 같은 날 실기에서 *"메모장에 회의록 적어줘"* 가 **Pluiz 오버레이 입력창**에
    들어갔는데 도구는 `"✓ 입력 완료"`라고 답했다. 처음 만든 검증은 **활성창**을 찍어
    (=글자가 간 그 창) **거짓 성공에 화면 증거까지 붙여 줬다.** 순환 설계였다.
  - 검증: `tests/test_type_text_focus.py` 21개 · `tests/test_visual_verify.py` 42개.
- **성격**: 도구 정직성 + 테스트 정직성. (2026-09-02 Vision으로 확인)

### BL-18 — Vision 기능이 **주 모니터만** 본다
- **증상**: 전체 화면을 다루는 Vision 경로가 `PIL.ImageGrab.grab()`을 쓰는데, 이건
  **주 모니터만** 캡처한다. 보조 모니터에 있는 것은 `describe_screen`도 `find_ui_element`도
  `watch_screen`도 보지 못한다.
- **왜 이제 적나**: 원래 `find_ui_element` 하나의 한계였는데, 2026-09-03에 좌표 클릭과
  화면 감시가 붙으면서 **세 기능이 같은 한계를 공유**하게 됐다. 특히 감시는
  *"오류 뜨면 알려줘"* 라고 해 놓고 보조 모니터의 오류를 영영 못 본다 —
  **못 봤다는 사실조차 알려줄 수 없다.**
- **완화(현재)**: `window=` 로 창을 지정하면 그 창은 어느 모니터에 있든 `_capture_hwnd`가
  잡는다. 즉 **전체화면 모드에서만 문제**다.
- **해결 방향**: `ImageGrab.grab(all_screens=True)` + 캡처 원점을 가상 화면 좌표계
  기준으로 계산. ⚠️ 원점이 음수가 될 수 있어(주 모니터 왼쪽에 붙인 경우)
  `box_to_screen`·클릭 범위 검사가 함께 바뀌어야 한다. 좌표가 걸린 변경이라
  **실측 없이 손대지 말 것.**
- **성격**: 기능 한계. (2026-09-03 화면 감시 작업 중 정리)

### ✅ BL-13 — 웨이크워드 설정 UI가 없다 — 2026-09-07 완료
- **문제**: API(`POST /api/wakeword`)는 2026-09-02부터 열려 있었는데 **입력란이 없어서**
  사용자가 `.env`를 직접 열어야 했다.
- **한 일**: ⚙️ 설정 화면에 호출어 입력란 + 사용 토글. `wake_words_default`를
  플레이스홀더로 쓴다(비워 두면 실제로 그 값이 쓰인다).
- **API 키 저장과 일부러 분리했다** — 같은 버튼에 묶으면 호출어만 바꾸려는 사람이
  키를 다시 넣어야 하고 에이전트가 불필요하게 재초기화된다. 상태줄도 따로 둔다.
- **반영에 10초 걸린다고 말한다** — 웨이크워드는 Electron이 띄운 **별도 프로세스**라
  서버가 직접 못 바꾸고 `.env`를 다시 읽는다. 안 말하면 저장 직후 불러 보고
  **고장으로 오해한다.** 껐을 때는 대체 수단(Alt+Space)을 알려 준다.
- **회귀**: `tests/test_wakeword_ui.py` **25건**. 프런트와 서버가 다른 파일이라
  어긋나도 아무도 안 알려 준다 — 필드 이름(422) · 토큰 경로(401, BL-14) ·
  키 저장과의 분리 · 고지 문구를 소스 대조로 못 박았다. (2026-09-07)

### BL-04 — 그래프 코어 미이관 항목 (P1.5 후속)
- 도구 미호출 재시도(RETRY_PROMPT)를 PluizGraphAgent에 미이관. 실기 문제 없었음.
- /ws 토큰 단위 스트리밍 미구현(현재 단일 청크). 음성 경로 영향 없음.
- **성격**: 품질/UX. 필요 시 노드/엣지로 추가.

### ✅ BL-07 — 파일 찾기 UX — 2026-09-10 **완전 해결**
- 파일명/확장자가 모호하면 에이전트가 헤맴("개발착수서" 확장자 모를 때 등).
  find_file/open_file에 부분매칭·후보 제시·확장자 자동탐색 등 개선 여지.
- **2026-09-02 실기에서 이게 "UX"가 아니라 기능 구멍이라는 게 드러났다**:
  ```
  사용자: "바탕화면에 있는 폴더 이름 나열해 봐, 거기서 내가 고를게"
  Pluiz : "파일명이나 확장자를 지정해야 찾을 수 있다고 하네요"
  ```
  `find_file`은 **이름이나 확장자를 알아야** 동작하는데, 이름을 모를 때
  목록을 보여줄 수단이 도구 34개 중 **하나도 없었다.** 사용자는 폴더를
  '학교 문서'로 기억했지만 실제 이름은 '학교문'이라, 삭제 요청이 계속
  "없어요"로 끝나는데 확인할 방법이 없었다.
- **해결(절반)**: **`list_directory` 도구 신설** — `location`(바탕화면/문서/다운로드/
  절대경로) + `only`(all/folders/files). 비밀 파일·숨김·`desktop.ini` 제외,
  40개 초과 시 개수만. 검증: `tests/test_list_directory.py` 19개 + 라이브 확인.
- **나머지 절반 — 2026-09-10 해결.** `find_file`을 고쳤다.
  **고치기 전에 무엇이 실제로 막고 있는지 쟀다**(오기억 16쌍 평가셋):

  | 방법 | 긍정 | 오매칭 |
  |---|---:|---:|
  | 현행 glob (공백 그대로) | **3/8** | 0 |
  | 공백·구분자만 걷어내기 | **7/8** | **0** |
  | difflib 0.70 | 6/8 | 0 |

  🚨 **막고 있던 건 근사 매칭의 부재가 아니라 띄어쓰기였다.** «학교 문서» 하나만
  보고 «퍼지 매칭이 필요하다»고 결론냈으면 더 복잡한 걸 만들고 덜 고쳤을 것이다.
- **그래서 difflib는 «제안»에만 쓴다** — 정규화 매칭보다 성적이 나쁘고,
  마진도 좁다(`이력서`↔`이론서.pdf`가 **0.667**로 임계 0.70에 붙어 있다).
  [임베딩 캐시 ADR §6-3](design/M5_임베딩_캐시.md)에서 *"임베딩으로 실행을 결정하는 것은
  성립하지 않는다"* 고 정한 것과 **같은 모양**이다. 제안은 틀려도 사고가 아니다.
- **한 일**:
  - 띄어쓰기·구분자·대소문자를 정규화해 비교 (`개발 착수서` → `개발_착수서.hwp`)
  - 위치를 안 주면 바탕화면·다운로드·문서를 훑고, **시킨 곳이 아니었으면 밝힌다** —
    안 밝히면 다음 명령(*"그거 지워줘"*)이 **엉뚱한 걸 가리킨다**
  - 확장자로 0건이면 **빼고 다시** 찾는다 (LLM이 확장자를 지어냈을 수 있다)
  - 못 찾으면 가까운 이름을 제안한다. **폴더도 제안에 넣는다** —
    🚩 원문 사례(`학교 문서` → `학교문`)가 하필 **폴더**라, 안 넣으면 고치고도
    *"왜 없는지 확인할 방법이 없다"* 는 원래 불평이 그대로 남는다.
    결과 목록에는 폴더를 안 넣는다 — 이 도구의 계약은 «파일 탐색»이다
- ⚠️ **빠른 경로는 그대로다.** glob이 먼저 돌고(다운로드 6개에서 0.002초)
  **0건일 때만** 정규화 매칭으로 내려간다. 느린 경로를 기본으로 만들지 않았다.
  (실측: 바탕화면 5.3만 항목 walk 0.86초 · 세 곳 전체 탐색 1.55초)
- ⚠️ **한계**: 평가셋 16쌍은 손으로 만든 것이라 과적합일 수 있다. 다만 주된 이득
  (3/8 → 7/8)은 **띄어쓰기 정규화라는 기계적인 이유**에서 나오므로 새 패턴에도
  그대로 간다. difflib 쪽 숫자(6/8)는 덜 믿는다.
- **검증**: `tests/test_find_file.py` **28건** (임시 폴더만 쓴다 — 사용자의 실제
  바탕화면을 뒤지지 않는다) · 전체 mock **38파일 1167개**
- **성격**: 품질/UX. (2026-08-12 관찰 → 2026-09-02 절반 → **2026-09-10 완전 해결**)

### BL-10 — 수동 테스트 문서 3개 통합
- `docs/testing/`의 `TEST_CASES.md` · `MANUAL_TEST_CASES.md` · `MANUAL_TESTS.md`가
  **자동/수동 경계를 서로 다르게 잡고 있어** 셋 다 부분적으로 낡았다.
  (예: `MANUAL_TEST_CASES.md`는 "test_commands.py가 25개 검증"이라고 적고 있으나 현재는 54개)
- 2026-09-01 라이브 스위트를 신 엔진 기준으로 갱신했으므로, 그 기준으로 재정리 가능.
- **해결 방향**: 자동 검증되는 항목은 문서에서 빼고(테스트 코드가 진실), 정말 사람 눈이
  필요한 것만 한 문서로 모은다. 시각 확인 15건이 실제 MANUAL 대상이다.
- **해결 (2026-09-08)** — 셋을 [`testing/MANUAL_TESTS.md`](testing/MANUAL_TESTS.md)
  **하나(44건)** 로 합쳤다. 걷어낸 것: 보안 필터 11건(`test_injection.py`가 검증) ·
  캐시 경로 13건 · 조사 처리 5건 · 파일 생성 3건 · **구 엔진 회귀 12건.**
  마지막 것이 제일 중요하다 — `core/agent.py`와 `[PluizAgent]` 로그를 확인하라고
  적고 있었는데 **그 엔진은 2026-09-01에 제거됐다.** 실행할 수 없는 절차가
  «수동 테스트»로 남아 있었던 것이다.
  대신 **웨이크워드 2건**(⚠️ 미검증)을 새로 넣었고, 실행 안내를 `start.bat`(서버 전용)에서
  `launch.bat`으로 고쳤다.
- **남은 절반**: Vision·승인·계획의 확인 절차는 [TASKS](TASKS.md) ①에 있다.
  **지금 옮기면 «지금 하는 중»과 «상시 카탈로그»가 섞이므로**, TASKS ①이 정리될 때 옮긴다.
- **상태**: **해결** (문서 통합 완료 · 절차 이관은 TASKS ① 정리 시점)
- **성격**: 문서 유지보수. (2026-09-01 `docs/README.md`가 "통합 예정"이라 적고 있는데
  정작 BACKLOG에 항목이 없어 추가)

### BL-08 — README 데모 GIF/영상
- **필요성**: 음성으로 PC를 제어하는 프로젝트인데 README가 정적 텍스트뿐이라
  실제 체감이 전달되지 않는다. 포트폴리오로서 가장 큰 공백.
- **필요 작업**: 사용자가 직접 화면 녹화 (코드 작업 아님).
- **담을 내용 제안** (README 예시 명령과 맞추면 좋음):
  1. `"메모장 켜줘"` → 실행 → `"그거 꺼줘"` → 종료 (**맥락 이해**가 가장 인상적)
  2. `"바탕화면 임시파일 지워줘"` → *"정말 삭제할까요?"* (**HITL 승인**)
  3. 오프라인 상태에서 `"소리 좀 키워봐"` 즉시 동작 (**캐시**)
- **넣을 위치**: `README.md` 상단 "이런 게 됩니다" 절 바로 아래.
  파일은 `docs/presentation/`에 두고 상대경로로 참조 (GitHub는 GIF 인라인 재생 지원).
- **성격**: 품질/전달력. (2026-09-01 기록)

---

## 해결됨 (기록 보존)

### ✅ BL-14 — 로컬 서버가 아무 웹페이지에서나 조작 가능했다 — 2026-09-02 완료
- **증상**: `main.py`의 CORS가 `allow_origins=["*"]`이고 **인증이 없었다.**
  사용자가 열어 둔 아무 웹사이트가 다음 한 줄로 PC 제어 명령을 넣을 수 있었다.
  ```js
  fetch('http://127.0.0.1:8765/chat', {method:'POST',
    headers:{'Content-Type':'application/json'},
    body:JSON.stringify({text:'...'})})
  ```
  삭제는 HITL이 막지만 `open_app`·`type_text`·`open_url`·**`describe_screen`(화면을
  외부 LLM으로 전송)** 은 전부 통과했다. OWASP 4층 방어를 내세우는 프로젝트인데
  **가장 큰 구멍이 가드레일 바깥**에 있었다.

- **고치면서 알게 된 것 — 원래 적어 둔 해결 방향 ①이 틀렸다**:
  1. **`allow_origins`를 "Electron origin으로 축소"할 수 없다.** 렌더러는
     `loadFile`이라 출처가 `file://` → 브라우저가 `Origin: null`을 보낸다.
     그 값을 허용해야 UI 자신이 도는데 `null`은 아무 사이트의 sandboxed iframe도
     받는 값이다. 게다가 **CORS는 응답 읽기만 막고 요청 처리는 막지 않는다** —
     명령은 그대로 실행된다. **즉 CORS로는 애초에 못 막는다. 토큰이 유일한 방어다.**
  2. **`/ws`에는 CORS가 아예 적용되지 않는다.** 웹페이지가
     `new WebSocket('ws://127.0.0.1:8765/ws')`로 그냥 붙을 수 있어 **fetch보다 큰
     구멍**이었는데 원래 항목에 적혀 있지 않았다.
  3. **CORS 프리플라이트를 인증에서 면제해야 한다.** `X-Pluiz-Token`은 safelisted
     헤더가 아니라 렌더러의 모든 요청이 프리플라이트를 거치는데 거기엔 토큰이 실리지
     않는다. 여기서 401을 주면 UI 자신이 전부 막힌다.
  4. **서버는 Electron이 띄우지 않는다** (`launch.bat`이 둘을 따로 띄운다).
     env로 토큰을 건넬 수 없어 **파일 핸드셰이크**가 필요했다.

- **해결**:
  - **[신규] `core/auth.py`** — 기동 시 `secrets.token_urlsafe(32)` 발급 →
    `cache/.auth_token`. Electron과 라이브 테스트가 같은 파일을 읽는다.
    **웹페이지는 로컬 파일을 못 읽는다 — 이게 방어의 근거다.**
    stdlib만 쓴다(CI가 fastapi를 설치하지 않으므로 mock 테스트가 돌아야 한다).
  - `main.py` — `auth_guard` HTTP 미들웨어(`/health`만 면제, 실패 시 401) ·
    `/ws`는 `accept()` 전에 쿼리 토큰 검사 후 `close(1008)` ·
    CORS `["*"]` → `["null"]` + 메서드/헤더 축소 · `TrustedHostMiddleware`(DNS
    리바인딩 차단) · `ConfigRequest.provider`를 `Literal`로 · `api_key` 개행 제거
    (`.env` 인젝션) · `/cache/ui`는 토큰을 HTML에 주입하고 전체 URL을 기동 로그에 출력
  - `config/settings.py` `auth_enabled`(기본 `true`) — 사고 시 탈출구
  - Electron 3파일 — `main.js` 토큰 파일 폴링 + `get-token` IPC ·
    `preload.js` `getToken` · 렌더러는 `fetch`를 **한 번 감싸서** 호출부 6곳을 안 건드림
  - 라이브 3스위트가 토큰을 자동으로 읽도록 `requests.Session()`으로 전환

- **검증**:
  - **[신규] `tests/test_auth.py` 42개** — 발급/읽기 왕복 · 면제 경로 · 헤더/쿼리 매칭 ·
    부분일치·공백·대소문자 변형 거부 · fail-closed. **Python 상수 ↔ JS 리터럴 계약**도
    검사해 한쪽만 고치는 드리프트를 막는다
  - **라이브 실측 16항목** — 토큰 없는 `/chat` 401 · 틀린 토큰 401 · 올바른 토큰 200 ·
    토큰 없는 WS 거절 · `Origin: null` 프리플라이트 통과(UI 정상) · 공격 출처 CORS 거절 ·
    Host 위조 400 · provider 인젝션 422
  - **라이브 3스위트 전부 통과** (인증 켠 상태) — `regression` **35/35**
    (신규 **AUTH-01~AUTH-04** 포함) · `commands` 39P/0F · `sprint1_2` 55/55
  - mock 20파일 회귀 0건

- **후속 수정 — 서버 이중 기동 사고** (같은 날, 라이브 검증 중 발견):
  uvicorn은 **소켓을 잡기 전에 lifespan을 돌린다.** 그래서 서버가 이미 떠 있는데
  또 띄우면, 죽을 운명인 두 번째 인스턴스가 **살아 있는 첫 번째의 토큰을 덮어쓰고
  지운다.** 첫 번째 서버는 멀쩡한데 UI만 재연결에서 영영 거절된다
  (`launch.bat` 두 번 실행 시 재현). → `port_in_use()`로 **발급 전에** 확인하고,
  `clear_token(expected=...)`로 **내 토큰일 때만** 지우게 했다.

- **남은 한계 (의도한 것)**: 로컬에서 실행 중인 **다른 프로그램**은 토큰 파일을 읽을 수
  있다. 그 수준의 공격자는 이미 PC에서 코드를 실행 중이라 이 앱을 거칠 이유가 없다 —
  위협모델 밖이다. → [ARCHITECTURE § 보안 0층](ARCHITECTURE.md#보안--5층-방어)

### ✅ BL-06 — 테스트 스위트 새 엔진 기준 갱신 — 2026-09-01 완료
- **증상**: 라이브 테스트 2개가 구 엔진 기준으로 작성돼 있었다.
- **실제로 깨져 있던 것**: `test_regression.py`의 **R-07**이 `core/agent.py`를 읽는데
  그 파일은 M1-P5에서 `archive/`로 옮겨졌다 → 항상 실패하는 상태였다.
- **더 큰 공백**: 신 엔진의 P2~P4 기능(HITL·하이브리드 가드레일·출력 마스킹·캐시 학습)에
  **라이브 검증이 아예 없었다.** mock은 로직을 덮지만 실제 서버 경로는 미검증이었다.
- **해결**:
  - R-07 → `core/graph.py` 기준으로 교체. 오타 검사에 더해 신 엔진 프롬프트의
    핵심 지시(도구 강제 호출·P3-3 자기검증) 포함 여부까지 검증.
  - **신규 G-01~G-04** — HITL 승인(삭제→질문→거부 시 파일 보존) / 하이브리드 가드레일 /
    출력 마스킹 / 캐시 동적 학습(`/cache` 반영 확인)
  - **신규 S-09 · C-01** — BL-03·BL-02 수정이 실제 서버 경로에서도 먹는지
  - `test_commands.py`에 **TOOL-01** 추가 — `DANGEROUS_TOOLS ⊆ 등록 도구` 불변식.
    엔진 단일화로 "구 엔진엔 삭제 도구를 안 준다"는 이중 방어가 사라져,
    이제 `DANGEROUS_TOOLS` 등록이 유일한 안전장치이기 때문.
  - 두 파일 헤더에 실행법·mock과의 역할 분담·주의사항 명시
- **주의**: G-02(하이브리드 가드레일)는 **온라인 전용**. 오프라인이면 LLM 판정기가
  skip 돼 통과할 수 있다.
- **검증**: 컴파일 통과, 깨진 파일 참조 0건, R-07 4항목·TOOL-01 로직 사전 확인 완료.
  **전체 실행은 Windows에서 서버를 띄우고 사용자가 해야 한다.**

### ✅ BL-03 — 보안필터 공백 없는 변형 미포착 — 2026-09-01 완료
- **증상**: `rm-rf`(공백 없이)를 정규식 `rm\s+-[rRfF]`가 놓쳤다. 실기에선 LLM이
  대신 거절해 안전했으나 코드필터가 1차로 잡는 게 맞다.
- **조사 중 추가 발견**: 같은 문제가 **5개 패턴에 더** 있었다 —
  `del/f/s` · `rmdir/s/q` · `taskkill/f` · `shutdown/r` · `schtasks/delete` · `cipher/w`.
- **해결**: 명령어와 플래그 사이 공백을 `\s+` → `\s*`로 완화 (8개 패턴).
  앞의 단어경계 ``가 `warm-rf` 같은 단어 내부 매칭을 막아 오탐을 방지한다.
  `net user x y`처럼 공백이 있어야 의미가 성립하는 건 `\s+`를 유지했다.
- **검증**: `tests/test_bl02_bl03.py` — 차단 16 + 오탐방지 6.
  `check_security`의 위험 명령어 패턴에 **mock 테스트가 아예 없었던 것**도 함께 해소
  (기존엔 서버가 필요한 `test_commands.py`에만 있었다).

### ✅ BL-02 — 캐시 부정어 오매칭 — 2026-09-01 완료
- **증상**: "계산기 **말고** 다른거 열어" → 캐시가 `계산기 열어줘`로 오매칭해 계산기를 엶.
- **원인**: 캐시가 entity(계산기)+action(열어)만 보고 부정어를 무시한다.
  부정어는 의미를 뒤집으므로 캐시 매칭 자체를 신뢰할 수 없다.
- **해결**: `core/fast_path.py`에 `_NEGATION_CMD` / `has_negation()` 추가하고
  `is_compound_command()`에 연결 → 부정어가 있으면 캐시를 **조회조차 하지 않고**
  LLM이 문장 전체를 해석한다.
- **오탐 방지**: 어절 경계를 요구한다(`말고(?=\s|$)`). 무조건 찾으면
  "말고기 검색해줘" · "대신동 지도" 같은 정상 입력이 캐시를 못 탄다.
- **커버 어휘**: 말고/말구/아니라/아니고/대신/대신에/이외/외에/하지 말/열지 마/끄지 마
- **검증**: `tests/test_bl02_bl03.py` — 부정어 9 + 오탐방지 5 + 통합 4.
- **메모**: 기존 BACKLOG에 해결 위치가 `agent.py`로 적혀 있었으나, 그 파일은
  M1-P5에서 `archive/`로 이동했다. 실제 위치는 `core/fast_path.py`였다.

### ✅ BL-09 — 노출된 Gemini API 키 — 2026-09-01 완료
- **무슨 일**: `tests/test_sensitive.py`의 마스킹 테스트가 실제 형식 Google API 키
  (`AIzaSyClLQ`로 시작)를 하드코딩하고 있었고 `.env.example`에도 같은 값이 있었다.
  `main`까지 푸시된 상태였고, **저장소는 public**이었다.
- **조치**:
  - `1d2ca22` — 테스트의 키를 명백한 더미(`AIzaSyFAKE...`)로 교체
  - `a244d7e` — `.env.example`을 플레이스홀더로 재작성 + 저장소에 포함되도록 수정
  - CI `secret-guard` 잡 추가 (`.env` 추적 · 실제 키 패턴 · `.pyc`) — 재발 방지
  - **키 폐기 완료** (사용자, Google AI Studio)
- **이력 재작성은 하지 않음**: 이미 푸시된 공개 커밋이라 `filter-repo`로 지우려면
  전체 이력 재작성 + 강제 푸시가 필요하고 협업 브랜치가 깨진다.
  **키를 폐기했으므로 이력에 남은 문자열은 무의미하다.**
- **교훈**: 테스트용 더미 값에는 `FAKE`/`DUMMY`/`EXAMPLE`을 넣는다. CI가 이를 기준으로
  실제 키와 구분한다. → [WORKFLOW.md § CI](WORKFLOW.md#ci-github-actions)

### ✅ BL-05 — 구 엔진 완전 제거 (엔진 단일화) — 2026-09-01 완료
- `core/agent.py`(771줄) → `archive/core_agent_v1.py` 로 이동(삭제 아님),
  `core/factory.py`·`USE_GRAPH` 플래그 제거. `PluizGraphAgent` 단일 엔진으로 수렴.
- **"서두르지 말 것(롤백 낙하산)" 전제가 무효화돼 진행함**: 신 엔진이 `_build_llm`
  하나 때문에 구 엔진을 import 하고 있었고(폴백으로서 이미 깨짐), P2~P4 개선이
  구 엔진에 반영되지 않아 되돌리면 HITL·가드레일·캐시학습이 전부 사라지는 상태였다.
- **문제 발생 시 참고**: [design/M1_P5_엔진단일화.md](design/M1_P5_엔진단일화.md) —
  제거 배경, 기능 대응표(구 함수 → 신 위치), git 복원 명령.

---

_최종 업데이트: 2026-09-04 (BL-19 신설·부분 해결 — 감시 도구 미호출)_
